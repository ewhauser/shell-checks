# [\#2171 Issue](https://github.com/koalaman/shellcheck/issues/2171) `open`: local associative array seen as global

#### <img src="https://avatars.githubusercontent.com/u/1452158?u=c0f0ba99529b1bd12060da93562493af3ff1588f&v=4" width="50">[wybodekker](https://github.com/wybodekker) opened issue at [2021-03-11 22:12](https://github.com/koalaman/shellcheck/issues/2171):

#### For bugs
- Rule Id: SC2190
- My shellcheck version (`shellcheck --version` or 'online'): online and local version 0.7.1
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit
- [ ] It's not reproducible on shellcheck.net, but I think that's because it's an OS, configuration or encoding issue

#### Here's a snippet or screenshot that shows the problem:

#!/bin/bash

f() {
   local -A map
   map[x]=assoc
   map[y]=array
   echo "local map: ${map[*]}"
}

g() {
   local -a map
   map+=(index)
   map+=(arr)
}

f
echo "x: ${map[x]} y: ${map[y]}"
g
echo "global map: ${map[*]}"
```

#### Here's what shellcheck currently says:
```
Line 12	SC2190: Elements in associative arrays need index, e.g. array=( [index]=value ) .
Line 13	SC2190: Elements in associative arrays need index, e.g. array=( [index]=value ) .
```
#### Here's what I wanted or expected to see:
it should give error code SC2034 instead: map appears unused. Verify...




#### <img src="https://avatars.githubusercontent.com/u/965550?u=d09f03c1186bb5cff2ed7d576a769d16dc84eb3b&v=4" width="50">[pmhahn](https://github.com/pmhahn) commented at [2021-09-14 08:04](https://github.com/koalaman/shellcheck/issues/2171#issuecomment-918912606):

```sh
# shellcheck shell=bash
foo () {
        declare -a cmd=(1 2 3)
        echo "func ${cmd[*]}"
}
cmd="bar"
foo
echo "main ${cmd[*]}"
```
results in
> Line 6:
> cmd="bar"
> ^-- SC2178: Variable was used as an array but is now assigned a string.

while `cmd` is local to the function `foo` only and does not spill out


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
