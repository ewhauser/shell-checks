# [\#507 Issue](https://github.com/koalaman/shellcheck/issues/507) `closed`: allow the expansion of ~ when sourcing files

#### <img src="https://avatars.githubusercontent.com/u/126441?v=4" width="50">[travi](https://github.com/travi) opened issue at [2015-10-15T02:48:55Z](https://github.com/koalaman/shellcheck/issues/507):

In scripts that I use across multiple systems, I use `~` when referencing my home directory since it is at a different location on each system. However, recent versions have been giving me warnings related to [`SC1091`](https://github.com/koalaman/shellcheck/wiki/SC1091) even when running as `shellcheck -x` for files that do exist. 

Would it be reasonable to enable shellcheck to follow paths that include `~` like this?

#### <img src="https://avatars.githubusercontent.com/u/302055?v=4" width="50">[deryni](https://github.com/deryni) commented at [2015-10-15T18:19:20Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-148478667):

Why `~` instead of `$HOME`? Which I would imagine shellcheck would handle better.


#### <img src="https://avatars.githubusercontent.com/u/126441?v=4" width="50">[travi](https://github.com/travi) commented at [2015-10-15T18:34:40Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-148482667):

I would be open to that too, but the same problem exists. Shellcheck does not expand  `$HOME` and gives the (very similar) [`SC1090`](https://github.com/koalaman/shellcheck/wiki/SC1090) warning. This warning was added to shellcheck earlier, which ended up being part of the reason I became more consistent with using `~` instead. 


#### <img src="https://avatars.githubusercontent.com/u/302055?v=4" width="50">[deryni](https://github.com/deryni) commented at [2015-10-15T18:48:55Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-148486125):

Ah. Sounds like shellcheck should possibly understand `~` in that location as non-constant and get you back to SC1090 then. =)

shellcheck not assuming `$HOME` is valid for the current environment somewhat unreasonable. It might be nice to have a way to tell shellcheck what the "environment" of the running script will look like for things like this to work though.


#### <img src="https://avatars.githubusercontent.com/u/126441?v=4" width="50">[travi](https://github.com/travi) commented at [2015-10-15T21:36:38Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-148527764):

Sounds reasonable, but I do hope that expanding both `~` and `$HOME` could be supported as both are valid and I personally think `~` is a bit cleaner.

Also, I've realized that not all of the cases where I use shellcheck are in a shell instance (like [the atom plugin](https://atom.io/packages/linter-shellcheck), so env vars would not be available directly. I do think that populating the "environment" would be important.


#### <img src="https://avatars.githubusercontent.com/u/302055?v=4" width="50">[deryni](https://github.com/deryni) commented at [2015-10-16T01:11:59Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-148565338):

`$HOME` is unambiguous in its expansion.

`~` is a prefix to a number of possible expansions (which isn't a reason not to support the simple expansion this is just a comment).

`$HOME` also expands in variables where `~` doesn't (and while this doesn't come up much in general it is surprisingly common for people to try and get confused when it doesn't just look on StackOverflow for example).

That being said I'm not sure using either of those in scripts is all that good of an idea. Finding the location of the currently executing script and using that to find bundled scripts seems like a more robust plan to me (and would be something that shellcheck could, I think, easily detect the common idioms for and support but that's not my decision).


#### <img src="https://avatars.githubusercontent.com/u/126441?v=4" width="50">[travi](https://github.com/travi) commented at [2015-10-16T01:32:06Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-148570830):

All good points. I do believe that shellcheck already warns about `~` not expanding in certain circumstances. Probably related: when using `$HOME`, shellcheck also applies its normal variable rules and recommends things like quoting to prevent word splitting, etc. Using `~` should be able to avoid those steps safely because of the difference in how expansion works, correct?

My primary use case for using either of these is related to my [dotfiles](https://github.com/travi/dotfiles) where many of the scripts need to be relative to the home directory. However, I version all of the scripts in a separate directory and symlink them from there to the proper location. Because of that, referencing them relative to the home directory definitely simplifies things.


#### <img src="https://avatars.githubusercontent.com/u/302055?v=4" width="50">[deryni](https://github.com/deryni) commented at [2015-10-16T02:32:49Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-148580496):

Yes, tilde expansion is implicitly quoted and not subject to field splitting and pathname expansion.

From the [spec](http://pubs.opengroup.org/onlinepubs/9699919799/utilities/V3_chap02.html#tag_18_06_01):

> The pathname resulting from tilde expansion shall be treated as if quoted to prevent it being altered by field splitting and pathname expansion.

And yeah, I see why you want to use `~` there instead of `$HOME`.


#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2015-10-17T18:35:12Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-148940696):

It would be both reasonable and convenient, and `source` resolution needs more work in general. 

However, to postpone any decisions about whether to use shellcheck's HOME or the script's HOME or the user's home dir or whether to support ~foo and ~+ and ~- and whether to account for PATH with `./` as fallback like bash does or PATH with no fallback like dash does, and again which PATH to use,  630f20e just adds a kludge so shellcheck doesn't consider `~/` a static path (just so you'll get a more understandable error message). 

Maybe you can add an example config in the script's source dir and use that, or run shellcheck from the dir with the include files you want?


#### <img src="https://avatars.githubusercontent.com/u/126441?v=4" width="50">[travi](https://github.com/travi) commented at [2015-10-19T15:56:38Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-149257132):

I think the change to get the other error reporting was an appropriate step, but I don't understand what you mention about a config? I found some other issues mentioning a `~/.shellcheck` file, but don't find any concrete reference to something like that. 

Could you expand on how I could do what you are referring to?


#### <img src="https://avatars.githubusercontent.com/u/126441?v=4" width="50">[travi](https://github.com/travi) commented at [2016-08-28T05:21:37Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-242956996):

any chance there is a better recommendation than simply disabling the `SC1090` rule for this use case by this point?


#### <img src="https://avatars.githubusercontent.com/u/5931862?v=4" width="50">[reynoldscem](https://github.com/reynoldscem) commented at [2019-01-17T11:16:13Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-455135999):

@travi Did you ever find a nice way around this? I also want to use shellcheck with my dotfiles (and so refer to things relative to my home).

#### <img src="https://avatars.githubusercontent.com/u/126441?v=4" width="50">[travi](https://github.com/travi) commented at [2019-01-17T16:38:35Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-455240391):

I don't believe so, but it has been a bit of time since I've tried very hard. It looks like i have a mix of switching `~` to `$HOME` and leaving `~`, but disabling `SC1090` with a comment. I dislike the fact that I'm not consistent, but I must have lost motivation since there wasn't a supported was to lint for consistently using the "right" way.

Interesting timing with your question though, I also got [a notification](https://github.com/creationix/nvm/issues/1277#issuecomment-455175289) about a thread I've been following for `nvm`, which suggests that `~` is faster than `$HOME`. I haven't done anything to verify the claim, but if true, I'd want to undo any of my changes to use `$HOME`. 

#### <img src="https://avatars.githubusercontent.com/u/133706?v=4" width="50">[stdedos](https://github.com/stdedos) commented at [2022-04-19T09:51:16Z](https://github.com/koalaman/shellcheck/issues/507#issuecomment-1102415975):

> Maybe you can add an example config in the script's source dir and use that, or run shellcheck from the dir with the include files you want?

Well, an example file could be the Ubuntu's default `.bashrc`:

```bash
...
if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi
...
```

We can freely argue semantics; but I'd like for it to be "clean cut and working out of the box" without (much) additional changes/configs/lintings.
