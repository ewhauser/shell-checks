# [\#885 Issue](https://github.com/koalaman/shellcheck/issues/885) `closed`: SC2199 warns on [[ ${ARRAY[@]:x:1} == ... ]]

#### <img src="https://avatars.githubusercontent.com/u/92684?v=4" width="50">[floatingatoll](https://github.com/floatingatoll) opened issue at [2017-04-21T23:58:31Z](https://github.com/koalaman/shellcheck/issues/885):

#### For bugs
- Rule Id: SC2199
- My shellcheck version: online
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit

#### Here's a snippet or screenshot that shows the problem:

```bash
#!/bin/bash
A=(1 2 3); if [[ ${A[@]:0:1} == '1' ]]; then true; fi
```

#### Here's what shellcheck currently says:

SC2199: Arrays implicitly concatenate in [[ ]]. Use a loop (or explicit * instead of @).

#### Here's what I wanted or expected to see:

No warning, since the array is sliced to a single element before implicit concatenation occurs.

#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2017-04-23T02:33:01Z](https://github.com/koalaman/shellcheck/issues/885#issuecomment-296415121):

You're right in this particular case because the array is statically defined, but in general this is not the case: it will expand to 1 *or 0* elements.

Here's what happens in `[` which doesn't implicitly concatenate:

```
$ A=(); [ "${A[@]:0:1}" == "foo" ]  && echo ok
bash: [: ==: unary operator expected
```

If you want 0 elements to be considered an empty string, you should explicitly concatenate the 0 or 1 elements so that you always end up with a string to compare. Alternatively, you can use "${A[0]}" which is shorter and amounts to the same.

#### <img src="https://avatars.githubusercontent.com/u/92684?v=4" width="50">[floatingatoll](https://github.com/floatingatoll) commented at [2017-04-23T02:54:54Z](https://github.com/koalaman/shellcheck/issues/885#issuecomment-296415884):

ooo.

Thank you! Sorry to bother.

> On Apr 22, 2017, at 19:33, koalaman <notifications@github.com> wrote:
> 
> You're right in this particular case because the array is statically defined, but in general this is not the case: it will expand to 1 or 0 elements.
> 
> Here's what happens in [ which doesn't implicitly concatenate:
> 
> $ A=(); [ "${A[@]:0:1}" == "foo" ]  && echo ok
> bash: [: ==: unary operator expected
> If you want 0 elements to be considered an empty string, you should explicitly concatenate the 0 or 1 elements so that you always end up with a string to compare. Alternatively, you can use "${A[0]}" which is shorter and amounts to the same.
> 
> —
> You are receiving this because you authored the thread.
> Reply to this email directly, view it on GitHub, or mute the thread.
> 


#### <img src="https://avatars.githubusercontent.com/u/92684?v=4" width="50">[floatingatoll](https://github.com/floatingatoll) commented at [2017-04-23T02:55:45Z](https://github.com/koalaman/shellcheck/issues/885#issuecomment-296415911):

(I suppose there's an opportunity for :x:1 warning specifically recommending the simpler syntax.)
