# [\#3030 Issue](https://github.com/koalaman/shellcheck/issues/3030) `open`: "SC2004 (style): $/${} is unnecessary on arithmetic variables" is too strict

#### <img src="https://avatars.githubusercontent.com/u/16990250?u=09e51ee7d77ac546ee6656b2ac1c2aaeaabe32f7&v=4" width="50">[laurenthuberdeau](https://github.com/laurenthuberdeau) opened issue at [2024-07-26 19:09](https://github.com/koalaman/shellcheck/issues/3030):

#### For bugs
- Rule Id: `SC2004`
- My shellcheck version (`shellcheck --version` or "online"): `0.10.0`
- [x] The rule's wiki page does not already cover this (e.g. https://shellcheck.net/wiki/SC2086)
- [x] I tried on https://www.shellcheck.net/ and verified that this is still a problem on the latest commit

```sh
#!/bin/sh

foo=1
var1="foo"

echo $(($var1))
```

#### Here's what shellcheck currently says:

```
[Line 3:](javascript:setPosition(3, 1))
foo=1
^-- [SC2034](https://www.shellcheck.net/wiki/SC2034) (warning): foo appears unused. Verify use (or export if used externally).
 
[Line 6:](javascript:setPosition(6, 9))
echo $(($var1))
        ^-- [SC2004](https://www.shellcheck.net/wiki/SC2004) (style): $/${} is unnecessary on arithmetic variables.
```

#### Here's what I wanted or expected to see:

In POSIX shell, indirect expansion of numeric variables can be done using variable expansion in arithmetic expansion. `$((var1))` is not equivalent to `$(($var1))`, the first returns `0` and the second `12`. 

The https://www.shellcheck.net/wiki/SC2004 page should mention this exception.

#### <img src="https://avatars.githubusercontent.com/u/15793015?u=4f3dce4311464d558795ba29dd30e14bf9940d1e&v=4" width="50">[ale5000-git](https://github.com/ale5000-git) commented at [2024-07-27 02:03](https://github.com/koalaman/shellcheck/issues/3030#issuecomment-2253707790):

I'm not sure where you have tried but with this:
```
foo=1
var1="foo"
echo $(($var1))
echo $((var1))
```
you get:
```
1
1
```

I have tried both in `bash` and `busybox ash`.

#### <img src="https://avatars.githubusercontent.com/u/16990250?u=09e51ee7d77ac546ee6656b2ac1c2aaeaabe32f7&v=4" width="50">[laurenthuberdeau](https://github.com/laurenthuberdeau) commented at [2024-07-28 02:05](https://github.com/koalaman/shellcheck/issues/3030#issuecomment-2254313410):

I use dash which outputs:

```
1
test.sh: 4: Illegal number: foo
```

Curiously, ash, bash, ksh and zsh all seem to do the same thing where variables are recursively expanded until variables (except for variables that appear left in assignment operators) no longer appear in the expression. Dash seems to only expand each variables once and so it errors on `foo` as it's not a valid integer literal.

Take this code for example:

```
baz=12
bar="baz"
foo="bar"
var1="foo"
echo $(($var1))
echo $((var1))
```

It produces on bash, ksh, zsh:

```
12
12
```

while dash produces:

```
test.sh: 5: Illegal number: bar
```

#### <img src="https://avatars.githubusercontent.com/u/15793015?u=4f3dce4311464d558795ba29dd30e14bf9940d1e&v=4" width="50">[ale5000-git](https://github.com/ale5000-git) commented at [2024-07-30 02:51](https://github.com/koalaman/shellcheck/issues/3030#issuecomment-2257357647):

@koalaman
I think that SC2004 should be skipped for dash and there should be a new rule specific to dash that say to avoid multiple levels of "variable variables".


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
