# [\#908 Issue](https://github.com/koalaman/shellcheck/issues/908) `closed`: For Bash builtin 'source filename', if filename does not contain a slash, the PATH variable is NOT used to find filename

#### <img src="https://avatars.githubusercontent.com/u/407695?v=4" width="50">[jesuspv](https://github.com/jesuspv) opened issue at [2017-05-17T07:16:54Z](https://github.com/koalaman/shellcheck/issues/908):

#### For bugs
- Rule Id: SC1091
- My shellcheck version:  0.4.6
- [ ] I tried on shellcheck.net and verified that this is still a problem on the latest commit
- [x] It's not reproducible on shellcheck.net, but I think that's because it's an OS, configuration or encoding issue

From [Bash manual](https://www.gnu.org/software/bash/manual/html_node/Bourne-Shell-Builtins.html#Bourne-Shell-Builtins):
>. (a period)
> . filename [arguments]
>Read and execute commands from the filename argument in the current shell context. **If filename does not contain a slash, the PATH variable is used to find filename**. When Bash is not in POSIX mode, the current directory is searched if filename is not found in $PATH. If any arguments are supplied, they become the positional parameters when filename is executed. Otherwise the positional parameters are unchanged. If the -T option is enabled, source inherits any trap on DEBUG; if it is not, any DEBUG trap string is saved and restored around the call to source, and source unsets the DEBUG trap while it executes. If -T is not set, and the sourced file changes the DEBUG trap, the new value is retained when source completes. The return status is the exit status of the last command executed, or zero if no commands are executed. If filename is not found, or cannot be read, the return status is non-zero. This builtin is equivalent to source.

Note that [`source` is a synonym for `.`](https://www.gnu.org/software/bash/manual/html_node/Bash-Builtins.html).

#### For new checks and feature suggestions
- [ ] shellcheck.net (i.e. the latest commit) currently gives no useful warnings about this
- [x] I searched through https://github.com/koalaman/shellcheck/issues and didn't find anything related

#### Here's a snippet or screenshot that shows the problem:

```sh
# directory structure
$ tree .
.
├── cmd
│   └── mycmd.sh
└── lib
    └── mylib.sh

# PATH containing lib directory to resolve source Bash builtin
export PATH+=:$PWD/lib

# 'source' example with no slash (PATH variable is used to find mylib.sh according to Bash manual).
# Please, note that mylib.sh content is not relevant for this issue.
$ cat cmd/mycmd.sh
#!/bin/bash
source mylib.sh

# test
shellcheck --shell=bash --external-sources cmd/mycmd.sh
```

#### Here's what shellcheck currently says:

```
In cmd/mycmd.sh line 2:
source mylib.sh
^-- SC1091: Not following: mylib.sh: openBinaryFile: does not exist (No such file or directory)
```

#### Here's what I wanted or expected to see:

Results of also checking `mylib.sh`, which can be found in `$PATH`.

#### <img src="https://avatars.githubusercontent.com/u/2199511?v=4" width="50">[Nightfirecat](https://github.com/Nightfirecat) commented at [2017-05-17T18:12:21Z](https://github.com/koalaman/shellcheck/issues/908#issuecomment-302179271):

Ok, so Shellcheck isn't following `$PATH`, where bash is? It took me a little while to understand your comment since the title doesn't match what is written in the bash manual.

#### <img src="https://avatars.githubusercontent.com/u/407695?v=4" width="50">[jesuspv](https://github.com/jesuspv) commented at [2017-05-18T10:05:36Z](https://github.com/koalaman/shellcheck/issues/908#issuecomment-302360421):

Sorry for any inconvenience. The title matches the sentence in bold but for the 'NOT' (which is what I understand as an issue).

Regarding your question, `bash` is in `/bin/bash`.

Thank you very much for taking the time to understand my comment.

#### <img src="https://avatars.githubusercontent.com/u/90273?v=4" width="50">[LukeShu](https://github.com/LukeShu) commented at [2017-09-27T07:26:47Z](https://github.com/koalaman/shellcheck/issues/908#issuecomment-332433982):

I think the real problem here is that it *isn't* triggering SC1090.  If `$PATH` is being involved in the lookup, then  it isn't a constant location.  It's been my experience that most programmers don't expect it to search `$PATH`, so it would probably be a good thing to warn on; perhaps give it a new number though, so it can be toggled separately.

#### <img src="https://avatars.githubusercontent.com/u/2211051?v=4" width="50">[HariSekhon](https://github.com/HariSekhon) commented at [2020-02-13T12:10:23Z](https://github.com/koalaman/shellcheck/issues/908#issuecomment-585725619):

I prefer to source explicitly anchored to the source directory of the script as it is more explicit and intuitive than searching `$PATH` as most shell scripts are not written that way and it's not a commonly expected behaviour of shell scripters in my experience.

I've raised a related ticket https://github.com/koalaman/shellcheck/issues/1837 to support a $srcdir or equivalent variable in `source` statements or search the source directory of the script if not found in $PWD.

#### <img src="https://avatars.githubusercontent.com/u/407695?v=4" width="50">[jesuspv](https://github.com/jesuspv) commented at [2020-03-06T21:01:24Z](https://github.com/koalaman/shellcheck/issues/908#issuecomment-595963633):

First of all, let me thank you for the effort supporting such tools to make our shell scripting more pleasant and secure.

IMHO, the mechanism Bash offers us in the form of "free" sources is a key enabler for
importing system libraries in our scripts (other concerns would be how Bash could avoid imported name clashing or installing those libraries in a convenient way). Unfortunately, as you mention, people seem to not be aware of this mechanism, and tend to design their scripts without bearing in mind such opportunity for software reuse.

More importantly, I would also like to point out to the fact that this behaviour is part of the Bash specification. Regardless anyone's experience, not supporting it makes shellcheck fail in what this tool should be checking.

#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2021-10-15T19:19:42Z](https://github.com/koalaman/shellcheck/issues/908#issuecomment-944594465):

ShellCheck has since gotten a `source-path` directive that can be used to specify where ShellCheck looks for included files. It can be set to absolute paths, and paths relative to the cwd or script file. This can be set in the script itself, or in a `.shellcheckrc` in the project. Hopefully this will cover all these use cases.

See https://github.com/koalaman/shellcheck/wiki/Directive
