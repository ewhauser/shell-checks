# [\#2248 Issue](https://github.com/koalaman/shellcheck/issues/2248) `closed`: SC2093 exec fd operation not recognised

#### <img src="https://avatars.githubusercontent.com/u/133706?u=c19422ae9646a1a58b9c62a57a2143e62c5f8001&v=4" width="50">[stdedos](https://github.com/stdedos) opened issue at [2021-06-09 19:36](https://github.com/koalaman/shellcheck/issues/2248):

#### For bugs
- Rule Id (if any, e.g. SC1000): SC2093
- My shellcheck version (`shellcheck --version` or "online"): online
- [x] The rule's wiki page does not already cover this (e.g. https://shellcheck.net/wiki/SC2086)
- [x] I tried on https://www.shellcheck.net/ and verified that this is still a problem on the latest commit

#### Here's a snippet or screenshot that shows the problem:

```sh
#!/bin/bash
#~/.bashrc: executed by bash(1) for non-login shells.
# kevin gallagher (@ageis) <kevingallagher@gmail.com>

function precmd_git_dirty() {
        
		BASH_XTRACEFD="19"
		exec "${BASH_XTRACEFD}"> "$(dirname "${BASH_SOURCE[0]}")/xtrace_bash_${FUNCNAME[0]}.log"
	if [ -d "$PWD/.git" ]; then
		git diff-index --quiet HEAD -- &>/dev/null
		local GITSTATUS=$?
		if [ "$GITSTATUS" -eq 1 ]; then
			failure "The current working directory is a Git repository with unstaged changes."
		fi
	fi
 }

#preexec_functions+=(precmd_git_dirty)
#precmd_functions+=(precmd_git_dirty)

# exit status emoji
#export PROMPT_COMMAND='echo -ne "$(smiley) \033]0;${USER}@${HOSTNAME}: ${PWD}\007"'
#export PROMPT_COMMAND='echo -ne "$(smiley)$(__vte_osc7) \033]0 ${USER}@${HOSTNAME}: ${PWD}\007"'

```

#### Here's what shellcheck currently says:

```
$ shellcheck myscript
 
Line 8:
                exec "${BASH_XTRACEFD}"> "$(dirname "${BASH_SOURCE[0]}")/xtrace_bash_${FUNCNAME[0]}.log"
                ^-- SC2093: Remove "exec " if script should continue after this command.
```

#### Here's what I wanted or expected to see:

Nothing. Instead of "evaluating" `BASH_XTRACEFD` myself, I leave it to the terminal (I've set it before to a number).


#### <img src="https://avatars.githubusercontent.com/u/133706?u=c19422ae9646a1a58b9c62a57a2143e62c5f8001&v=4" width="50">[stdedos](https://github.com/stdedos) commented at [2021-06-09 19:38](https://github.com/koalaman/shellcheck/issues/2248#issuecomment-858040035):

Actually, no - too trigger happy (bash does not understand this)


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
