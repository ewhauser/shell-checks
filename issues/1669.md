# [\#1669 Issue](https://github.com/koalaman/shellcheck/issues/1669) `open`: SC2178: detection of "if … then … else"

#### <img src="https://avatars.githubusercontent.com/u/41806113?v=4" width="50">[Tamaranch](https://github.com/Tamaranch) opened issue at [2019-08-12T18:19:44Z](https://github.com/koalaman/shellcheck/issues/1669):

#### For bugs
- Rule Id (if any, e.g. SC1000): SC2178 (and related, e.g. SC2128)
- My shellcheck version (`shellcheck --version` or "online"): online
- [x] The rule's wiki page does not already cover this (e.g. https://shellcheck.net/wiki/SC2086)
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit

#### Here's a snippet or screenshot that shows the problem:

```sh

#!/bin/bash

if [[ "$1" == "array" ]]; then
        a=(1 2 3)
        echo "${a[@]}"
else
        a="1 2 3"
        echo "$a"
fi

```

#### Here's what shellcheck currently says:

Line 7:
        a="1 2 3"
        ^-- SC2178: Variable was used as an array but is now assigned a string.
 
Line 8:
        echo "$a"
              ^-- SC2128: Expanding an array without an index only gives the first element.

#### Here's what I wanted or expected to see:

Nothing: if we assigns `a="1 2 3"`, then `a` was not used as an array.
