# [\#135 Issue](https://github.com/koalaman/shellcheck/issues/135) `closed`: nested ((..)) in $((..))

#### <img src="https://avatars.githubusercontent.com/u/1205722?v=4" width="50">[bahamas10](https://github.com/bahamas10) opened issue at [2014-04-11T17:43:29Z](https://github.com/koalaman/shellcheck/issues/135):

```
In ../basher/basher line 335:
    log "run finished in $seconds $(((seconds == 1)) && echo second || echo seconds)"
            ^-- SC1009: The mentioned parser error was in this double quoted string.
                                      ^-- SC1073: Couldn't parse this $((..)) expression.
                                                       ^-- SC1072: Unexpected " ". Fix any mentioned problems and try again.
```

AFAICT: this code is good and properly nested (it works).

My goal in that line is simple: to print "second" if `$seconds` is 1 (singular), or to print "seconds" if `$seconds` is !=1 (plural).

However, there may be a better way or idiom to do what i'm trying to do.

#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2014-04-12T23:49:16Z](https://github.com/koalaman/shellcheck/issues/135#issuecomment-40294845):

Wow. `$(((foo)))` is interpretted like `$(( (foo) ))` while `$(((foo)) )` is correctly interpretted as `$( ((foo))  )`. Apparently bash does some lookahead magic here. 


#### <img src="https://avatars.githubusercontent.com/u/70800?v=4" width="50">[Wilfred](https://github.com/Wilfred) commented at [2014-07-18T14:05:14Z](https://github.com/koalaman/shellcheck/issues/135#issuecomment-49434185):

I think this parse error is affecting me too. Here's a small testcase:

```
#!/bin/bash

for v in $((set -o posix;set) | \grep "$var_name"); do
    echo
done
```

Output:

```
for v in $((set -o posix;set) | \grep "$var_name"); do
^-- SC1009: The mentioned parser error was in this for loop.
         ^-- SC1058: Expected 'do'.
         ^-- SC1073: Couldn't parse this $((..)) expression.
         ^-- SC1072: Unexpected '$'. Fix any mentioned problems and try again.
```


#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2016-06-27T05:15:06Z](https://github.com/koalaman/shellcheck/issues/135#issuecomment-228655704):

Fixed in 43c2606
