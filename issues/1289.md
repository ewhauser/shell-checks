# [\#1289 Issue](https://github.com/koalaman/shellcheck/issues/1289) `open`: False positive for SC2057

#### <img src="https://avatars.githubusercontent.com/u/748033?v=4" width="50">[pwagland](https://github.com/pwagland) opened issue at [2018-07-12T09:50:33Z](https://github.com/koalaman/shellcheck/issues/1289):

#### For bugs
- Rule Id (if any, e.g. SC1000): 
SC2057

- My shellcheck version (`shellcheck --version` or "online"): 
```sh
$ shellcheck --version
ShellCheck - shell script analysis tool
version: 0.5.0
license: GNU General Public License, version 3
website: https://www.shellcheck.net
```

- [X] I tried on shellcheck.net and verified that this is still a problem on the latest commit

#### Here's a snippet or screenshot that shows the problem:

```sh
#!/bin/bash
if [ \! -d "${SRC_DIR}" ]
then
  echo "could not find the expected source directory: ${SRC_DIR}"
  exit 1
fi
```

#### Here's what shellcheck currently says:

```
Line 2:
if [ \! -d "${SRC_DIR}" ]
        ^-- SC2057: Unknown binary operator.
```


#### Here's what I wanted or expected to see:

Nothing, the `!` operator is allowed, and `\!` should be treated identically, since that is what the shell does.

#### <img src="https://avatars.githubusercontent.com/u/45886288?v=4" width="50">[cdoconno](https://github.com/cdoconno) commented at [2019-10-11T13:17:35Z](https://github.com/koalaman/shellcheck/issues/1289#issuecomment-541060739):

This is also the case for `if [\! -f "${FILE_NAME}"]`
#### For bugs
* Rule Id (if any, e.g. SC1000): SC2057
* My shellcheck version(`shellcheck --version` or "online")
```bash
$ shellcheck --version
ShellCheck - shell script analysis tool
version: 0.7.0
license: GNU General Public License, version 3
website: https://www.shellcheck.net
```

- [X] I tried on shellchec.net and verified this is still a problem on the latest commit 

#### Here's a snippet or screenshot that shows the problem:
```
#!/bin/bash

CONFFILE="$(pwd)/config.conf"
if [ \! -f "$CONFFILE" ]; then
  echo "$SCRIPT: No configuration file found \"$CONFFILE\""
  exit 1
fi
```

#### Here's what shellcheck currently says:
```
Line 4:
if [ \! -f "$CONFFILE" ]; then
        ^-- SC2057: Unknown binary operator.
```

This appears to be an issue with both `-f` and `-d` to check for existence of files or directories
