# [\#2370 Issue](https://github.com/koalaman/shellcheck/issues/2370) `closed`: Bug: `--format=diff` introduce syntax errors when there are multiple issues for a single token

#### <img src="https://avatars.githubusercontent.com/u/16078332?u=f9f1459ea9036ce3d6479c266297b628a58c6870&v=4" width="50">[XuehaiPan](https://github.com/XuehaiPan) opened issue at [2021-11-09 11:44](https://github.com/koalaman/shellcheck/issues/2370):

#### For bugs
- Rule Id (if any, e.g. SC1000): all (SC2086, SC2250)
- My shellcheck version (`shellcheck --version` or "online"): 0.8.0
- [X] The rule's wiki page does not already cover this (e.g. https://shellcheck.net/wiki/SC2086)
- [X] I tried on https://www.shellcheck.net/ and verified that this is still a problem on the latest commit


#### Here's a snippet or screenshot that shows the problem:

https://github.com/Homebrew/homebrew-command-not-found/blob/master/handler.sh

```sh
#
# brew-command-not-found script for macOS
#
# Usage: Source it somewhere in your .bashrc (bash) or .zshrc (zsh)
#
# Author: Baptiste Fontaine
# URL: https://github.com/Homebrew/homebrew-command-not-found
# License: MIT
# Version: 0.2.0
#

if ! which brew > /dev/null; then return; fi

homebrew_command_not_found_handle() {

    local cmd="$1"

    # The code below is based off this Linux Journal article:
    #   http://www.linuxjournal.com/content/bash-command-not-found

    # do not run when inside Midnight Commander or within a Pipe, except if CI
    if test -z "$CONTINUOUS_INTEGRATION" && test -n "$MC_SID" -o ! -t 1 ; then
        [ -n "$BASH_VERSION" ] && \
            TEXTDOMAIN=command-not-found echo $"$cmd: command not found"
        # Zsh versions 5.3 and above don't print this for us.
        [ -n "$ZSH_VERSION" ] && [[ "$ZSH_VERSION" > "5.2" ]] && \
            echo "zsh: command not found: $cmd" >&2
        return 127
    fi

    local txt="$(brew which-formula --explain $cmd 2>/dev/null)"

    if [ -z "$txt" ]; then
        [ -n "$BASH_VERSION" ] && \
            TEXTDOMAIN=command-not-found echo $"$cmd: command not found"

        # Zsh versions 5.3 and above don't print this for us.
        [ -n "$ZSH_VERSION" ] && [[ "$ZSH_VERSION" > "5.2" ]] && \
            echo "zsh: command not found: $cmd" >&2
    else
        echo "$txt"
    fi

    return 127
}

if [ -n "$BASH_VERSION" ]; then
    command_not_found_handle() {
        homebrew_command_not_found_handle $*
        return $?
    }
elif [ -n "$ZSH_VERSION" ]; then
    command_not_found_handler() {
        homebrew_command_not_found_handle $*
        return $?
    }
fi
```

#### Here's what shellcheck currently says:

The output of `shellcheck --format=diff --shell=bash --enable=all handler.sh`:

```diff
--- a/handler.sh
+++ b/handler.sh
@@ -19,39 +19,39 @@
     #   http://www.linuxjournal.com/content/bash-command-not-found
 
     # do not run when inside Midnight Commander or within a Pipe, except if CI
-    if test -z "$CONTINUOUS_INTEGRATION" && test -n "$MC_SID" -o ! -t 1 ; then
-        [ -n "$BASH_VERSION" ] && \
-            TEXTDOMAIN=command-not-found echo $"$cmd: command not found"
+    if test -z "${CONTINUOUS_INTEGRATION}" && test -n "${MC_SID}" -o ! -t 1 ; then
+        [ -n "${BASH_VERSION}" ] && \
+            TEXTDOMAIN=command-not-found echo $"${cmd}: command not found"
         # Zsh versions 5.3 and above don't print this for us.
-        [ -n "$ZSH_VERSION" ] && [[ "$ZSH_VERSION" > "5.2" ]] && \
-            echo "zsh: command not found: $cmd" >&2
+        [ -n "${ZSH_VERSION}" ] && [[ "${ZSH_VERSION}" > "5.2" ]] && \
+            echo "zsh: command not found: ${cmd}" >&2
         return 127
     fi
 
-    local txt="$(brew which-formula --explain $cmd 2>/dev/null)"
+    local txt="$(brew which-formula --explain ${cmd"} 2>/dev/null)"
 
-    if [ -z "$txt" ]; then
-        [ -n "$BASH_VERSION" ] && \
-            TEXTDOMAIN=command-not-found echo $"$cmd: command not found"
+    if [ -z "$${xt"} ]; then
+        [ -n "$${ASH_VERSION"} ] && \
+            TEXTDOMAIN=command-not-found echo $"$${md:} command not found"
 
         # Zsh versions 5.3 and above don't print this for us.
-        [ -n "$ZSH_VERSION" ] && [[ "$ZSH_VERSION" > "5.2" ]] && \
-            echo "zsh: command not found: $cmd" >&2
+        [ -n "$${SH_VERSION"} ] && [[ "$${SH_VERSION"} > "5.2" ]] && \
+            echo "zsh: command not found: $${md"} >&2
     else
-        echo "$txt"
+        echo "$${xt"}
     fi
 
     return 127
 }
 
-if [ -n "$BASH_VERSION" ]; then
+if [ -n "$${ASH_VERSION"} ]; then
     command_not_found_handle() {
-        homebrew_command_not_found_handle $*
-        return $?
+        homebrew_command_not_found_handle $"*
+"        return $?
     }
-elif [ -n "$ZSH_VERSION" ]; then
+elif [ -n "$${SH_VERSION"} ]; then
     command_not_found_handler() {
-        homebrew_command_not_found_handle $*
-        return $?
+        homebrew_command_not_found_handle $"*
+"        return $?
     }
 fi
```

`shellcheck` introduces syntax errors after the line `local txt ...`.

Tiny snippet to reproduce:

```diff
$ echo 'local txt="$(brew which-formula --explain $cmd 2>/dev/null)"' | shellcheck --format=diff --shell=bash --enable=all -
--- a/-
+++ b/-
@@ -1,1 +1,1 @@
-local txt="$(brew which-formula --explain $cmd 2>/dev/null)"
+local txt="$(brew which-formula --explain ${cmd"} 2>/dev/null)"
```

There are multiple fixable issues for token `$cmd`.

```console
$ echo 'local txt="$(brew which-formula --explain $cmd 2>/dev/null)"' | shellcheck --shell=bash --enable=all -

In - line 1:
local txt="$(brew which-formula --explain $cmd 2>/dev/null)"
^---^ SC2168 (error): 'local' is only valid in functions.
      ^-^ SC2034 (warning): txt appears unused. Verify use (or export if used externally).
      ^-^ SC2155 (warning): Declare and assign separately to avoid masking return values.
                                          ^--^ SC2154 (warning): cmd is referenced but not assigned.
                                          ^--^ SC2086 (info): Double quote to prevent globbing and word splitting.
                                          ^--^ SC2250 (style): Prefer putting braces around variable references even when not strictly required.

Did you mean: 
local txt="$(brew which-formula --explain "$cmd" 2>/dev/null)"

For more information:
  https://www.shellcheck.net/wiki/SC2168 -- 'local' is only valid in functions.
  https://www.shellcheck.net/wiki/SC2034 -- txt appears unused. Verify use (o...
  https://www.shellcheck.net/wiki/SC2154 -- cmd is referenced but not assigned.
```

No errors when I fix it in cascading:

```diff
$ echo 'local txt="$(brew which-formula --explain $cmd 2>/dev/null)"' | shellcheck --format=diff --shell=bash -
--- a/-
+++ b/-
@@ -1,1 +1,1 @@
-local txt="$(brew which-formula --explain $cmd 2>/dev/null)"
+local txt="$(brew which-formula --explain "$cmd" 2>/dev/null)"

$ echo 'local txt="$(brew which-formula --explain "$cmd" 2>/dev/null)"' | shellcheck --format=diff --shell=bash --enable=all -
--- a/-
+++ b/-
@@ -1,1 +1,1 @@
-local txt="$(brew which-formula --explain "$cmd" 2>/dev/null)"
+local txt="$(brew which-formula --explain "${cmd}" 2>/dev/null)"
```

#### Here's what I wanted or expected to see:

```diff
$ echo 'local txt="$(brew which-formula --explain $cmd 2>/dev/null)"' | shellcheck --format=diff --shell=bash --enable=all -
--- a/-
+++ b/-
@@ -1,1 +1,1 @@
-local txt="$(brew which-formula --explain $cmd 2>/dev/null)"
+local txt="$(brew which-formula --explain "${cmd}" 2>/dev/null)"
```


#### <img src="https://avatars.githubusercontent.com/u/10134766?u=f49488226855e99f56c850ccf2573c5f5f8a067c&v=4" width="50">[razziel89](https://github.com/razziel89) commented at [2022-05-08 08:52](https://github.com/koalaman/shellcheck/issues/2370#issuecomment-1120377396):

I just found this issue because I run into the problem often and thought maybe a smaller example could help. A script like this one already triggers it:
```bash
#!/bin/bash
echo $PATH
```
where the diff suggested by `shellcheck --format=diff --enable=all test.sh` is then:
```diff
--- a/test.sh
+++ b/test.sh
@@ -1,2 +1,2 @@
 #!/bin/bash
-echo $PATH
+echo ${PATH"}
```

A quick fix could be to only fix one issue at a time, which would not fix everything in on go but keep the file intact.

Is there any way I could help? I don't have experience with Haskell, though.

#### <img src="https://avatars.githubusercontent.com/u/310867?v=4" width="50">[chutzimir](https://github.com/chutzimir) commented at [2022-07-01 04:05](https://github.com/koalaman/shellcheck/issues/2370#issuecomment-1171909239):

> A quick fix could be to only fix one issue at a time, which would not fix everything in on go but keep the file intact.

Thanks. This is a good workaround. What I ended up doing is fixing all issues from one kind at a time, which produced a good file in a couple of rounds.

```bash
(
for code in $(shellcheck -o all $file -f json | jq -r '.[] | .code' | sort -u)
do
  shellcheck -o all -i $code $file -f diff | patch -p1
done
)
```

I wanted to check if multiple issues on the same line which have the same code would trigger a problem, and they don't:

```bash
#!/usr/bin/env bash
echo $PATH $HOSTNAME $SHELL
```

Here is the output of the diff for the checks one by one

```diff
--- a/sample
+++ b/sample
@@ -1,3 +1,3 @@
 #!/usr/bin/env bash

-echo $PATH $HOSTNAME $SHELL
+echo "$PATH" "$HOSTNAME" "$SHELL"

--- a/sample
+++ b/sample
@@ -1,3 +1,3 @@
 #!/usr/bin/env bash

-echo $PATH $HOSTNAME $SHELL
+echo ${PATH} ${HOSTNAME} ${SHELL}
```

and if trying to do them all in one shot:

```diff
--- a/sample
+++ b/sample
@@ -1,3 +1,4 @@
 #!/usr/bin/env bash

-echo $PATH $HOSTNAME $SHELL
+echo ${PATH"} $${OSTNAME "}$S${ELL
+"}
```

#### <img src="https://avatars.githubusercontent.com/u/2818758?u=40d36e49d938fda80abd46d6347b9bace1f73dd4&v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2022-07-26 17:25](https://github.com/koalaman/shellcheck/issues/2370#issuecomment-1195767148):

ShellCheck's merging of text replacements is kludgy, and in diff output it unintentionally made it worse by checking individual ranges for overlap instead of the fix as a whole. This has now been improved to hopefully not ruin the replacement, even though it may require a couple of passes.


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
