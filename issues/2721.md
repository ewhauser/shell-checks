# [\#2721 Issue](https://github.com/koalaman/shellcheck/issues/2721) `open`: ShellCheck busy loops and allocates memory until it is killed by OOM killer

#### <img src="https://avatars.githubusercontent.com/u/227483?v=4" width="50">[kdudka](https://github.com/kdudka) opened issue at [2023-03-22 09:00](https://github.com/koalaman/shellcheck/issues/2721):

#### For bugs
- My shellcheck version (`shellcheck --version` or "online"):  0.9.0
- Originally reported in Red Hat Bugzilla: https://bugzilla.redhat.com/show_bug.cgi?id=2180035

#### Here's a snippet or screenshot that shows the problem:
A (gzipped) script that triggers the issue is attached: [testsuite.gz](https://github.com/koalaman/shellcheck/files/11038108/testsuite.gz)

I was not able to isolate the problem to a small shell script.  Perhaps the attached shell script is just too long/complex for shellcheck to process it?

#### Here's what shellcheck currently says:
shellcheck busy loops and allocates memory until it is killed by OOM killer.


#### Here's what I wanted or expected to see:
shellcheck should print static analysis results and exit successfully.

#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2023-03-23 08:39](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-1480787180):

probably same as #2652 (guessed reason is in a [comment)](https://github.com/koalaman/shellcheck/issues/2652#issuecomment-1368662822).

> This is undoubtedly due to ShellCheck's new data flow analysis engine. It takes great care to be acceptably fast even for larger scripts, but 22,000 lines is something else. 

you have about 307 742 lines in that test file.

#### <img src="https://avatars.githubusercontent.com/u/227483?v=4" width="50">[kdudka](https://github.com/kdudka) commented at [2023-03-23 08:59](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-1480815936):

@brother Thanks for the pointer!  Unfortunately, in our case the problem happens with `shellcheck-0.7.2`, too.

#### <img src="https://avatars.githubusercontent.com/u/96933655?u=09108f0f354534e0706b13ad19f105cf8adc7d44&v=4" width="50">[ogios](https://github.com/ogios) commented at [2023-08-20 08:55](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-1685229194):

same problem even if i close the nvim, and i have to forcibly power off

#### <img src="https://avatars.githubusercontent.com/u/53572814?u=1edea9550d4e9113126cd674f024fd8b0c2015e6&v=4" width="50">[nukemiko](https://github.com/nukemiko) commented at [2024-01-20 17:37](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-1902188256):

Same problem happened during run shellcheck for a configure script (7.6k, 20986 lines).

#### <img src="https://avatars.githubusercontent.com/u/6209108?u=ab7547d1bebac415d323896b6564624efd036aaf&v=4" width="50">[magicus](https://github.com/magicus) commented at [2024-01-31 13:15](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-1919083074):

I have encountered the same issue with huge generated scripts made by autoconf. 

I had a look at the attached testsuite.gz file. It turns out to be generated by autotest (part of the autotools suite, just as autoconf). Some searching indicates that this is based on https://github.com/firewalld/firewalld/blob/main/src/tests/testsuite.at, but that is not really relevant.

I bisected the file to try and understand what caused the issue. The large file itself was not a problem; if you cut the file in half, one half completes shellcheck in 0.1 seconds, while the other spins forever consuming more and more memory. (I hit ctrl-c after ~10 seconds as an indicator of whether or not the problem was present).

I managed to get it down to a single line of difference wether shellcheck hangs or not. It is an opening `(`, which has no obvious matching closing `)` (not one that I could easily find, anyway).

#### <img src="https://avatars.githubusercontent.com/u/6209108?u=ab7547d1bebac415d323896b6564624efd036aaf&v=4" width="50">[magicus](https://github.com/magicus) commented at [2024-01-31 13:18](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-1919088319):

Here is the patch that turns `testsuite` from a CPU/memory hogger to completing in 0.2 seconds on my computer:

```
--- testsuite	2024-01-31 14:15:15
+++ testsuite.working	2024-01-31 14:16:06
@@ -2326,7 +2326,6 @@
 at_fn_group_banner 1 'firewall-cmd.at:5' \
   "basic options" "                                  " 1
 at_xfail=no
-(
   printf "%s\n" "1. $at_setup_line: testing $at_desc ..."
   $at_traceon
```

#### <img src="https://avatars.githubusercontent.com/u/6209108?u=ab7547d1bebac415d323896b6564624efd036aaf&v=4" width="50">[magicus](https://github.com/magicus) commented at [2024-01-31 13:20](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-1919090911):

It is not just the matching of `(` itself that is problematic. I had a hunch that a long search for the closing parenthesis could trigger this bug, so I created this script to generate a huge test file enclosed within a `(...)` pair. But it worked fine with shellcheck.

```
#!/bin/bash
SCRIPT=foo.sh
rm $SCRIPT
echo "#!/bin/bash" > $SCRIPT
echo "(" >> $SCRIPT
for i in {1..1000000} ; do
  echo "echo Line $i" >>$SCRIPT
done
echo ")" >> $SCRIPT
```

#### <img src="https://avatars.githubusercontent.com/u/6209108?u=ab7547d1bebac415d323896b6564624efd036aaf&v=4" width="50">[magicus](https://github.com/magicus) commented at [2024-02-06 08:33](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-1929014047):

As I said in #2652, this does not seem to be related to the extended analysis.

Furthermore, when closely looking at the memory consumption, I see that it increases not gradually, but in huge discreet steps. My guess is that it is a single array that is getting reallocated over and over as it grows without bound.

#### <img src="https://avatars.githubusercontent.com/u/6209108?u=ab7547d1bebac415d323896b6564624efd036aaf&v=4" width="50">[magicus](https://github.com/magicus) commented at [2024-02-06 10:11](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-1929183581):

I have turned my eye toward the script that is problematic for me. (This is the shell script generated by autoconf for the OpenJDK project.) I have attached the script in question here:
[autoconf-reproducer.zip](https://github.com/koalaman/shellcheck/files/14177861/autoconf-reproducer.zip)

When I run this on my M1 mac, it seemingly never ends. After about 1 minute of running, at which point it consumed > 6 GB RAM, I aborted it.

However, when a single line is deleted from the file, the analysis finished in 4-5 seconds (which is reasonable, considering that the file is ~ 170k lines), and I could not even measure the RAM consumption.

The patch that does this magic trick is here:
```
--- autoconf-BAD        2024-02-06 10:57:23
+++ autoconf-GOOD       2024-02-06 10:57:32
@@ -25504,7 +25504,6 @@
     fi # end with or without slashes
 
     # Now we have a usable command as new_path, with arguments in arguments
-    if test "x$OPENJDK_BUILD_OS" = "xwindows"; then
       if test "x$fixpath_prefix" = x; then
         # Only mess around if fixpath_prefix was not given
 
```

Now I realize that this deletes an opening `if` statements, and that will throw off all subsequent syntax. However, my point here is that it is not reasonable to expect shellcheck to handle one file perfectly fine, but fail miserably if a single line is added.

So the issue is not really the huge file of the script per se. I understand that a 170 k line script is massive, but without the problematic construct this works perfectly fine. 

My guess is that there is some special construct in here that triggers a bad behavior in shellcheck. It seems like the complexity in both time and memory is growing with O(n^x) with the number of lines in the file when this construct is encountered.

Hence the large reproducer. A smaller reproducer would not show the bad complexity convincingly. I've tried my best to get it down to a single line difference. My hope is that someone more versed in shellcheck debugging can figure out exactly what problem this additional line provokes. I am a complete noob at Haskell; sorry. Otherwise I'd tried running the "bad" file for a while and then checked which array it is that is growing without bound. That'll probably give a decent clue to what the problem is. Maybe @koalaman can have a look?

#### <img src="https://avatars.githubusercontent.com/u/6209108?u=ab7547d1bebac415d323896b6564624efd036aaf&v=4" width="50">[magicus](https://github.com/magicus) commented at [2024-02-06 10:23](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-1929204173):

I also tried this with the latest version, `v0.9.0-99-gd80fdfa`, and running as `shellcheck --extended-analysis=false autoconf-BAD`.

This gave a better result -- the memory consumption stayed below 3 GB, and the command actually finished after somewhat more than 2 minutes. (Just to confirm, I re-ran without `--extended-analysis=false` and waited 3 minutes. It was still running by then, and had consumed 20 GB memory so I had to kill it.)

However, it is still a far cry from the patched file, which finishes in 5 seconds, both with and without `--extended-analysis=false`.

So whatever the problem is, it is aggravated by the extended analysis, but it definitely exists even without it.

#### <img src="https://avatars.githubusercontent.com/u/36262513?u=d4078efc45621fcdbea585010009187b49a0092b&v=4" width="50">[LucunJi](https://github.com/LucunJi) commented at [2024-05-08 16:43](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-2100989438):

I am having a similar issue when editing my `.bashrc` with nvim + mason + bash-language-server (which calls shellcheck for linting). After exploring a bit, I found it's because a reference that finally leading to the file `/usr/share/nvm/nvm.sh`, which is only 4000 lines of code but takes 10 seconds and 2.6G memory to analyze.

Different from the result from @magicus https://github.com/koalaman/shellcheck/issues/2721#issuecomment-1929204173, setting `--extended-analysis=false` helps with my issue (1.3 seconds and 130M memory). I'm not an expert of shell script, so failed pinning down the part causing the issue, even after bisecting the script.

A workaround is adding a line `# shellcheck external-sources=false` before the line causing problem.

- OS: arch linux, with kernel `Linux 6.8.9-arch1-1 x86_64`
- shellcheck version: 0.10.0
- script causing the issue: [nvm.zip](https://github.com/koalaman/shellcheck/files/15251970/nvm.zip) (from nvm 0.39.7)

#### <img src="https://avatars.githubusercontent.com/u/82731?u=34b023ed000b19ecdb861d5a0afd8f1d249ae9d9&v=4" width="50">[juhp](https://github.com/juhp) commented at [2024-05-13 12:12](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-2107411878):

I am not familiar with the shellcheck code or architecture at all, so I probably shouldn't comment
but sounds to me like shellcheck should fail faster for a broken (unbalanced) shell script:
maybe they can just induce too many errors?

#### <img src="https://avatars.githubusercontent.com/u/136646048?v=4" width="50">[jobeirne-nydig](https://github.com/jobeirne-nydig) commented at [2024-10-24 01:22](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-2434010020):

<img width="845" alt="image" src="https://github.com/user-attachments/assets/cdabd45f-1051-46c1-baa0-cfbab88bfaf3">

I'm seeing this issue using 0.10.0 on an arm64 macbook, as invoked through `bash-language-server` in nvim.

Our codebase makes use of `# shellcheck source=`, and I'm wondering if maybe shellcheck is getting into some kind of infinite recursion on that basis. But that's just a guess.

#### <img src="https://avatars.githubusercontent.com/u/136646048?v=4" width="50">[jobeirne-nydig](https://github.com/jobeirne-nydig) commented at [2024-10-24 12:45](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-2435197413):

I've resorted to running this while developing with shellcheck:
```sh
#!/bin/bash

# Memory limit in kilobytes (5 GiB = 5 * 1024 * 1024 KB)
MEMORY_LIMIT=$((5 * 1024 * 1024))

echo "Defending your system against rogue shellchecks..."

while true; do
    ps -eo comm,pid,rss | while read -r COMM PID RSS; do
      if [[ "$COMM" == "shellcheck" && "$RSS" -gt "$MEMORY_LIMIT" ]]; then
        echo "Process $COMM (PID: $PID) is using $RSS KB of memory, exceeding limit. Killing it."
        
        kill -9 "$PID"
      fi
    done

    sleep 5
done
```

#### <img src="https://avatars.githubusercontent.com/u/133886?u=d44a156a4214131eaecf83d6207dc0b50345a5c0&v=4" width="50">[jntesteves](https://github.com/jntesteves) commented at [2024-11-24 16:05](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-2496087406):

While writing a performance benchmark for a script, I've created a 64 MB string and hit this bug with a very small reproducer:

```
#!/bin/sh
btx=.................................................................................................100.................................................................................................200.................................................................................................300.................................................................................................400
btx="$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx" # 20x
btx="$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx" # 20x
btx="$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx" # 20x
btx="$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx$btx" # 20x

for i; do
	:
done
```

The loop causes the issue. It can be any loop, after a large enough string, it fills up my 16 GB of RAM. If I comment out the last `btx=` assignment, shellcheck completes successfully on my PC, while using some ~7 GB of RAM in the process.

My reproducer does not misbehave with `--extended-analysis=false`, completing in 0.01s.

#### <img src="https://avatars.githubusercontent.com/u/6503931?v=4" width="50">[smprather](https://github.com/smprather) commented at [2025-06-08 15:40](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-2954142839):

I hit this bug trying to open a `configure` file (automake/autoconf `configure`) in nvim. 100% CPU and eventually fills up 16GB of RAM. And when I exit nvim, the process continues. I have to kill it manually.

#### <img src="https://avatars.githubusercontent.com/u/78285518?v=4" width="50">[hueychen27](https://github.com/hueychen27) commented at [2025-07-06 14:17](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-3041786405):

> I've resorted to running this while developing with shellcheck:
> 
> #!/bin/bash
> 
> # Memory limit in kilobytes (5 GiB = 5 * 1024 * 1024 KB)
> MEMORY_LIMIT=$((5 * 1024 * 1024))
> 
> echo "Defending your system against rogue shellchecks..."
> 
> while true; do
>     ps -eo comm,pid,rss | while read -r COMM PID RSS; do
>       if [[ "$COMM" == "shellcheck" && "$RSS" -gt "$MEMORY_LIMIT" ]]; then
>         echo "Process $COMM (PID: $PID) is using $RSS KB of memory, exceeding limit. Killing it."
>         
>         kill -9 "$PID"
>       fi
>     done
> 
>     sleep 5
> done

Set the memory limit to be lower to keep your fan quiet.

#### <img src="https://avatars.githubusercontent.com/u/78285518?v=4" width="50">[hueychen27](https://github.com/hueychen27) commented at [2025-07-06 21:44](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-3042790951):

> I've resorted to running this while developing with shellcheck:
> 
> #!/bin/bash
> 
> # Memory limit in kilobytes (5 GiB = 5 * 1024 * 1024 KB)
> MEMORY_LIMIT=$((5 * 1024 * 1024))
> 
> echo "Defending your system against rogue shellchecks..."
> 
> while true; do
>     ps -eo comm,pid,rss | while read -r COMM PID RSS; do
>       if [[ "$COMM" == "shellcheck" && "$RSS" -gt "$MEMORY_LIMIT" ]]; then
>         echo "Process $COMM (PID: $PID) is using $RSS KB of memory, exceeding limit. Killing it."
>         
>         kill -9 "$PID"
>       fi
>     done
> 
>     sleep 5
> done

My version which is more strict:
```bash
#!/bin/bash

# Memory limit in kilobytes (5 GiB = 5 * 1024 * 1024 KB)
MEMORY_LIMIT=$((5 * 1024 * 1024))
# VM limit in kilobytes (4 TB = 4 * 1000 * 1000 * 1000 KB)
VM_LIMIT=$((4 * 1000 * 1000 * 1000))

echo "Defending your system against rogue shellchecks..."

while true; do
    total_vsize=0
    ps -eo comm,pid,rss,vsize | while read -r COMM PID RSS VSIZE; do
        if [[ "$COMM" == "shellcheck" ]]; then
            total_vsize=$((total_vsize + VSIZE))
            if [[ "$RSS" -gt $MEMORY_LIMIT ]]; then
                echo "Process $COMM (PID: $PID) is using $RSS KB of memory, exceeding limit, $MEMORY_LIMIT KB. Killing it."
                kill -TERM "$PID"
                total_vsize=$((total_vsize - VSIZE))
            fi
            if [[ $total_vsize -gt $VM_LIMIT ]]; then
                echo "Process $COMM (PID: $PID) contributes to the aggregrate virtual memory size, $total_vsize KB of virtual memory, exceeding limit, $VM_LIMIT KB. Killing it."
                kill -TERM "$PID"
                total_vsize=$((total_vsize - VSIZE))
            fi
        fi
    done

    sleep 2
done
```

#### <img src="https://avatars.githubusercontent.com/u/11627708?v=4" width="50">[e-kwsm](https://github.com/e-kwsm) commented at [2025-07-30 06:23](https://github.com/koalaman/shellcheck/issues/2721#issuecomment-3135002639):

You may want to use `ulimit`, or `systemd-run` with MemoryMax property.


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
