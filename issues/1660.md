# [\#1660 Issue](https://github.com/koalaman/shellcheck/issues/1660) `closed`: Unexpected SC2034 warning

#### <img src="https://avatars.githubusercontent.com/u/1979931?v=4" width="50">[pcolmer](https://github.com/pcolmer) opened issue at [2019-08-02T07:17:11Z](https://github.com/koalaman/shellcheck/issues/1660):

#### For bugs
- Rule Id (if any, e.g. SC1000): SC2034
- My shellcheck version (`shellcheck --version` or "online"): 0.4.6 and online
- [x] The rule's wiki page does not already cover this (e.g. https://shellcheck.net/wiki/SC2086)
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit

#### Here's a snippet or screenshot that shows the problem:

```sh

#!/bin/bash
process_repos() {
    # If we already have a "merged_sources" directory, clean it out.
    if [ -d "/srv/source/merged_sources" ]; then
        rm -r /srv/source/merged_sources
    fi
    # Iterate through the manifest and process each repo in turn.
    while IFS=, read -r varname, giturl, dirpath
    do
        process_single_repo varname giturl dirpath
    done < /srv/source/manifest.csv
}

```

#### Here's what shellcheck currently says:

dirpath appears unused. Verify it or export it.

#### Here's what I wanted or expected to see:

Nothing. All three variables are assigned values from the read command and then passed as values to process_single_repo, so all three should be seen as being used.


#### <img src="https://avatars.githubusercontent.com/u/2199511?v=4" width="50">[Nightfirecat](https://github.com/Nightfirecat) commented at [2019-08-02T07:29:13Z](https://github.com/koalaman/shellcheck/issues/1660#issuecomment-517590352):

Aren't you just passing string literals to `process_single_repo`? That is, I think your code is the same as the following:

```sh
#!/bin/bash
process_repos() {
    # If we already have a "merged_sources" directory, clean it out.
    if [ -d "/srv/source/merged_sources" ]; then
        rm -r /srv/source/merged_sources
    fi
    # Iterate through the manifest and process each repo in turn.
    while IFS=, read -r varname, giturl, dirpath
    do
        process_single_repo 'varname' 'giturl' 'dirpath'
    done < /srv/source/manifest.csv
}
```

#### <img src="https://avatars.githubusercontent.com/u/1979931?v=4" width="50">[pcolmer](https://github.com/pcolmer) commented at [2019-08-02T07:32:09Z](https://github.com/koalaman/shellcheck/issues/1660#issuecomment-517591381):

Yeah, there are a number of mistakes in my code :).

It actually needed to read:

```
    while IFS=, read -r varname giturl dirpath
    do
        process_single_repo "$varname" "$giturl" "$dirpath"
    done < /srv/source/manifest.csv
```
and then shellcheck is happy, and so am I.

Sorry about the noise.
