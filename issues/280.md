# [\#280 Issue](https://github.com/koalaman/shellcheck/issues/280) `open`: SC2030, SC2031: Too eager & too widely scoped?

#### <img src="https://avatars.githubusercontent.com/u/166207?v=4" width="50">[mietek](https://github.com/mietek) opened issue at [2015-01-09T05:54:56Z](https://github.com/koalaman/shellcheck/issues/280):

This function appears to confuse Shellcheck:

``` bash
copy_dir_glob_into () {
    local src_dir src_glob dst_dir
    expect_args src_dir src_glob dst_dir -- "$@"
    shift 3

    expect_existing "${src_dir}" || return 1

    (
        cd "${src_dir}" || return 1

        local -a src_files_a
        IFS='' && src_files_a=( ${src_glob} )
        if [[ -z "${src_files_a[@]:+_}" ]]; then
            return 0
        fi

        local src_file
        for src_file in "${src_files_a[@]}"; do
            copy_dir_entry_into '.' "${src_file}" "${dst_dir}" "$@" || return 1
        done
    ) || return 1
}
```

The confusion spills over to uses of a `src_file` variable in completely unrelated functions defined later in the same file:

```
In src/tar.sh line 159:
        for src_file in "${src_files_a[@]}"; do
                ^-- SC2030: Modification of src_file is local (to subshell caused by (..) group).


In src/tar.sh line 227:
    expect_existing "${src_file}" || return 1
                         ^-- SC2031: src_file was modified in a subshell. That change might be lost.


In src/tar.sh line 230:
    name=$( basename "${src_file}" ) || return 1
                          ^-- SC2031: src_file was modified in a subshell. That change might be lost.


In src/tar.sh line 235:
    if ! bashnot_internal_tar_extract "${src_file}" "${dst_dir}" "$@" 2>"${stderr}"; then
                                           ^-- SC2031: src_file was modified in a subshell. That change might be lost.


In src/tar.sh line 255:
    expect_existing "${src_file}" || return 1
                         ^-- SC2031: src_file was modified in a subshell. That change might be lost.


In src/tar.sh line 259:
    extract_archive_into "${src_file}" "${dst_dir}" || return 1
                              ^-- SC2031: src_file was modified in a subshell. That change might be lost.
```

Removing the `local` declarations or moving them out of the subshell does not change anything.

#### <img src="https://avatars.githubusercontent.com/u/166207?v=4" width="50">[mietek](https://github.com/mietek) commented at [2015-01-09T06:16:33Z](https://github.com/koalaman/shellcheck/issues/280#issuecomment-69297777):

A rewritten variant of the same function still triggered the same warnings, until `src_file` was renamed to `glob_file` — a globally-unique identifier:

``` bash
expand_glob () {
    local dir glob
    expect_args dir glob -- "$@"

    expect_existing "${dir}" || return 1

    # TODO: Use $'\0' as delimiter.

    (
        local -a files_a
        cd "${dir}" &&
            IFS=$'\n' && files_a=( ${glob} ) &&
            echo "${files_a[*]}"
    ) || return 1
}

copy_dir_glob_into () {
    local src_dir src_glob dst_dir
    expect_args src_dir src_glob dst_dir -- "$@"
    shift 3

    expect_existing "${src_dir}" || return 1

    # TODO: Use read -rd $'\0'.

    local glob_file
    expand_glob "${src_dir}" "${src_glob}" "${dst_dir}" |
        while read -r glob_file; do
            copy_dir_entry_into "${src_dir}" "${glob_file}" "${dst_dir}" "$@" || return 1
        done || return 1
}
```


#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2015-03-08T07:08:49Z](https://github.com/koalaman/shellcheck/issues/280#issuecomment-77736551):

This is true and definitely a problem. ShellCheck currently has a very naive variable tracking algorithm that basically assumes that every branch of everything is executed in the order it appears.


#### <img src="https://avatars.githubusercontent.com/u/133706?v=4" width="50">[stdedos](https://github.com/stdedos) commented at [2020-03-11T12:55:52Z](https://github.com/koalaman/shellcheck/issues/280#issuecomment-597615732):

Also irrelevant to this check:

```sh
i_must_fail() {
    (set -Eeuo pipefail ; test "${foo}" = "bar" )
}
```

I didn't _modify_ anything here
