# [\#2609 Issue](https://github.com/koalaman/shellcheck/issues/2609) `closed`:  Run script through tr -d '\r' .

#### <img src="https://avatars.githubusercontent.com/u/71359260?v=4" width="50">[whoami-1337](https://github.com/whoami-1337) opened issue at [2022-10-19 20:41](https://github.com/koalaman/shellcheck/issues/2609):

#### For bugs
- Rule Id (if any, e.g. SC1000): SC1017, SC1017, SC1017, SC1017, SC1017, SC2213, SC2220, SC1017, SC1017, SC2214, SC1017, SC1017, SC1017, SC1017, SC1017, SC2221, SC1017, SC1017, SC2222, SC1017, SC1017, SC1017, SC1017, SC2193, SC1017, SC1017, SC1017, SC1017
- My shellcheck version (`shellcheck --version` or 'online'): online
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit
- [ ] It's not reproducible on shellcheck.net, but I think that's because it's an OS, configuration or encoding issue

#### For new checks and feature suggestions
- [x] shellcheck.net (i.e. the latest commit) currently gives no useful warnings about this
- [ ] I searched through https://github.com/koalaman/shellcheck/issues and didn't find anything related


#### Here's a snippet or screenshot that shows the problem:

```sh
#!/bin/bash
## Example: ShellCheck can detect some higher level semantic problems

while getopts "nf:" param
do
  case "$param" in
    f) file="$OPTARG" ;;
    v) set -x ;;
  esac
done

case "$file" in
  *.gz) gzip -d "$file" ;;
  *.zip) unzip "$file" ;;
  *.tar.gz) tar xzf "$file" ;;
  *) echo "Unknown filetype" ;;
esac

if [[ "$(uname)" == "Linux" ]]
then
  echo "Using Linux"
fi

```

#### Here's what shellcheck currently says:
```
Line 1	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 2	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 3	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 4	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 5	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 6	SC2213: getopts specified -n, but it's not handled by this 'case'.
Line 6	SC2220: Invalid flags are not handled. Add a *) case.
Line 6	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 7	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 8	SC2214: This case is not specified by getopts.
Line 8	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 9	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 10	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 11	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 12	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 13	SC2221: This pattern always overrides a later one on line 15.
Line 13	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 14	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 15	SC2222: This pattern never matches because of a previous pattern on line 13.
Line 15	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 16	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 17	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 18	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 19	SC2193: The arguments to this comparison can never be equal. Make sure your syntax is correct.
Line 19	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 20	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 21	SC1017: Literal carriage return. Run script through tr -d '\r' .
Line 22	SC1017: Literal carriage return. Run script through tr -d '\r' .
```
#### Here's what I wanted or expected to see:

TODO: Describe expected/desired output






-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
