# [\#1167 Issue](https://github.com/koalaman/shellcheck/issues/1167) `closed`: SC2181 suggests to check exit code directly when I check for several possible values

#### <img src="https://avatars.githubusercontent.com/u/464913?v=4" width="50">[AleXoundOS](https://github.com/AleXoundOS) opened issue at [2018-04-01T22:14:04Z](https://github.com/koalaman/shellcheck/issues/1167):

- Rule Id: SC2181
- My shellcheck version: 0.4.7
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit
- [ ] It's not reproducible on shellcheck.net, but I think that's because it's an OS, configuration or encoding issue

#### Here's a snippet that shows the problem:

```sh

#!/usr/bin/env bash

gzip --test "$1"
if [[ $? -eq 0 || $? -eq 2 ]]
then
    echo ok
else
    echo bad
fi
```

#### Here's what shellcheck currently says:
In shellcheck-issue.sh line 4:
if [[ $? -eq 0 || $? -eq 2 ]]
      ^-- SC2181: Check exit code directly with e.g. 'if mycmd;', not indirectly with $?.

#### Here's what I wanted or expected to see:
Nothing, because the successful operation is not the only exit code I'm testing against. gzip possible exit statuses:
```
Exit status is normally 0; if an error occurs, exit status is 1. If a warning occurs, exit status is 2.
```
I want to test for both normal and warning, i.e. any of 0 and 2. And the remaining exit status - 1 falls into the else conditional path.

Moreover, shellcheck is silent for
```bash
if $? || [[ $? -eq 2 ]]
```
that is completely wrong. Since testing the first expression `$?` actually gets executed and rewrites the gzip exit code value. So that the second `[[ $? -eq 2 ]]` never matches.

P.S.
Of course, I could test just for exit status 1 in my case, but there could be new exit statues beside 0, 1, 2 in general.

#### <img src="https://avatars.githubusercontent.com/u/2199511?v=4" width="50">[Nightfirecat](https://github.com/Nightfirecat) commented at [2018-04-01T23:38:16Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-377825440):

Why not `if gzip --test "$1" || [[ $? -eq 2 ]]` ?

#### <img src="https://avatars.githubusercontent.com/u/464913?v=4" width="50">[AleXoundOS](https://github.com/AleXoundOS) commented at [2018-04-01T23:43:12Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-377825703):

Ok, this works for checking for 2 different values. What about 3?

`if gzip --test "$1" || [[ $? -eq 2 ]] || [[ $? -eq 3 ]]` will not work as needed. This way we can test `$?` only once.

#### <img src="https://avatars.githubusercontent.com/u/2199511?v=4" width="50">[Nightfirecat](https://github.com/Nightfirecat) commented at [2018-04-02T01:57:28Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-377835989):

`if gzip --test "$1" || [[ $? -eq 2 || $? -eq 3 ]]`, like in your original snippet?

#### <img src="https://avatars.githubusercontent.com/u/464913?v=4" width="50">[AleXoundOS](https://github.com/AleXoundOS) commented at [2018-04-02T16:57:48Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-377977881):

@Nightfirecat, thank you, this works. I missed this variant somehow.

#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2018-04-04T18:12:07Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-378694466):

This should still be fixed though. 

#### <img src="https://avatars.githubusercontent.com/u/5596239?v=4" width="50">[dschrempf](https://github.com/dschrempf) commented at [2018-05-07T08:03:22Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-386990179):

May I add, that whenever a function checks for the last exit code, spellcheck complains about SC2181. E.g., I want to check if the last command succeeded and act accordingly in $PROMP_COMMAND, ike this

    function prompt_command() {
    if [ $? != 0 ]
    then
      ...
    fi
    
    PROMPT_COMMAND=prompt_command

I don't think SC2181 can be avoided then.

#### <img src="https://avatars.githubusercontent.com/u/3028797?v=4" width="50">[qneill](https://github.com/qneill) commented at [2018-06-28T16:52:10Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-401101713):

Also what about subshells with possibly multiple commands?
`( set -e; cmd1; cmd2; if [...]; then cmd3; else cmd4; fi; cmd5 ); [ $? -ne 0 ] && exit 1`

#### <img src="https://avatars.githubusercontent.com/u/406446?v=4" width="50">[dteirney](https://github.com/dteirney) commented at [2018-07-02T04:38:04Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-401670450):

I ran into this problem when checking the exit code in a bash EXIT trap function. Pretty sure there isn't any other way to do the check than using $? in that case.

```bash
finish() {
	# shellcheck disable=SC2181
	if [ $? -ne 0 ]; then
		echo "ERROR: failed to create or initialize the JSPM package."
	fi
}

# bash ERR traps with unofficial-bash-strict-mode are a complicated beast.
# Easier to trap EXIT and then check the exit code.
trap finish EXIT
```

#### <img src="https://avatars.githubusercontent.com/u/1239089?v=4" width="50">[stevecheckoway](https://github.com/stevecheckoway) commented at [2019-03-30T04:25:09Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-478205723):

@dschrempf  and @dteirney, the way I worked around this is to put
``` bash
local ret=$?
```
as the first line of the function and then test against `${ret}` instead. It's not ideal, but it works.

#### <img src="https://avatars.githubusercontent.com/u/28293373?v=4" width="50">[arendtio](https://github.com/arendtio) commented at [2019-04-06T23:41:07Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-480546131):

FWIW, I am not even sure SC2181 is a wise suggestion at all. I mean there is the hint towards `set -e` but if I am not mistaken, there is a difference in execution of a function depending on if it was started as a normal command or from within an condition/if clause.

I think when it is being started as a normal command it does inherit the `set -e` but when being run as part of an if clause it does not inherit the set `set -e`:

```
#!/bin/sh
    
fail() {                                                                                                                                                                                                                                                                      
    return 1
}   
       
mySub() {
    printf 'mySub: '
    printf 'Foo'
    fail
    printf 'Bar'
}   
    
(   
    set -e
    mySub
)
printf '\n'
 
(   
    set -e
    if mySub; then
        printf ''
    fi
)
printf '\n'
```
The output is as follows:
```
$ ./demo.sh 
mySub: Foo
mySub: FooBar
```

So yes, `$?` isn't easy to handle due to its variable nature, but suggesting one way is better than the other when they are not resulting in the same execution isn't any better. I mean, when you are using `set -e` you probably want it to work within your functions too.

Furthermore, I think it is a valid use-case to check the exact return code to know what has gone wrong instead of just having one `if` handling all error cases without knowing which one occurred.

#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2019-04-15T01:00:19Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-483075392):

@arendtio If anything, this suggestion seems *more* relevant when `set -e` is in effect, since it would make any sequence like `foo; if [ $? -eq 0 ]` pointless

You also only get this suggestion in comparisons against 0, so e.g. `foo; if [ $? -eq 42 ]` won't trigger it

#### <img src="https://avatars.githubusercontent.com/u/28293373?v=4" width="50">[arendtio](https://github.com/arendtio) commented at [2019-04-15T08:47:52Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-483163975):

@koalaman I think I wasn't precise enough about what the issue is: The problem I am having is that when `set -e` is active, the two alternatives `foo; if [ $? -eq 0 ]` and `if foo;` do not result in the same execution. So they are no alternatives, they are different programs.

Yet SC2181 suggests you can easily replace one with the other, but in reality, it depends on what `foo` does (and if it requires `set -e` to be active).

#### <img src="https://avatars.githubusercontent.com/u/28293373?v=4" width="50">[arendtio](https://github.com/arendtio) commented at [2019-04-15T09:47:26Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-483184456):

Maybe this `demo2.sh` illustrates the problem a bit better:

```sh
#!/bin/sh

process() {
        # $1 = Step name
        # $2 = log file

        # write the step to the log
        printf 'Step %s\n' "$1" >>"$2"

        #fail on step B
        if [ "$1" = "B" ]; then
                return 1
        fi
}

mySub() {
        # $1 = name of the test

        # initialize log file
        log="debug_log_$1.txt"
        printf '' >"$log"

        # do the work
        process A "$log"
        process B "$log"
        process C "$log"

        # relying on set -e to remove the debug log if no error occurred
        rm "$log"
}

printf 'Test One: '
(
        set -e
        mySub "One"
        if [ $? -eq 0 ]; then
                printf 'Finished without errors! Not possible... '
        fi
)
printf 'return code %s\n' "$?"

printf 'Test Two: '
(
        set -e
        if mySub "Two"; then
                printf 'Finished without errors! Not possible... '
        fi
)
printf 'return code %s\n' "$?"
```

Output:

```
$ ./demo2.sh 
Test One: return code 1
Test Two: Finished without errors! Not possible... return code 0
```

In addition, the first test leaves the `debug_log_One.txt` as it is supposed to.

#### <img src="https://avatars.githubusercontent.com/u/32216346?v=4" width="50">[Akianonymus](https://github.com/Akianonymus) commented at [2020-04-03T05:22:52Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-608236009):

how do i tackle this ??
```
OPTS=$(getopt -q --options $SHORTOPTS --longoptions $LONGOPTS --name "$PROGNAME" -- "$@")

if [ $? -ne 0 ]; then
short_help
exit 1
fi
eval set -- "$OPTS"
```



#### <img src="https://avatars.githubusercontent.com/u/1239089?v=4" width="50">[stevecheckoway](https://github.com/stevecheckoway) commented at [2020-04-03T06:09:19Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-608248857):

@Akianonymus, you can put the assignment in the `if`.
```sh
if ! OPTS=$(getopt ...); then
  short_help
  exit 1
fi
```

#### <img src="https://avatars.githubusercontent.com/u/32216346?v=4" width="50">[Akianonymus](https://github.com/Akianonymus) commented at [2020-04-03T06:14:44Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-608250560):

> @Akianonymus, you can put the assignment in the `if`.
> ```sh
> if ! OPTS=$(getopt ...); then
>   short_help
>   exit 1
> fi
> ```

I did try it, it outputs this

```
if​ ​!​ OPTS= --: command not found 
```

#### <img src="https://avatars.githubusercontent.com/u/1239089?v=4" width="50">[stevecheckoway](https://github.com/stevecheckoway) commented at [2020-04-03T14:38:20Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-608469881):

@Akianonymus, here's a complete example using GNU's getopt.

```sh
#!/bin/sh

shortopts=hv
longopts=help,verbose

help() {
  cat <<HELP_EOF
Usage: $0 [OPTIONS]

Options:
  -h, --help         show this help
  -v, --verbose      be verbose
HELP_EOF
}

if ! opts=$(getopt --options "${shortopts}" --longoptions "${longopts}" -- "$@"); then
  help >&2
  exit 1
fi

eval set -- "${opts}"

verbose=0
while [ $# -gt 0 ]; do
  opt=$1
  shift
  case ${opt} in
    -h|--help)
      help
      exit 0
      ;;
    -v|--verbose)
      verbose=$((verbose + 1))
      ;;
    --)
      break
      ;;
  esac
done
if [ "${verbose}" -gt 0 ]; then
  echo "Verbosity: ${verbose}"
fi
```

#### <img src="https://avatars.githubusercontent.com/u/32216346?v=4" width="50">[Akianonymus](https://github.com/Akianonymus) commented at [2020-04-03T15:06:18Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-608494535):

So, it worked, turns out i was missing '" on the variables, thanks for helping out.

#### <img src="https://avatars.githubusercontent.com/u/5604499?v=4" width="50">[ben-foxmoore](https://github.com/ben-foxmoore) commented at [2021-03-19T16:41:20Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-802964809):

Stumbled onto this problem as well. @arendtio's [post](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-483163975) explains the situation I'm in perfectly:

> when set -e is active, the two alternatives `foo; if [ $? -eq 0 ]` and `if foo;` do not result in the same execution. So they are no alternatives, they are different programs.

This is a quote from the POSIX standards:
> The -e setting shall be ignored when executing the compound list following the while, until, if, or elif reserved word

Unless shellcheck can determine if the `-e` is active, it seems wrong that SC2181 implies that the two options are exactly identical, as that is not always the case. If `-e` is active, the script will exit only if using the first of these two options.



#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2021-07-30T03:58:12Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-889609640):

This warning now only triggers on tests that only do one thing, like `[ $? -eq 0 ]`. It additionally tries to avoid warning on error checking functions that check `$?` as the first if statement or `&&/||` statement in a function. Thanks!

I looked into disabling the warning when `set -e` was in effect, but most of the corpus scripts containing `set -e` either used `set +e` first, or should have done so, so I left that behavior as-is.

#### <img src="https://avatars.githubusercontent.com/u/31679941?v=4" width="50">[linux40](https://github.com/linux40) commented at [2022-08-30T08:01:36Z](https://github.com/koalaman/shellcheck/issues/1167#issuecomment-1231301161):

What's your suggestion on `true; for i in $PidFiles; do [ $? -eq 0 ] && [ -f "$i" ]; done`, or any other `$?` in a loop?
