# [\#1673 Issue](https://github.com/koalaman/shellcheck/issues/1673) `open`: Shellcheck from snap does not work

#### <img src="https://avatars.githubusercontent.com/u/4455?v=4" width="50">[sobczyk](https://github.com/sobczyk) opened issue at [2019-08-13T09:48:02Z](https://github.com/koalaman/shellcheck/issues/1673):

shellcheck from snapstore does not work on ubuntu 16.04 (i did not test on newer releases)

```console
user@machine:~$ lsb_release -a
No LSB modules are available.
Distributor ID:	Ubuntu
Description:	Ubuntu 16.04.6 LTS
Release:	16.04
Codename:	xenial
```
#### Here's a snippet or screenshot that shows the problem:

```console
user@machine:~$ sudo snap install shellcheck
shellcheck v0.7.0 from Vidar (koalaman) installed
user@machine:~$ shellcheck
bash: /usr/bin/shellcheck: No such file or directory
```
I tried edge channel and devmode, and it has the same output

#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2019-08-16T18:16:02Z](https://github.com/koalaman/shellcheck/issues/1673#issuecomment-522103390):

Is it the same thing after a `hash -r`? What does `ls -l /usr/bin/shellcheck` say?

#### <img src="https://avatars.githubusercontent.com/u/10137?v=4" width="50">[ghost](https://github.com/ghost) commented at [2019-08-20T17:07:33Z](https://github.com/koalaman/shellcheck/issues/1673#issuecomment-523107293):

It works after `hash -r` on 16.04. 18.04 works fine without it.

#### <img src="https://avatars.githubusercontent.com/u/4455?v=4" width="50">[sobczyk](https://github.com/sobczyk) commented at [2019-08-21T07:18:43Z](https://github.com/koalaman/shellcheck/issues/1673#issuecomment-523330580):

hmm, it works now on 16.04 VMs I use, but it did not on the first try, maybe it's some caching problem?

#### <img src="https://avatars.githubusercontent.com/u/13607576?v=4" width="50">[KenSharp](https://github.com/KenSharp) commented at [2020-03-06T19:44:17Z](https://github.com/koalaman/shellcheck/issues/1673#issuecomment-595930355):

Could it be [this](https://askubuntu.com/questions/609828/usr-local-bin-apt-get-script-being-completely-ignored)?

#### <img src="https://avatars.githubusercontent.com/u/13408130?v=4" width="50">[brlin-tw](https://github.com/brlin-tw) commented at [2021-03-27T07:29:58Z](https://github.com/koalaman/shellcheck/issues/1673#issuecomment-808679400):

Bash caches command search using a hash table to speed up command lookup, if the cache doesn't hit, and the full search of command in PATH also failed it will present such message.

> If the name is neither a shell function nor a builtin, and contains no slashes, Bash searches each element of $PATH for a directory containing an executable file by that name. Bash uses a hash table to remember the full pathnames of executable files to avoid multiple PATH searches (see the description of hash in Bourne Shell Builtins). A full search of the directories in $PATH is performed only if the command is not found in the hash table. If the search is unsuccessful, the shell searches for a defined shell function named command_not_found_handle. If that function exists, it is invoked in a separate execution environment with the original command and the original command’s arguments as its arguments, and the function’s exit status becomes the exit status of that subshell. If that function is not defined, the shell prints an error message and returns an exit status of 127. 
> 
> -- Quoted from [Command Execution Environment (Bash Reference Manual)](https://www.gnu.org/software/bash/manual/html_node/Command-Execution-Environment.html)

It might be the user session hasn't had/snap/bin path in the PATH at the time running the command (probably just installed snapd without restarting the user session?), if the problem can't be reproduced after logout & re-login I'd say it is expected behavior.
