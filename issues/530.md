# [\#530 Issue](https://github.com/koalaman/shellcheck/issues/530) `closed`: unexpected '<' for the line: gpio -p mode $ModePin up </p><p># Main Program Loop

#### <img src="https://avatars.githubusercontent.com/u/15379855?v=4" width="50">[asyariamir](https://github.com/asyariamir) opened issue at [2015-10-29T12:46:45Z](https://github.com/koalaman/shellcheck/issues/530):

# !/bin/bash
# Detect motion on PiFace and play scary video.
# Define PiFace Pins

MotionPin=207 
TriggerPin=200 
VolUpPin=201 
VolDownPin=202 
ModePin=203

Volume=1500
Volstep=50 
CurrentState=0 
CurrentMode=0

DoFlip="" 
# If you need to flip the video upside down, uncomment the following line
# DoFlip="--orientation 180"
# Assign numbered videos to one of three video modes. Also define the order in which the videos appear

VidMode=0 # 0=Not Scary, 1=Scary, 2=Extra Long 
VidOrder[0]="14 30 46 62" 
VidOrder[1]="12 32 44 64 16 28 48 60" 
VidOrder[2]="6 22 38 54" 
PriorVid=$VidMode 
# Use piface internal pullup resistors on tactile button pins

gpio -p mode $TriggerPin up 
gpio -p mode $VolUpPin up 
gpio -p mode $VolDownPin up 
gpio -p mode $ModePin up </p><p># Main Program Loop 
clear
while : ; do 
  for i in ${VidOrder[$VidMode]}; do 
    if [ $VidMode -ne $PriorVid ] ; then echo -ne "Video Set $VidMode\r" ; PriorVid=$VidMode; break ; fi 
    while : ; do 
      PriorState=$CurrentState 
      PriorVolume=$Volume 
      PriorMode=$CurrentMode 
      let "CurrentState=(! `gpio -p read $TriggerPin`) | (`gpio -p read $MotionPin`)" 
      let "Volume += ( (! `gpio -p read $VolUpPin`) - (! `gpio -p read $VolDownPin`) )\* $Volstep" 
      let "CurrentMode=(! `gpio -p read $ModePin`)" 
      if [ $Volume -ne $PriorVolume ] ; then echo -ne "$Volume\r" ; fi 
      if [ $CurrentMode -gt $PriorMode ] ; then let "VidMode = ($VidMode + 1) % 3"; break ; fi 
      if [ $CurrentState -gt $PriorState ] ; then 
        omxplayer -b --vol $Volume $DoFlip ./Video/$i.mp4 >> /dev/null 
        clear 
        break 
      fi 
    done 
  done
done

#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2015-10-29T13:29:02Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-152178908):

@asyariamir maybe edit that text and wrap it in ``` to make a code block.


#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2015-10-29T13:33:27Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-152179874):

Btw. Not sure where you found the error, after adjusting it and pasting it into shellcheck.net I only see a warning about missing quotes at the omxplayer line.


#### <img src="https://avatars.githubusercontent.com/u/15379855?v=4" width="50">[asyariamir](https://github.com/asyariamir) commented at [2015-10-29T14:11:22Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-152192570):

@brother I'm sorry, I'm quite new to this and I'm using a ready made code. So, I am to wrap which text to make a code block?

Using PiFace

And the full error is as follows;

Unable to open SPI device: No such file or directory

Unable to open SPI device: No such file or directory

Unable to open SPI device: No such file or directory

./scareme.sh: line 32 : syntax error near unexpected token '<'

./scareme.sh: line 32: 'gpio -p mode $ModePin up </p><p> # Main Program Loop


#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2015-10-29T14:17:43Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-152194222):

This is how I inteded it.

``` bash
#!/bin/bash
# Detect motion on PiFace and play scary video.
# Define PiFace Pins

MotionPin=207
TriggerPin=200
VolUpPin=201
VolDownPin=202
ModePin=203

Volume=1500
Volstep=50
CurrentState=0
CurrentMode=0

DoFlip=""
# If you need to flip the video upside down, uncomment the following line
# DoFlip="--orientation 180"
# Assign numbered videos to one of three video modes. Also define the order in which the videos appear

VidMode=0 # 0=Not Scary, 1=Scary, 2=Extra Long
VidOrder[0]="14 30 46 62"
VidOrder[1]="12 32 44 64 16 28 48 60"
VidOrder[2]="6 22 38 54"
PriorVid=$VidMode
# Use piface internal pullup resistors on tactile button pins

gpio -p mode $TriggerPin up
gpio -p mode $VolUpPin up
gpio -p mode $VolDownPin up
gpio -p mode $ModePin up

# Main Program Loop
clear
while : ; do
    for i in ${VidOrder[$VidMode]}; do
        if [ $VidMode -ne $PriorVid ] ; then echo -ne "Video Set $VidMode\r" ; PriorVid=$VidMode; break ; fi
        while : ; do
            PriorState=$CurrentState
            PriorVolume=$Volume
            PriorMode=$CurrentMode
            let "CurrentState=(! gpio -p read $TriggerPin) | (gpio -p read $MotionPin)"
            let "Volume += ( (! gpio -p read $VolUpPin) - (! gpio -p read $VolDownPin) )* $Volstep"
            let "CurrentMode=(! gpio -p read $ModePin)"
            if [ $Volume -ne $PriorVolume ] ; then echo -ne "$Volume\r" ; fi
            if [ $CurrentMode -gt $PriorMode ] ; then let "VidMode = ($VidMode + 1) % 3"; break ; fi
            if [ $CurrentState -gt $PriorState ] ; then
                omxplayer -b --vol $Volume $DoFlip "./Video/$i.mp4" >> /dev/null
                clear
                break
            fi
        done
    done
done
```


#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2015-10-29T14:18:37Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-152194435):

What does you line 32 in ./scareme.sh contain? After I tried to understand things line 32 is empty =)


#### <img src="https://avatars.githubusercontent.com/u/15379855?v=4" width="50">[asyariamir](https://github.com/asyariamir) commented at [2015-10-29T14:44:27Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-152201866):

@brother In PiFace the line: gpio -p mode $ModePin up </p><p># Main Program Loop
is line 32 and not 31.

Anyway, thank you for your help but now the error lies in line 42-44
line 42: let: CurrentState=(!gpio -p read 0) | (gpio -p read 7): missing ')' (error token is "read 0) (gpio -p read 7)_)
line 43 : let: Volume += ((!gpio -p read 201) - (!gpio -p read 202))_ 50:missing ')' (error token is "read 201) - (! gpio -p read 202) )\* 50")
line 44 : let: CurrentMode= (! gpio -p read3): missing ')' (error token is "read 3)")


#### <img src="https://avatars.githubusercontent.com/u/15379855?v=4" width="50">[asyariamir](https://github.com/asyariamir) commented at [2015-10-29T14:47:52Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-152202837):

![20151029074002](https://cloud.githubusercontent.com/assets/15379855/10821775/12d19226-7e8f-11e5-892d-a5b0d3c348d2.jpg)


#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2015-10-29T15:41:57Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-152219704):

I do no think this is a shellcheck issue. 


#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2015-10-29T17:20:29Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-152255365):

For problems with shell scripts that are not related to shellcheck, try [stackoverflow](http://www.stackoverflow.com).


#### <img src="https://avatars.githubusercontent.com/u/15379855?v=4" width="50">[asyariamir](https://github.com/asyariamir) commented at [2015-10-30T01:48:48Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-152379312):

@brother is it? I see. But is it possible to have the code read as shown?


#### <img src="https://avatars.githubusercontent.com/u/197253?v=4" width="50">[kurahaupo](https://github.com/kurahaupo) commented at [2015-10-30T21:18:40Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-152653787):

This whole problem is because an ordinary newline has been replaced by HTML paragraph tags `</p><p>`, which @brother apparently can't see using his editor -- to him it just looks line an ordinary newline. Apparently he can't see them in the error message either.

@brother I suggest you use an editor which does _not_ interpret HTML tags, but actually shows them to you in raw form.


#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2015-11-03T10:26:05Z](https://github.com/koalaman/shellcheck/issues/530#issuecomment-153309169):

 @kurahaupo my emacs is working just fine. Are you confusing me with @asyariamir the original poster? If not your message is very unclear.
As I stated earlier, this is not a shellcheck bug and the code is fine, a missing quoting issue just.
