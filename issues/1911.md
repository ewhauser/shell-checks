# [\#1911 Issue](https://github.com/koalaman/shellcheck/issues/1911) `open`: SC2086 & SC2164 fighting?

#### <img src="https://avatars.githubusercontent.com/u/2242308?v=4" width="50">[thenoid](https://github.com/thenoid) opened issue at [2020-04-08 03:21](https://github.com/koalaman/shellcheck/issues/1911):

#### For bugs
- Rule Id (if any, e.g. SC1000): SC2086 & SC2164
- My shellcheck version (`shellcheck --version` or "online"): 
shellcheck --version
ShellCheck - shell script analysis tool
version: 0.7.1
license: GNU General Public License, version 3
website: https://www.shellcheck.net
- [X] The rule's wiki page does not already cover this (e.g. https://shellcheck.net/wiki/SC2086)
- [X] I tried on shellcheck.net and verified that this is still a problem on the latest commit - can't figure out how to load optional rules

#### For new checks and feature suggestions
- [X] shellcheck.net (i.e. the latest commit) currently gives no useful warnings about this
- [X] I searched through https://github.com/koalaman/shellcheck/issues and didn't find anything related
This might be a duplicate of https://github.com/koalaman/shellcheck/issues/1790 but that doesn't metion newlines so i could be wrong.


#### Here's a snippet or screenshot that shows the problem:

``` cat install.sh
#!/bin/bash
#
# Baseline developer configs
export MSGS=${MSGS:-()}

STARTDIR=$(pwd)
MYDIR=$(dirname $0)
cd $MYDIR
LOG="/tmp/install_log"
printf "\n\n\nStarting %s installer on " ${0} | tee -a ${LOG}
date | tee -a ${LOG}
for script in scripts.d/*.sh; do
    echo ${script} | tee -a ${LOG}
    . $script 2>&1 | tee -a ${LOG}
    cd ${MYDIR}
done
cd ${STARTDIR}
rm -fv /tmp/aptupdate| tee -a ${LOG}
printf "\n\n\nSpawning new shell\n\n\n"
exec ${SHELL}
```

#### Here's what shellcheck currently says:

```
shellcheck -o all install.sh -f diff
--- a/install.sh
+++ b/install.sh
@@ -4,17 +4,18 @@
 export MSGS=${MSGS:-()}

 STARTDIR=$(pwd)
-MYDIR=$(dirname $0)
-cd $MYDIR
-LOG="/tmp/install_log"
-printf "\n\n\nStarting %s installer on " ${0} | tee -a ${LOG}
-date | tee -a ${LOG}
+MYDIR=$(dirname "$0")
+cd ${MYDIR"}
+ || exitLOG="/tmp/install_log"
+printf "\n\n\nStarting %s installer on " $"{0} "| tee -a $"{LOG}
+"date | tee -a $"{LOG} "
 for script in scripts.d/*.sh; do
-    echo ${script} | tee -a ${LOG}
-    . $script 2>&1 | tee -a ${LOG}
-    cd ${MYDIR}
-done
-cd ${STARTDIR}
-rm -fv /tmp/aptupdate| tee -a ${LOG}
-printf "\n\n\nSpawning new shell\n\n\n"
-exec ${SHELL}
+    echo "${script}" | tee -a "${LOG}"
+    . ${script"} 2>&1 | tee -a $"{LOG}
+"    cd $"{MYDIR}
+" || exitdone
+cd ${"STARTDIR}
+r" || exitm -fv /tmp/aptupdate| tee -a ${"LOG}
+p"rintf "\n\n\nSpawning new shell\n\n\n"
+exec ${"SHELL}
+"
```

#### Here's what I wanted or expected to see:
```
shellcheck -o all install.sh -f diff
--- a/install.sh
+++ b/install.sh
@@ -4,17 +4,18 @@
 export MSGS=${MSGS:-()}

 STARTDIR=$(pwd)
-MYDIR=$(dirname $0)
-cd $MYDIR
-LOG="/tmp/install_log"
-printf "\n\n\nStarting %s installer on " ${0} | tee -a ${LOG}
-date | tee -a ${LOG}
+MYDIR=$(dirname "$0")
+cd "${MYDIR}" || exitLOG="/tmp/install_log"
+printf "\n\n\nStarting %s installer on " $"{0} "| tee -a $"{LOG}"
+date | tee -a $"{LOG} "
 for script in scripts.d/*.sh; do
-    echo ${script} | tee -a ${LOG}
-    . $script 2>&1 | tee -a ${LOG}
-    cd ${MYDIR}
-done
-cd ${STARTDIR}
-rm -fv /tmp/aptupdate| tee -a ${LOG}
-printf "\n\n\nSpawning new shell\n\n\n"
-exec ${SHELL}
+    echo "${script}" | tee -a "${LOG}"
+    . ${script"} 2>&1 | tee -a $"{LOG}"
+    cd $"{MYDIR}" || exitdone
+ cd "${STARTDIR}" || exit
+ rm -fv /tmp/aptupdate| tee -a "${LOG}"
+ printf "\n\n\nSpawning new shell\n\n\n"
+ exec "${SHELL}"
```







-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
