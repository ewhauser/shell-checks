# [\#1022 Issue](https://github.com/koalaman/shellcheck/issues/1022) `open`: Feature: warn about possibly failing pipelines when set -e is set without -o pipefail

#### <img src="https://avatars.githubusercontent.com/u/379509?v=4" width="50">[alexjurkiewicz](https://github.com/alexjurkiewicz) opened issue at [2017-10-06T02:56:53Z](https://github.com/koalaman/shellcheck/issues/1022):

I think this is a pretty common gotcha

#### Here's a snippet or screenshot that shows the problem:

```sh

#!/bin/bash -e
myvar=$(false | grep foo)
export myvar
```

#### Here's what shellcheck currently says:

#### Here's what I wanted or expected to see:
```
myvar=$(false | grep foo)
        ^-- SCXXXX: Warning: You have enabled `set -e` but this command can silently fail.
            Split the command or use `set -o pipefail`
```
