# [\#610 Issue](https://github.com/koalaman/shellcheck/issues/610) `open`: Detect semantic errors

#### <img src="https://avatars.githubusercontent.com/u/2039950?v=4" width="50">[VanNostrand](https://github.com/VanNostrand) opened issue at [2016-02-19T22:15:13Z](https://github.com/koalaman/shellcheck/issues/610):

... like static analysis tools for other languages do: http://www.viva64.com/en/b/0377/
I guess that is harder in shell than in C due to exported variables and such but cases where expressions are always true/false or where identical tests  are repeated could be found nonetheless:

```
#!/bin/sh
a=0
if [ $a -eq 1 ]
then
    echo never
fi
```

```
#!/bin/sh
a=0
if [ $a -gt $a ]
then
    echo never
fi
```

```
#!/bin/sh
a=4
b=3
if [ $a -gt $b ]
then
    if [ $b -lt $a ]
    then
        echo useless
    fi
fi
```

#### <img src="https://avatars.githubusercontent.com/u/197253?v=4" width="50">[kurahaupo](https://github.com/kurahaupo) commented at [2016-02-20T09:53:24Z](https://github.com/koalaman/shellcheck/issues/610#issuecomment-186560480):

"Harder" is rather an understatement. Once you hit the first "eval" or "."
("source") statement, anything goes.

After that, any command might have been redefined to do something completely
different. (Maybe "echo" becomes a shell function that changes the value of
a...)

On Fri, 19 Feb 2016, VanNostrand wrote:

> ... like static analysis tools for other languages do: http://www.viva64.com/en/b/0377/
> I guess that is harder in shell than in C due to exported variables and such but cases where expressions are always true/false or where identical tests  are repeated could be found nonetheless:
