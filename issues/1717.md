# [\#1717 Issue](https://github.com/koalaman/shellcheck/issues/1717) `open`: SC2094 triggers on appends

#### <img src="https://avatars.githubusercontent.com/u/10564044?v=4" width="50">[ratorx](https://github.com/ratorx) opened issue at [2019-10-07T11:08:33Z](https://github.com/koalaman/shellcheck/issues/1717):

#### For bugs
- Rule Id (if any, e.g. SC1000): SC2094
- My shellcheck version (`shellcheck --version` or "online"): online
- [x] The rule's wiki page does not already cover this (e.g. https://shellcheck.net/wiki/SC2086)
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit


#### Here's a snippet or screenshot that shows the problem:

```sh

#!/bin/bash
fd -H --maxdepth 1 --ignore-file example.txt >> example.txt

```

#### Here's what shellcheck currently says:
Make sure to not read and write in the same pipeline


#### Here's what I wanted or expected to see:
Nothing

Is this fine? Since it's an append, it doesn't have the truncate race condition?

I've been using the above command regularly without issues for a few months now.

A simpler example would be `cat file >> file`, which causes an error in `cat`. I tried bypassing the error `cat file | cat >> file` and that did what it was expected (duplicated the content). I think the `cat` error is possibly because it doesn't distinguish between `>` and `>>`?

If appends in the same pipeline are fine, then please mention it on the wiki (or add an exception in the check). Thanks!

