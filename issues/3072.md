# [\#3072 Issue](https://github.com/koalaman/shellcheck/issues/3072) `closed`: Run set -x in a subshell

#### <img src="https://avatars.githubusercontent.com/u/2305330?u=6438cc4c1949817b195c39b1b04b9de7fc7fb477&v=4" width="50">[alex-harvey-z3q](https://github.com/alex-harvey-z3q) opened issue at [2024-10-25 23:58](https://github.com/koalaman/shellcheck/issues/3072):

I apologise in advance if this is the wrong forum because I could not easily find a better way to start a discussion with the community.

This is a request for a new check and depends on whether the Bash programming community agrees with the style to enforce.

## Running cd in a subshell

The Bash community and Shell have long agreed that `cd` should run in a subshell if the intent is to later return to current dir. Thus this:

```bash
(cd my_dir || exit "$?"
 do_something)
```

Is considered preferable to 

```bash
cd my_dir || exit "$?"
do_something
cd -
```

## Running set -x in a subshell

For the same reason I always turn set -x on in a subshell, thus instead of:

```bash
set -x
do_something
set +x
```

It is preferrable to do:

```bash
(set -x
 do_something)
```

## Further discussion

Many programmers may not spend time worrying about how they do debugging, however use of `set -x` has production application as a simple and neat way to generate logging.

Consider for example this snippet from a tool I have written:

```bash
# Create the change set to import resources.
#
# Input Globals:
#   initial_template: The initial CloudFormation template used during import.
#   template_bucket_name: The template bucket.
#   stack_name: The stack name.
#
_create_change_set() {
  local resources_to_import base_name template_url

  resources_to_import="$(resources_to_import | jq -c .)"
  base_name="$(basename "$initial_template")"
  template_url="https://$template_bucket_name.s3.ap-southeast-2.amazonaws.com/$base_name"

  (set -x ; aws cloudformation create-change-set --stack-name "$stack_name" \
     --change-set-name "ImportChangeSet" --change-set-type "IMPORT" \
     --resources-to-import "$resources_to_import" --template-url "$template_url" \
     --capabilities "CAPABILITY_NAMED_IAM"

   aws cloudformation wait change-set-create-complete --stack-name "$stack_name" \
     --change-set-name "ImportChangeSet")

  # shellcheck disable=SC2181
  [[ "$?" -ne 0 ]] && _change_set_failure
}

# Execute the change set to complete importing the resources.
#
# Input Globals:
#   stack_name: The stack name.
#
_execute_change_set() {
  (set -x
   aws cloudformation execute-change-set --change-set-name "ImportChangeSet" --stack-name "$stack_name"
   aws cloudformation wait stack-import-complete --stack-name "$stack_name")
}
```

This pattern results in these benefits:

1. Clean, readable log output, where the caller can see both the outputs for commands in real time, along with the commands generating that output indicated clearly with a leading `+` in a way that will always make sense.
2. More concise source code without noisy calls to `set +x`
3. Use of a subshell here is (as far as I know) the only way to avoid the very ugly appearance of `+ set +x` in your logs, which often will not make sense to the reader. Noting that the initial `set -x` already does not appear in the log output.

I am not aware of any negatives to this approach or any scenario where it is preferrable not to use the subshell.

Therefore, I am proposing to enforce this in the linter, and welcome further discussion!

#### <img src="https://avatars.githubusercontent.com/u/2305330?u=6438cc4c1949817b195c39b1b04b9de7fc7fb477&v=4" width="50">[alex-harvey-z3q](https://github.com/alex-harvey-z3q) commented at [2024-10-26 00:04](https://github.com/koalaman/shellcheck/issues/3072#issuecomment-2439061922):

Please note I would be happy to implement this myself if there is agreement that it is a good feature.

#### <img src="https://avatars.githubusercontent.com/u/84648683?v=4" width="50">[wileyhy](https://github.com/wileyhy) commented at [2024-10-26 03:00](https://github.com/koalaman/shellcheck/issues/3072#issuecomment-2439199690):

Speaking for myself, I probably wouldn't use such a ShellCheck test.

My use case of xtrace is for debugging. Whenever there's some section of a
script that I'm working on, I'll enable xtrace a few lines prior and insert
an `exit "${LINENO}"` or a `: "{Halt:?}"` a few lines after. Then, when the
debugging is completed I'll remove or comment any debugging code. A longer
script (>1000 lines) would have numerous debugging blocks between sections.
Inserting additional subshell code would mean more code to keep track of,
when parentheses could be separated by 100's of lines. It seems like that
could get a little confusing.

In recent versions of Bash, xtrace, or any other feature of `set`, can also
be  temporarily enabled within the confines of any given function by using
`local -`.

foo(){
  local -;
  set -euxo pipefail;
  echo something;
}

Functions can also be defined using forced subshells:

bar() (
  set -x
  echo quux
)

A separate use case is when I'll occasionally use Thompson-style comments
(`: $'foo:\t' "${foo}"`), which are visible in xtrace, in place of the
ubiquitous Bourne-style (#). I find that helps if any xtrace output is
especially opaque. Also, with Thompson comments, debugging code at times
can remain in the script.

Regarding `cd`, I prefer to avoid it, actually. The idea being that the
filesystem is beyond my control and can change at any time. TOCTOU's are
the main concern. I suppose I've just gotten used to it; but then I do use
per-execution private temporary directories pretty often.

Cheers,

Wiley

On Fri, Oct 25, 2024, 5:05 PM Alex Harvey ***@***.***> wrote:

> Please note I would be happy to implement this myself if there is
> agreement that it is a good feature.
>
> —
> Reply to this email directly, view it on GitHub
> <https://github.com/koalaman/shellcheck/issues/3072#issuecomment-2439061922>,
> or unsubscribe
> <https://github.com/notifications/unsubscribe-auth/AUF2F2Z6A2PW6EMGZHGN5NLZ5LMDPAVCNFSM6AAAAABQUGX7Q2VHI2DSMVQWIX3LMV43OSLTON2WKQ3PNVWWK3TUHMZDIMZZGA3DCOJSGI>
> .
> You are receiving this because you are subscribed to this thread.Message
> ID: ***@***.***>
>

#### <img src="https://avatars.githubusercontent.com/u/84648683?v=4" width="50">[wileyhy](https://github.com/wileyhy) commented at [2024-10-26 03:12](https://github.com/koalaman/shellcheck/issues/3072#issuecomment-2439209869):

An additional side to the subshell question is that any alterations of
parameters which occur within subshells can be invisible to the "parent"
process.

~ $ xx=aa

~ $ fn()( set -x; xx=bb )

~ $ fn

+ xx=bb

~ $ declare -p xx

declare -- xx="aa"

If an xtrace subshell was removed after debugging, that could alter the
state of parameter values within the final script.
Wiley

On Fri, Oct 25, 2024, 7:59 PM Wiley Young ***@***.***> wrote:

>
> Speaking for myself, I probably wouldn't use such a ShellCheck test.
>
> My use case of xtrace is for debugging. Whenever there's some section of a
> script that I'm working on, I'll enable xtrace a few lines prior and insert
> an `exit "${LINENO}"` or a `: "{Halt:?}"` a few lines after. Then, when the
> debugging is completed I'll remove or comment any debugging code. A longer
> script (>1000 lines) would have numerous debugging blocks between sections.
> Inserting additional subshell code would mean more code to keep track of,
> when parentheses could be separated by 100's of lines. It seems like that
> could get a little confusing.
>
> In recent versions of Bash, xtrace, or any other feature of `set`, can
> also be  temporarily enabled within the confines of any given function by
> using `local -`.
>
> foo(){
>   local -;
>   set -euxo pipefail;
>   echo something;
> }
>
> Functions can also be defined using forced subshells:
>
> bar() (
>   set -x
>   echo quux
> )
>
> A separate use case is when I'll occasionally use Thompson-style comments
> (`: $'foo:\t' "${foo}"`), which are visible in xtrace, in place of the
> ubiquitous Bourne-style (#). I find that helps if any xtrace output is
> especially opaque. Also, with Thompson comments, debugging code at times
> can remain in the script.
>
> Regarding `cd`, I prefer to avoid it, actually. The idea being that the
> filesystem is beyond my control and can change at any time. TOCTOU's are
> the main concern. I suppose I've just gotten used to it; but then I do use
> per-execution private temporary directories pretty often.
>
> Cheers,
>
> Wiley
>
> On Fri, Oct 25, 2024, 5:05 PM Alex Harvey ***@***.***>
> wrote:
>
>> Please note I would be happy to implement this myself if there is
>> agreement that it is a good feature.
>>
>> —
>> Reply to this email directly, view it on GitHub
>> <https://github.com/koalaman/shellcheck/issues/3072#issuecomment-2439061922>,
>> or unsubscribe
>> <https://github.com/notifications/unsubscribe-auth/AUF2F2Z6A2PW6EMGZHGN5NLZ5LMDPAVCNFSM6AAAAABQUGX7Q2VHI2DSMVQWIX3LMV43OSLTON2WKQ3PNVWWK3TUHMZDIMZZGA3DCOJSGI>
>> .
>> You are receiving this because you are subscribed to this thread.Message
>> ID: ***@***.***>
>>
>

#### <img src="https://avatars.githubusercontent.com/u/2305330?u=6438cc4c1949817b195c39b1b04b9de7fc7fb477&v=4" width="50">[alex-harvey-z3q](https://github.com/alex-harvey-z3q) commented at [2024-10-26 05:59](https://github.com/koalaman/shellcheck/issues/3072#issuecomment-2439373823):

@wileyhy  Thank you! I did not know about forced subshells for functions and I agree that those would remove a need for this use case.

#### <img src="https://avatars.githubusercontent.com/u/2305330?u=6438cc4c1949817b195c39b1b04b9de7fc7fb477&v=4" width="50">[alex-harvey-z3q](https://github.com/alex-harvey-z3q) commented at [2024-10-26 12:21](https://github.com/koalaman/shellcheck/issues/3072#issuecomment-2439557845):

@wileyhy I closed the issue, but curious, do you agree that

```
set -x
do_something
set +x
```

Should be regarded as an anti-pattern (in production code, anyway)? I ask, because ChatGPT was rather stubbornly suggesting that pattern, suggesting that it must be in widespread use in OpenAI's training data! This being the case, I am still thinking perhaps Shellcheck should recommend against that pattern.

I think your response shows a lot of interesting ideas about how to debug Bash code, it doesn't really provide an argument against that the pattern I showed should be instead regarded an anti-pattern.

#### <img src="https://avatars.githubusercontent.com/u/84648683?v=4" width="50">[wileyhy](https://github.com/wileyhy) commented at [2024-10-27 06:23](https://github.com/koalaman/shellcheck/issues/3072#issuecomment-2439872613):

Hi Alex,

  Well, I haven't read "Design Patterns," so I suppose it's possible, but I
would lean towards 'probably not.'

  Wikipedia at the present moment has anti-pattern as, "a common response
to a recurring problem that is usually ineffective and risks being highly
counterproductive."

  As shell functionality, `set` is specified currently by POSIX-2024 as a
"special builtin," and `set -x` is defined as, "The shell shall write to
standard error a trace for each command after it expands the command and
before it executes it." This definition is also present in the POSIX-2008
standard.

  The "recurring problem" that I understand xtrace to be of assistance in
solving is, "how does this implementation of `sh` expand XYZ input?"
Expansions are a major component of how Bourne derivative shells process
any input.


~ $ man --nh bash 2>&1 \

  | grep -ie "expansion*" -e "expand*" \

  | wc -l

359


  TTBOMK, xtrace is the most efficient method available for ascertaining
that information.


~ $ AA=for

~ $ awk_prg=( -v xx=${AA} '$1 == xx { print $1, $2 }' )

~ $ set -x; help | awk "${awk_prg[@]}"; set -

+ awk -v xx=for '$1 == xx { print $1, $2 }'

+ help

for NAME

for ((

+ set -


The #bash channel at libera.chat is where the developers of bash often
answer these kinds of questions. Their opinions would be much more reliable
than my own, but I appreciate your curiosity.

Cheers,

Wiley

On Sat, Oct 26, 2024, 05:22 Alex Harvey ***@***.***> wrote:

> @wileyhy <https://github.com/wileyhy> I closed the issue, but curious, do
> you agree that
>
> set -x
> do_something
> set +x
>
> Should be regarded as an anti-pattern? I ask because ChatGPT was rather
> stubbornly suggesting that pattern, suggesting that it must be in
> widespread use in OpenAI's training data! This being the case, I am still
> thinking perhaps Shellcheck should recommend against that pattern.
>
> I think your response shows a lot of interesting ideas about how to debug
> Bash code, it doesn't really provide an argument against that the pattern I
> showed should be instead regarded an anti-pattern.
>
> —
> Reply to this email directly, view it on GitHub
> <https://github.com/koalaman/shellcheck/issues/3072#issuecomment-2439557845>,
> or unsubscribe
> <https://github.com/notifications/unsubscribe-auth/AUF2F26IA4CLYILCMTAMVULZ5OCPTAVCNFSM6AAAAABQUGX7Q2VHI2DSMVQWIX3LMV43OSLTON2WKQ3PNVWWK3TUHMZDIMZZGU2TOOBUGU>
> .
> You are receiving this because you were mentioned.Message ID:
> ***@***.***>
>


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
