# [\#858 Issue](https://github.com/koalaman/shellcheck/issues/858) `open`: Don’t demand quoting coproc FD numbers

#### <img src="https://avatars.githubusercontent.com/u/2346599?v=4" width="50">[lucaswerkmeister](https://github.com/lucaswerkmeister) opened issue at [2017-03-27T12:27:32Z](https://github.com/koalaman/shellcheck/issues/858):

#### For bugs
- Rule Id (if any, e.g. SC1000): **SC2086**
- My shellcheck version (`shellcheck --version` or "online"): **online**
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit


#### Here's a snippet or screenshot that shows the problem:

```bash

#!/bin/bash
coproc cat
printf 'hello\n' >&${COPROC[1]}
read -r -u ${COPROC[0]} greeting
printf '%s\n' "$greeting"
kill %1

```

#### Here's what shellcheck currently says:

> ^-- SC2086: Double quote to prevent globbing and word splitting.

#### Here's what I wanted or expected to see:

Coproc arrays only contain file descriptor numbers, so quoting them is unnecessary. Since shellcheck already tracks coproc variables afaik, the quoting warning should be disabled for those variables.
