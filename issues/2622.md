# [\#2622 Issue](https://github.com/koalaman/shellcheck/issues/2622) `open`: Question about `"${CMD[@]}` vs `"${CMD[*]}"`

#### <img src="https://avatars.githubusercontent.com/u/133706?u=c19422ae9646a1a58b9c62a57a2143e62c5f8001&v=4" width="50">[stdedos](https://github.com/stdedos) opened issue at [2022-11-07 13:20](https://github.com/koalaman/shellcheck/issues/2622):

#### For bugs
- Rule Id (if any, e.g. SC1000): 
- My shellcheck version (`shellcheck --version` or "online"): `v0.8.0` / `v0.8.0-12-g88cdb4e`
- [ ] The rule's wiki page does not already cover this (e.g. https://shellcheck.net/wiki/SC2086)
- [ ] I tried on https://www.shellcheck.net/ and verified that this is still a problem on the latest commit

#### For new checks and feature suggestions
- [x] https://www.shellcheck.net/ (i.e. the latest commit) currently gives no useful warnings about this
- [ ] I searched through https://github.com/koalaman/shellcheck/issues and didn't find anything related


#### Here's a snippet or screenshot that shows the problem:

```sh
#!/bin/bash

CMD=(shellcheck)
if ! command -V shellcheck > /dev/null 2>&1; then
    # shellcheck disable=SC2016 # intentional
    >&2 echo 'shellcheck command not found (must be on $PATH), trying docker ...'

    if command -V docker > /dev/null 2>&1; then
        CMD=(docker run --rm -v "$PWD:/mnt:rw,Z" --workdir /mnt koalaman/shellcheck-alpine:stable shellcheck)
    else
        # shellcheck disable=SC2016 # intentional
        >&2 echo '... neither `docker` command was not found!'
        exit 1
    fi
fi

SHELLCHECK_FIXES_PATCH="$(mktemp "/tmp/${MY_NAME}_$(date +"%s")_XXXXXX.patch")"
trap 'rm -f "${SHELLCHECK_FIXES_PATCH}"' EXIT HUP INT QUIT TERM PWR

PRE_COMMIT_SHELLCHECK_RC=0
"${CMD[*]}" "$@" || PRE_COMMIT_SHELLCHECK_RC=$?

echo "\$?:${PRE_COMMIT_SHELLCHECK_RC}"
```

#### Here's what shellcheck currently says:

_Nothing_

#### Here's what I wanted or expected to see:

Shouldn't `shellcheck` complain that `"${CMD[*]}"` should've been expanded as `"${CMD[@]}"` instead? :confused: 

#### <img src="https://avatars.githubusercontent.com/u/2818758?u=40d36e49d938fda80abd46d6347b9bace1f73dd4&v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2022-11-13 19:24](https://github.com/koalaman/shellcheck/issues/2622#issuecomment-1312802469):

It's hard for ShellCheck to always warn about `"${cmd[*]}"` since that would flag the few but distinctly valid use cases as well with no useful workaround. Is there any way to determine that this particular usage should be flagged, while not warning about other valid uses?

Unquoted `${cmd[*]}` is essentially always wrong, so that's why you get a warning in that case.

#### <img src="https://avatars.githubusercontent.com/u/133706?u=c19422ae9646a1a58b9c62a57a2143e62c5f8001&v=4" width="50">[stdedos](https://github.com/stdedos) commented at [2022-11-13 19:34](https://github.com/koalaman/shellcheck/issues/2622#issuecomment-1312804287):

> Is there any way to determine that this particular usage should be flagged, while not warning about other valid uses?

"Since you are parsing bash" (I assume 🚩) so ... "`${cmd[*]}"` as a first argument is **always** wrong? (and/or **both** `${cmd[*]}` and `"${cmd[*]}"`). I cannot come up with any counter-arguments to my proposal.

Idk if someone is interested to ... idk; build complex commands (`'-'.join(["shellcheck", "0.7.0"])`?) or paths (`/'.join(["this", "path", "command"])` !??!) somehow, that then **must** be expanded as a single argument and not multiple.
_PS: Python (pseudo)code_

#### <img src="https://avatars.githubusercontent.com/u/2818758?u=40d36e49d938fda80abd46d6347b9bace1f73dd4&v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2022-11-13 23:06](https://github.com/koalaman/shellcheck/issues/2622#issuecomment-1312847324):

`${cmd[*]}` as the first argument is already flagged. Flagging `"${cmd[*]}"` could be reasonable but I wasn't able to find any other instances of this happening in 40k scripts, so having code to identify it might be overfitting. The mistake normally wouldn't survive a test run, but your case is particularly treacherous because it happens to work until docker gets involved.

#### <img src="https://avatars.githubusercontent.com/u/133706?u=c19422ae9646a1a58b9c62a57a2143e62c5f8001&v=4" width="50">[stdedos](https://github.com/stdedos) commented at [2022-11-14 11:53](https://github.com/koalaman/shellcheck/issues/2622#issuecomment-1313562793):

> The mistake normally wouldn't survive a test run

It's my favorite kind of errors :heart_eyes: (e.g. https://github.com/koalaman/shellcheck/issues/1994)

IANAL (... or rather, the amazing creator of `shellcheck` that Xs of people depend on), and I would hope there's a way to introduce and test new "maybe-ambiguous/over-fitting" warnings, "maybe" without only putting them on the "what I feel could be the side-tracked `--enable=check1,check2..` list".

> but your case is particularly treacherous because it happens to work until docker gets involved.

Haven't thought of that :star_struck:, but "I get the logic" of how would it work (`== break`).

Of course, up to you :smile: Whatever you think is best for the `shellcheck` community, **and** you.


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
