# [\#2118 Issue](https://github.com/koalaman/shellcheck/issues/2118) `open`: SC2148: add support for bash completions (magic ex comment)

#### <img src="https://avatars.githubusercontent.com/u/20344300?u=69df0dc582d8beda63a4fca3868e3d928ed34d9b&v=4" width="50">[ChillerDragon](https://github.com/ChillerDragon) opened issue at [2020-12-27 13:17](https://github.com/koalaman/shellcheck/issues/2118):

#### For bugs
- SC2148
- My shellcheck version: 0.7.1
- [x] The rule's wiki page does not already cover this (e.g. https://shellcheck.net/wiki/SC2148)
- [x] I tried on https://www.shellcheck.net/ and verified that this is still a problem on the latest commit

#### For new checks and feature suggestions
- [x] https://www.shellcheck.net/ (i.e. the latest commit) currently gives no useful warnings about this
- [x] I searched through https://github.com/koalaman/shellcheck/issues and didn't find anything related


#### Here's a snippet or screenshot that shows the problem:

```sh
# POSIX sh(1) completion                                   -*- shell-script -*-

_sh()
{
    local cur prev words cword
    _init_completion || return

    case $prev in
        -c)
            return
            ;;
        -o | +o)
            COMPREPLY=($(compgen -W 'allexport errexit ignoreeof monitor
                noclobber noglob noexec nolog notify nounset verbose vi
                xtrace' -- "$cur"))
            return
            ;;
    esac

    local opts="-a -b -C -e -f -h -i -m -n -o -u -v -x"
    if [[ $cur == -* ]]; then
        COMPREPLY=($(compgen -W "$opts -c -s" -- "$cur"))
        return
    elif [[ $cur == +* ]]; then
        COMPREPLY=($(compgen -W "${opts//-/+}" -- "$cur"))
        return
    fi

    local args ext=
    _count_args "" "@(-c|[-+]o)"
    ((args == 1)) && ext="sh"
    _filedir $ext
} &&
    complete -F _sh sh

# ex: filetype=sh
```

#### Here's what shellcheck currently says:

```
In sh line 1:
# POSIX sh(1) completion                                   -*- shell-script -*-
^-- SC2148: Tips depend on target shell and yours is unknown. Add a shebang or a 'shell' directive.
```


#### Here's what I wanted or expected to see:

Parse either the ``-*- shell-script -*-`` in the first line or the ``# ex: filetype=sh`` in the last line.

#### Some of my thoughts

I know ``-*- shell-script -*-`` is not telling which shell it is maybe checking if the path to the file contains ``/bash-completion/`` works. Or if the first line also contains the string ``completion`` since I think these completions are always bash. It is weird that the ``# ex`` comment says ``filetype=sh`` and then uses bash features but it is so widely used already so yeah.

Here some stats about my /usr/share/bash-completion/completions directory (arch linux):

```
[chiller@arch /usr/share/bash-completion/completions]$ ls | wc -l
947
[chiller@arch /usr/share/bash-completion/completions]$ grep -r "#\!/bin"
bwrap:#!/bin/bash
pkgfile:#!/bin/bash
ipset:#!/bin/bash
runc:#!/bin/bash
pulseaudio:#!/bin/bash
mkinitcpio:#!/bin/bash
[chiller@arch /usr/share/bash-completion/completions]$ grep -r "# ex: " | wc -l
480
[chiller@arch /usr/share/bash-completion/completions]$ grep -rh "# ex:" | sort | uniq
# ex:et ts=2 sw=2 ft=sh
# ex: filetype=sh
# ex: ts=4 sw=4 et filetype=sh
[chiller@arch /usr/share/bash-completion/completions]$ grep -r "shell-script" | wc -l
511
```

I am not sure how this ``ex:`` standard is called and where it is documented but it seems to be wide spread.

I also saw the extension .bash.completion at [pass-otp](https://github.com/tadfisher/pass-otp/blob/develop/pass-otp.bash.completion) maybe also checking for that is an option.




-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
