# [\#769 Issue](https://github.com/koalaman/shellcheck/issues/769) `closed`: SC1090/SC1091: sourcing should find files relative to script

#### <img src="https://avatars.githubusercontent.com/u/2822847?v=4" width="50">[petersohn](https://github.com/petersohn) opened issue at [2016-11-04T09:43:26Z](https://github.com/koalaman/shellcheck/issues/769):

When using the directive `source=foobar.sh`, then it seems to look up the source relative to the path shellcheck is being run from. For example, I have the following scripts: `foo.sh` and `foobar.sh` in the same directory (for example `bar/baz` from the project root).

Suppose there is the following line in `foo.sh`:

```bash
source "${0%/*}/foobar.sh"
```

I will get a [SC1090](https://github.com/koalaman/shellcheck/wiki/SC1090) error because shellcheck is unable to resolve the path. I have to do this:

```bash
# shellcheck source bar/baz/foobar.sh
source "${0%/*}/foobar.sh"
```

This works, but only if I run the checker from the project root (either by specifying `foo/bar/foobar.sh` as an input file, or with `-x`), but not if I run it from anywhere else. There should be a way to tell shellcheck to look for a file relative to the checked file's location, not where shellcheck is run. For example, this way:

```bash
# shellcheck source ./foobar.sh
source "${0%/*}/foobar.sh"
```

#### <img src="https://avatars.githubusercontent.com/u/34676?v=4" width="50">[timvisher](https://github.com/timvisher) commented at [2016-11-04T12:23:34Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-258417768):

I think generally speaking this is a bad idea which is perhaps why
shellcheck requires you to be explicit about it.
http://mywiki.wooledge.org/BashFAQ/028


#### <img src="https://avatars.githubusercontent.com/u/2822847?v=4" width="50">[petersohn](https://github.com/petersohn) commented at [2016-11-04T12:44:27Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-258421453):

I know that `$0` is not the best solution to it. I should have used `$BASH_SOURCE` or something else. But it's besides the point. What I want is to tell ShellCheck where a certain file is located relative to the script. I think it is generally a bad idea to make something work differently (in this case, not work at all) depending on where you run it from.


#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2016-11-05T19:07:39Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-258633970):

`source foo` would also read from the current directory rather than the directory with the script, but I see how that could be useful. Maybe there should be some option to set the directory from which to resolve these paths.


#### <img src="https://avatars.githubusercontent.com/u/149464?v=4" width="50">[rbroemeling](https://github.com/rbroemeling) commented at [2017-02-06T14:59:55Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-277707597):

@koalaman I can see the argument that the behaviour as it stands could be useful in some edge cases, but _generally_ I would expect that "resolve relative `source=` directives from the location of the containing script" would make by far the most sense.

As it currently works (where shellcheck resolves relative `source=` directives from the location that it is executed from) is a bit surprising and disappointing. It makes it so that there is no good way to ensure that it works when executing shellcheck against a script that contains a relative inclusion (this is not an uncommon occurrence) based off of its own path (using either `$0` or `$BASH_SOURCE`, which is again not exactly unheard of).

I'd claim (at least in my work environment) that this scenario is actually _far_ more common than the environment wherein using the current working directory makes more sense. For us, it is rather unequivocal that the script's parent directory would be a far, far better default.

At the least, I'd like to see a command-line argument to switch behaviours, but I really think that serious thought should be given to making relative links resolve from the script's parent directory, rather than from the current working directory that `shellcheck` has been executed from.

#### <img src="https://avatars.githubusercontent.com/u/345640?v=4" width="50">[pszalko](https://github.com/pszalko) commented at [2017-02-22T21:34:51Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-281811589):

If directive `#shellcheck source=` is used then we explicitly say shellcheck where to look for included files, however there is currently no way to make this path relative to the script file itself.

I agree with @rbroemeling and @petersohn currently `#shellcheck source=` directive is not very usable. 

#### <img src="https://avatars.githubusercontent.com/u/188063?v=4" width="50">[viraptor](https://github.com/viraptor) commented at [2017-05-10T02:24:23Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-300355527):

I'm also interested in getting the relative sources to resolve.

> source foo would also read from the current directory rather than the directory with the script

If you're worried about existing scripts, you could always make both options explicit. For example:
```
# shellcheck source=relative/to/pwd
# shellcheck rel-source=relative/to/analysed-script
```

#### <img src="https://avatars.githubusercontent.com/u/544596?v=4" width="50">[nihilus](https://github.com/nihilus) commented at [2017-11-06T20:53:01Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-342282734):

Actually 'proot' solved my problems enabling me to chroot to the target directory without modifying it and invoke shellcheck as if it was part of the target directory.

#### <img src="https://avatars.githubusercontent.com/u/17302670?v=4" width="50">[jpresley23](https://github.com/jpresley23) commented at [2017-12-01T15:24:08Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-348522844):

In a CI/CD pipeline this isn't an issue, since I control which directory shellcheck is executed from. The issue I have is when using a shellcheck in an IDE. It would be useful to have an argument I can pass to shellcheck to indicate the root path it should use to resolve the directives.

#### <img src="https://avatars.githubusercontent.com/u/40799?v=4" width="50">[docwhat](https://github.com/docwhat) commented at [2018-10-23T20:02:07Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-432397570):

This seems to be a duplicate of #539 — Maybe we should move everything into one place (and possible edit the subject to be easier to find).

#### <img src="https://avatars.githubusercontent.com/u/133706?v=4" width="50">[stdedos](https://github.com/stdedos) commented at [2018-12-03T09:35:16Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-443645844):

@koalaman Wiki for https://github.com/koalaman/shellcheck/wiki/SC1090 is confusing: The path there looks relative. However, if I have 2 files in the same directory (and I get SC1090 on one of them) I cannot write `# shellcheck source=./script.bash` or `# shellcheck source=script.bash` on the other one (consistent with this ticket).

"Who is right", "is there any fix coming", "any status update"? (`shellcheck 0.6.0` static)

#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2018-12-09T22:46:45Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-445579224):

The path is relative, and it's relative to shellcheck's current working directory. It is *not* relative to the file in which you put this directive.

#### <img src="https://avatars.githubusercontent.com/u/464648?v=4" width="50">[austin987](https://github.com/austin987) commented at [2018-12-10T01:04:14Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-445605783):

This presents a problem if i.e. I want to run shellcheck as part of the unit tests, from the top of the project. If I then want to run shellcheck individually while editing a file, the path may be incorrect.

Maybe two (or more) parts could be provided, to account for that?

#### <img src="https://avatars.githubusercontent.com/u/947350?v=4" width="50">[DanielJoyce](https://github.com/DanielJoyce) commented at [2018-12-11T17:41:25Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-446293374):

This really needs to be fixed, makes it a pain to run shellcheck as part of builds, etc, as paths wrt where shellcheck is run can change.

#### <img src="https://avatars.githubusercontent.com/u/2491070?v=4" width="50">[nthexwn](https://github.com/nthexwn) commented at [2019-02-26T00:50:29Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-467246244):

Specifying the path [relative to where shellcheck is running from](https://github.com/koalaman/shellcheck/wiki/SC1090) is only realistic for a single user/system.  Using `# shellcheck source=/dev/null` as a workaround seems to be the best approach until this is fixed.

#### <img src="https://avatars.githubusercontent.com/u/464648?v=4" width="50">[austin987](https://github.com/austin987) commented at [2019-02-26T01:04:12Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-467249372):

Just because it's hard doesn't mean it can't be improved. The path isn't relative to shellcheck, but rather the current working directory.

Allowing relative paths and/or multiple paths would help for automated builds among other cases.

#### <img src="https://avatars.githubusercontent.com/u/49906529?v=4" width="50">[ee987](https://github.com/ee987) commented at [2019-04-23T11:35:58Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-485765998):

We also have big problems with source-directive being relative cwd and not script location. 
While relative paths would be very useful for resolving this, why not look at the actual root cause: shellcheck not being able to resolve dynamic paths such as these:

    source "${0%/*}/foobar.sh"    
    or
    source "${BASH_SOURCE[0]}/foobar.sh"
    or
    SCRIPT_DIR=$(cd "$(dirname "$(readlink -f "$0")")" || exit 1; pwd)
    source "$SCRIPT_DIR/foobar.sh"

That would remove the need for source-directive completely because the only time when we actually need source-directive in the first place is in such scenarios. 

It might be runtime code but the pattern is easily identifiable so a small set of special cases could be defined to resolve script directory. Shellcheck could recommend one of them and make sure that source-following works with it. Or shellcheck could bundle a companion-library with built in functions for this.

#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2019-04-25T02:08:00Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-486492469):

With 301705edea you can specify `-P SCRIPTDIR`  to search relative to the script's directory, or `-P /my/path` to search relative to a path in the filesystem. Hopefully this helps!

It would also be simple to make this the default, if that would make things even easier

#### <img src="https://avatars.githubusercontent.com/u/25647368?v=4" width="50">[iyerusad](https://github.com/iyerusad) commented at [2019-05-22T10:19:00Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-494743384):

I recently added shellcheck to my workflow within VS Code (via this [extension](https://marketplace.visualstudio.com/items?itemName=timonwong.shellcheck)). As such, shellcheck is called per file. Very helpful in IDE, I tend to jump between several languages/syntax in any given day - Thank you.

In my limited view, I'm thinking `#shellcheck source=` directive _should **attempt** to be relative by default:_
1. If I'm specifying a shellcheck source directive, it means higher up helpers (such as recent https://github.com/koalaman/shellcheck/commit/301705edea829c5153c43bf52215d09b5e00d182) have been unsuccessful (or are undefined)
2. `shellcheck` executable location not likely same as script being checked.

---
My particular instance of SC1090 is (where `dotfiles` is a repo):
```
if [ -f "$HOME/dotfiles/1" ];then
  . "$HOME/dotfiles/1"
fi
```
It would be nice if could handle case of `$HOME` - I rather avoid helper directives in code unless explicitly breaking a rule for a really good reason.


#### <img src="https://avatars.githubusercontent.com/u/52473140?v=4" width="50">[solarmosaic-kflorence](https://github.com/solarmosaic-kflorence) commented at [2019-12-10T00:16:49Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-563500354):

Agreed, `-P SCRIPTDIR` is great but hard to utilize in IDE.

#### <img src="https://avatars.githubusercontent.com/u/1442700?v=4" width="50">[drAlberT](https://github.com/drAlberT) commented at [2020-01-10T11:13:27Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-572994261):

> With [301705e](https://github.com/koalaman/shellcheck/commit/301705edea829c5153c43bf52215d09b5e00d182) you can specify `-P SCRIPTDIR` to search relative to the script's directory, or `-P /my/path` to search relative to a path in the filesystem. Hopefully this helps!
> 
> It would also be simple to make this the default, if that would make things even easier

this is not useful for using shellcheck inside editors. for example vim and syntastic .. Please make the current-script-relative source the default one

#### <img src="https://avatars.githubusercontent.com/u/5340840?v=4" width="50">[gotrevor](https://github.com/gotrevor) commented at [2020-02-06T20:27:59Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-583095244):

> > With [301705e](https://github.com/koalaman/shellcheck/commit/301705edea829c5153c43bf52215d09b5e00d182) you can specify `-P SCRIPTDIR` to search relative to the script's directory, or `-P /my/path` to search relative to a path in the filesystem. Hopefully this helps!
> > It would also be simple to make this the default, if that would make things even easier
> 
> this is not useful for using shellcheck inside editors. for example vim and syntastic .. Please make the current-script-relative source the default one

@drAlberT You can add this line to your .vimrc to pass options to shellcheck w/ syntastic:
```
[~] $ grep shellcheck .vimrc
let g:syntastic_sh_shellcheck_args = "-x -P SCRIPTDIR"
```

#### <img src="https://avatars.githubusercontent.com/u/1442700?v=4" width="50">[drAlberT](https://github.com/drAlberT) commented at [2020-02-07T09:40:37Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-583311046):

> > > With [301705e](https://github.com/koalaman/shellcheck/commit/301705edea829c5153c43bf52215d09b5e00d182) you can specify `-P SCRIPTDIR` to search relative to the script's directory, or `-P /my/path` to search relative to a path in the filesystem. Hopefully this helps!
> > > It would also be simple to make this the default, if that would make things even easier
> > 
> > 
> > this is not useful for using shellcheck inside editors. for example vim and syntastic .. Please make the current-script-relative source the default one
> 
> @drAlberT You can add this line to your .vimrc to pass options to shellcheck w/ syntastic:
> 
> ```
> [~] $ grep shellcheck .vimrc
> let g:syntastic_sh_shellcheck_args = "-x -P SCRIPTDIR"
> ```

My use case is to have relative scripts, not a global wide SCRIPTDIR ..  I made a number of different combination of dirs in .vimrc but no luck

Have you any suggestion to add the current SCRIPT dir as SCRIPTDIR on a per file basis ?


#### <img src="https://avatars.githubusercontent.com/u/40799?v=4" width="50">[docwhat](https://github.com/docwhat) commented at [2020-02-07T15:39:15Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-583458253):

I totally did not understand that `SCRIPTDIR` is a magic value that allows relative-to-script's-directory lookups.

I  thought `SCRIPTDIR`  was meant to be "the path to the script's directory".

#### <img src="https://avatars.githubusercontent.com/u/4670883?v=4" width="50">[JoshCooley](https://github.com/JoshCooley) commented at [2020-02-25T23:49:04Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-591144367):

@docwhat yeah I thought the same thing until I read your comment.
For anyone using [SublimeLinter-shellcheck](https://github.com/SublimeLinter/SublimeLinter-shellcheck) you can add the needed args to the user's SublimeLinter.sublime-settings file, e.g.

```
// SublimeLinter Settings - User
{
  "linters": {
    "shellcheck": {
      "args": [
        "--source-path=SCRIPTDIR",
        "--external-sources"
      ]
    }
  }
}
```

#### <img src="https://avatars.githubusercontent.com/u/464857?v=4" width="50">[jeffomatic](https://github.com/jeffomatic) commented at [2022-08-07T18:02:40Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-1207457431):

For folks using VSCode and Timon Wong's shellcheck extension, add the following to your user settings:

```
    "shellcheck.customArgs": [
        "--source-path=SCRIPTDIR",
        "--external-sources"
    ]
```

This is just a quick adaptation of @JoshCooley's [Sublime config](https://github.com/koalaman/shellcheck/issues/769#issuecomment-591144367).

#### <img src="https://avatars.githubusercontent.com/u/7662191?v=4" width="50">[armenr](https://github.com/armenr) commented at [2023-01-18T11:29:42Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-1386904967):

> For folks using VSCode and Timon Wong's shellcheck extension, add the following to your user settings:

@jeffomatic - This doesn't seem to work for me. 

#### Config:
![Screenshot 2023-01-18 at 3 28 33 PM](https://user-images.githubusercontent.com/7662191/213160059-de309821-6dbe-4112-8f73-f57e0f58b552.png)

#### Code (some-script.sh)
```
# shellcheck source=../_shared/utils-shared_functions.sh
source ../_shared/utils-shared_functions.sh
```

#### IDE:
![Screenshot 2023-01-18 at 3 29 31 PM](https://user-images.githubusercontent.com/7662191/213160244-cfde7a22-498b-4624-8c26-d67d15e256fd.png)


#### <img src="https://avatars.githubusercontent.com/u/5264823?v=4" width="50">[cfgnunes](https://github.com/cfgnunes) commented at [2023-06-18T14:28:15Z](https://github.com/koalaman/shellcheck/issues/769#issuecomment-1596167979):

Hi @armenr!

I think that the `--source-path=` should have the full path of the base directory, such as:
`/home/armenr/app/_shared/`
