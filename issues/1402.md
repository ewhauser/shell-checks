# [\#1402 Issue](https://github.com/koalaman/shellcheck/issues/1402) `open`: SC2003 should not warn about `expr index` usage

#### <img src="https://avatars.githubusercontent.com/u/176950?v=4" width="50">[vapier](https://github.com/vapier) opened issue at [2018-11-29T19:15:01Z](https://github.com/koalaman/shellcheck/issues/1402):

#### Here's a snippet or screenshot that shows the problem:

```sh
#!/bin/sh
expr index 'abc' b
```

#### Here's what shellcheck currently says:

```
Line 2:
expr index 'abc' b
^-- SC2003: expr is antiquated. Consider rewriting this using $((..)), ${} or [[ ]].
```

#### Here's what I wanted or expected to see:

It shouldn't warn at all.  There is no builtin shell equivalent for `expr index`.

#### <img src="https://avatars.githubusercontent.com/u/176950?v=4" width="50">[vapier](https://github.com/vapier) commented at [2018-11-29T19:22:18Z](https://github.com/koalaman/shellcheck/issues/1402#issuecomment-442960069):

hrm, handling of length, substr, index, or match is a bit dubious.  POSIX marks those as undefined.
http://pubs.opengroup.org/onlinepubs/9699919799/utilities/expr.html

But it's not a shell builtin, so we can't bind to the dialect (POSIX/bash/etc...) as a signal whether they would be available in the script environment.

#### <img src="https://avatars.githubusercontent.com/u/3129206?v=4" width="50">[matthewpersico](https://github.com/matthewpersico) commented at [2018-11-29T19:41:57Z](https://github.com/koalaman/shellcheck/issues/1402#issuecomment-442966710):

@vapier, I think the point is that `expr` is not antiquated in Bourne shell, that `$((..)), ${}, [[ ]]` are not available in Bourne shell to do arithmetic stuff. And even if `#!/bin/sh` happens to be a link to `bash` at the time when shellcheck is run, you cannot guarantee that going forward; think running shellcheck in dev and then deploying somewhere else.

#### <img src="https://avatars.githubusercontent.com/u/176950?v=4" width="50">[vapier](https://github.com/vapier) commented at [2018-11-30T07:23:24Z](https://github.com/koalaman/shellcheck/issues/1402#issuecomment-443115190):

i think you missed the point i'm highlighting.  i'm not talking about expr's arithmetic subcommands here (which the builtin `$((...))` arithmetic syntax handles fine), i'm talking about the string subcommands.  there is no equiv in the shell.

#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2018-12-02T21:55:35Z](https://github.com/koalaman/shellcheck/issues/1402#issuecomment-443545832):

SC2003 already has a few exceptions for things that are hard to reproduce, like `:`, and `index` could definitely be added to that list.

POSIX says that "The use of string arguments length, substr, index, or match produces unspecified results.", so are there any workarounds that also work while also being completely POSIX?

Maybe `var=abc; prefix=${var%%b*}; echo "${#prefix}"` but you'd have to handle the case of no matching substring.

#### <img src="https://avatars.githubusercontent.com/u/176950?v=4" width="50">[vapier](https://github.com/vapier) commented at [2018-12-02T22:40:06Z](https://github.com/koalaman/shellcheck/issues/1402#issuecomment-443549182):

`expr length <str>` can be replaced with `var="<str>"; echo ${#var}` or `echo "<str>" | wc -c`.

`expr substr <string> <pos> <length>` could be replaced with two cuts: `echo "<string>" | cut -b<pos>- | cut -b-<length>`.

`expr match <string> <regex>` (and `expr <string> : <regex>`) could be replaced with `echo "<string>" | grep -q "<regex>"`.

i don't have an answer for `expr index`.  but we should be able to improve SC2003 for the other subcommands in the meantime.

#### <img src="https://avatars.githubusercontent.com/u/2182746?v=4" width="50">[chuhn](https://github.com/chuhn) commented at [2020-12-21T15:11:45Z](https://github.com/koalaman/shellcheck/issues/1402#issuecomment-749022656):

It's not only about return codes.

`expr match …` returns matched subexpressions:
```bash
$ expr match 'ASDF1234qwer' '[[:upper:]]\+\([[:digit:]]\+\)[[:lower:]]\+'
1234
```

The same results may also be achieved with bash built-ins  (or `echo '…' | sed -e 's/…/\1/'`) but those approaches are IMHO not more favourable than `expr` in general.

#### <img src="https://avatars.githubusercontent.com/u/10828883?v=4" width="50">[patsevanton](https://github.com/patsevanton) commented at [2023-01-16T10:04:37Z](https://github.com/koalaman/shellcheck/issues/1402#issuecomment-1383790674):

How correct write bash code?
```
echo Execution time was $(expr "$end_time" - "$start_time")  s.
```

#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2023-01-16T11:48:16Z](https://github.com/koalaman/shellcheck/issues/1402#issuecomment-1383932457):

> How correct write bash code?
> 
> ```
> echo Execution time was $(expr "$end_time" - "$start_time")  s.
> ```

either using printf or echo and with bash as destination shell (the thread is about portability though).

```sh
echo "Execution time was $(( end_time - start_time )) s."
printf "Execution time was %d s." $(( end_time - start_time ))
```
