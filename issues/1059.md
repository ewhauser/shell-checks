# [\#1059 Issue](https://github.com/koalaman/shellcheck/issues/1059) `open`: Single quotes with awk stored in variable

#### <img src="https://avatars.githubusercontent.com/u/29568484?v=4" width="50">[winmasta](https://github.com/winmasta) opened issue at [2017-11-22T05:53:01Z](https://github.com/koalaman/shellcheck/issues/1059):

#### For new checks and feature suggestions
```sh
"$AWK" '/inet addr/{print substr($2,6)}'
```
if there is no straight link to awk shellcheck returns SC2016 but it would bi nice to check vars before "problem" quotes




#### <img src="https://avatars.githubusercontent.com/u/5853223?v=4" width="50">[rdebath](https://github.com/rdebath) commented at [2018-02-14T12:43:56Z](https://github.com/koalaman/shellcheck/issues/1059#issuecomment-365596244):

I would suggest confusing it into doing the right thing...
``` sh
awk() {
    local a
    a=/usr/bin/mawk
    [[ -x "$a" ]] || a=/usr/bin/gawk
    [[ -x "$a" ]] || a=/usr/bin/awk
    [[ -x "$a" ]] || a="command awk"
    eval "awk() { $a \"\$@\"; }"
    $a "$@"
}
```
