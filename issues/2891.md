# [\#2891 Issue](https://github.com/koalaman/shellcheck/issues/2891) `open`: Single-quoted solitary item in for loop spuriously triggers SC2041 but not SC2043

#### <img src="https://avatars.githubusercontent.com/u/11864?v=4" width="50">[antifuchs](https://github.com/antifuchs) opened issue at [2023-12-24 14:22](https://github.com/koalaman/shellcheck/issues/2891):

#### For bugs
- Rule Id (if any, e.g. SC1000): SC2041
- My shellcheck version (`shellcheck --version` or 'online'): online
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit
- [ ] It's not reproducible on shellcheck.net, but I think that's because it's an OS, configuration or encoding issue

#### For new checks and feature suggestions
- [x] shellcheck.net (i.e. the latest commit) currently gives no useful warnings about this
- [x] I searched through https://github.com/koalaman/shellcheck/issues and didn't find anything related


#### Here's a snippet or screenshot that shows the problem:

```sh
#!/usr/bin/env bash

for dev in '/dev/disk/by-uuid/28DD-A89E'; do
  echo "$dev"
done
```

#### Here's what shellcheck currently says:
```
Line 3	SC2041: This is a literal string. To run as a command, use $(..) instead of '..' . 
```
#### Here's what I wanted or expected to see:

I would not expect this to warn about SC2041, but SC2043. The following snippet where a second single-quoted element is iterated over does not warn about SC2041 (and, correctly, not about SC2043):

```sh
#!/usr/bin/env bash

for dev in '/dev/disk/by-uuid/28DD-A89E' 'foo'; do
  echo "$dev"
done
```

and neither does the following with double quotes (it does warn about SC2043, as expected):

```sh
#!/usr/bin/env bash

for dev in "double-quoted"; do
  echo "$dev"
done
```


#### <img src="https://avatars.githubusercontent.com/u/255938?v=4" width="50">[ormaaj](https://github.com/ormaaj) commented at [2023-12-30 18:05](https://github.com/koalaman/shellcheck/issues/2891#issuecomment-1872575819):

That word doesn't require quotes anyway so... maybe it's just guessing that it's a command-in-a-string for some reason. Erroneous if so.

[This](https://github.com/koalaman/shellcheck/wiki/SC2041) is just silly and hopefully not the actual rationale. There are several noisy and not super useful single-quoting warnings similar to this - where it tries to guess that the content of a single-quoted word was supposed to be something else.

#### <img src="https://avatars.githubusercontent.com/u/9742635?u=36720f45562c3a3a8945fc3fc9fd019bbb3d329d&v=4" width="50">[iFreilicht](https://github.com/iFreilicht) commented at [2024-11-10 14:40](https://github.com/koalaman/shellcheck/issues/2891#issuecomment-2466761592):

This is indeed quite annoying, especially when auto-generating scripts, see https://github.com/nix-community/disko/issues/868

In my case, it's also purely about strings that all start with `/dev`, so I'd be fine if there was just a short list of exceptions that prevented this from triggering. Something like `ls` or `/bin/bash` could indeed be a command.


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
