# [\#3288 Issue](https://github.com/koalaman/shellcheck/issues/3288) `open`: Recommend set -eufo pipefail and discourage POSIX traps

#### <img src="https://avatars.githubusercontent.com/u/5316?u=8763bdb9ba5cbe23084e8249dcbd21bbb23150f2&v=4" width="50">[mcandre](https://github.com/mcandre) opened issue at [2025-09-02 00:23](https://github.com/koalaman/shellcheck/issues/3288):

#### For new checks and feature suggestions
- [x] https://www.shellcheck.net/ (i.e. the latest commit) currently gives no useful warnings about this
- [x] I searched through https://github.com/koalaman/shellcheck/issues and didn't find anything related

#### Here's a snippet or screenshot that shows a potential problem:

seach-java:

```sh
#!/bin/sh
trap ./clean ERR EXIT SIGINT

(false)

ls test/*

find "$JAVA_HOME" -type f -name -print0 |
    xargs -0 echo
```

```console
$ mkdir -p test/fixtures
$ unset JAVA_HOME
$ ./search-java
...
```

#### Here's what shellcheck currently says:

Nothing

#### Here's what I wanted to see:

* Prefer function traps to list traps for safety
* Enable `set -e` to safeguard against system corruption
* Enable `set -u` to safeguard against potentially undefined variables
* Enable `set -f` to safeguard against glob portability issues
* Enable `set -o pipefail` to safeguard against failures during piped aggregate commands

#### <img src="https://avatars.githubusercontent.com/u/12705661?u=7d7c4a7a8564e173cc8c0f6ac0254707a340ba49&v=4" width="50">[juliyvchirkov](https://github.com/juliyvchirkov) commented at [2025-09-18 07:08](https://github.com/koalaman/shellcheck/issues/3288#issuecomment-3305732844):

Seems shellcheck implements some internal policy to ignore a variable totally if the last one is named with all caps and hasn't been declared in the script implicitly, probably assuming such a variable to be an external global one

For example, in my huge set of scripts implemented to provide flexible customised automated maintenance of remote server routines each script starts with the following header

```sh
#!/bin/sh
############ *posix* #############
# shellcheck shell=sh enable=all #
##################################

[ -z "${SHDEBUG}" ] || set -vx
```

And shellcheck with all its rules enabled (enforced by directive on line 3 of the header) had never produced no warning kinda [SC2154: var is referenced but not assigned](https://www.shellcheck.net/wiki/SC2154), it just silently ignores `$SHDEBUG`

#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2025-09-18 07:54](https://github.com/koalaman/shellcheck/issues/3288#issuecomment-3305948458):

> Seems shellcheck implements some internal policy to ignore a variable totally if the last one is named with all caps and hasn't been declared in the script implicitly, probably assuming such a variable to be an external global one

indeed. all caps are treated as globals and ignored.

A [optional check](https://github.com/koalaman/shellcheck/wiki/Optional) exists in this space.
> name:    check-unassigned-uppercase
desc:    Warn when uppercase variables are unassigned
example: echo $VAR
fix:     VAR=hello; echo $VAR

#### <img src="https://avatars.githubusercontent.com/u/12705661?u=7d7c4a7a8564e173cc8c0f6ac0254707a340ba49&v=4" width="50">[juliyvchirkov](https://github.com/juliyvchirkov) commented at [2025-09-18 12:18](https://github.com/koalaman/shellcheck/issues/3288#issuecomment-3307144964):

@brother [As it follows from this document](https://github.com/koalaman/shellcheck/wiki/Optional), the whole list of optional checks, including ``check-unassigned-uppercase``, should turn active when ``enable=all`` directive is set

And I can confirm linting my scripts with ``shellcheck`` I've faced a number of times a lions' share of optional checks listed by ``shellcheck --list-optional`` - like ``add-default-case``, ``check-extra-masked-returns``, ``quote-safe-variables``, ``useless-use-of-cat`` and ``deprecate-which``

Since I prefer to enable all checks by default and then to suppress the concrete one once per case if any, the header of each my script includes the line ``# shellcheck shell=sh enable=all #``

```sh
#!/bin/sh
############ *posix* #############
# shellcheck shell=sh enable=all #
##################################

[ -z "${SHDEBUG}" ] || set -vx
```

Thus, as I understand, ``check-unassigned-uppercase`` should fire for ``$SHDEBUG``, but for some reason it never does

[Shellcheck online](https://www.shellcheck.net/) ignores ``$SHDEBUG`` as well

<img src="https://i.ibb.co/FbkzX5mL/2025-09-18-15-11-41.png">

Please advice

#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2025-09-18 13:21](https://github.com/koalaman/shellcheck/issues/3288#issuecomment-3307428406):

The test for -z var is special and might not be the best example for demonstrating that the enable=all is broken.
If you change to a echo of the variable the output indicates it to work as expected. 

I guess you could wish for a more comprehensive check and warning, beats me what path to choose there.

<img width="829" height="351" alt="Image" src="https://github.com/user-attachments/assets/d17b1dbd-dbc4-4767-b8c0-ab4ed9e41ad8" />


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
