# [\#613 Issue](https://github.com/koalaman/shellcheck/issues/613) `open`: SC2164 need not warn if cd is the last command in a function

#### <img src="https://avatars.githubusercontent.com/u/1885701?v=4" width="50">[dimo414](https://github.com/dimo414) opened issue at [2016-02-20T16:59:49Z](https://github.com/koalaman/shellcheck/issues/613):

If `cd` is the last command in a function there's no need to append `|| return`.

**Updated with new bug template:**

#### For bugs
- Rule Id (if any, e.g. SC1000): SC2164
- My shellcheck version (`shellcheck --version` or 'online'): online
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit
- [ ] It's not reproducible on shellcheck.net, but I think that's because it's an OS, configuration or encoding issue

#### For new checks and feature suggestions
- [x] shellcheck.net (i.e. the latest commit) currently gives no useful warnings about this
- [ ] I searched through https://github.com/koalaman/shellcheck/issues and found [#592](https://github.com/koalaman/shellcheck/issues/592#issuecomment-180002861) which is related, but not specifically about the last-command-in-function case.

#### Here's a snippet or screenshot that shows the problem:

```sh
#!/bin/bash
no_place_like_home() {
  cd ~
}
```

#### Here's what shellcheck currently says:
```
Line 3	SC2164: Use 'cd ... || exit' or 'cd ... || return' in case cd fails.
```
#### Here's what I wanted or expected to see:

This seems correct to me - adding `|| return` doesn't change `no_place_like_home`'s behavior at all.

#### <img src="https://avatars.githubusercontent.com/u/29582865?v=4" width="50">[felipecrs](https://github.com/felipecrs) commented at [2021-03-22T11:14:08Z](https://github.com/koalaman/shellcheck/issues/613#issuecomment-803982024):

+1, I'm currently skipping the check because of it.
