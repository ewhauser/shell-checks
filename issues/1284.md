# [\#1284 Issue](https://github.com/koalaman/shellcheck/issues/1284) `open`: SC2086: Double quote while using set

#### <img src="https://avatars.githubusercontent.com/u/17122340?v=4" width="50">[sahil-lakhwani](https://github.com/sahil-lakhwani) opened issue at [2018-07-06T12:10:35Z](https://github.com/koalaman/shellcheck/issues/1284):

Rule Id: SC2086
shellcheck -V : 0.5.0

I am using [this compliance script](https://secscan.acron.pl/centos7/6/2/6).

shellcheck complains at the below line:
`set -- $p`

`error: SC2086: Double quote to prevent globbing and word splitting.`

The problem is that if I put a double quote around `$p`, the set command, I guess, interprets in a different way. Hence, the value of `$1` is computed same as of `$p` instead of the first token from `$p`.

#### <img src="https://avatars.githubusercontent.com/u/3129206?v=4" width="50">[matthewpersico](https://github.com/matthewpersico) commented at [2018-07-06T12:33:59Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-403020830):

Correct. With quotes, $p is one string, with spaces embedded. The solution is to add a shellcheck disable directive at the line above; that’s what they are for.

The REAL solution is to change the assignment to $p so that $p is an array. I am surprised that shellcheck didn’t complain about that assignment. 

#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2018-07-06T16:58:11Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-403090750):

In an `array=( $p )` you would have gotten a better warning "Quote to prevent word splitting, or split robustly with mapfile or read -a."

Maybe `set -- $p` should have a similar warning when the shell is bash, and be ignored in sh.

#### <img src="https://avatars.githubusercontent.com/u/17122340?v=4" width="50">[sahil-lakhwani](https://github.com/sahil-lakhwani) commented at [2018-07-10T11:54:43Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-403796076):

@matthewpersico I am not sure how would `set` work on an array then.

#### <img src="https://avatars.githubusercontent.com/u/3129206?v=4" width="50">[matthewpersico](https://github.com/matthewpersico) commented at [2018-07-10T12:10:33Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-403799779):

```bash
declare -a p
readarray -t p < <(echo "$PATH" | sed -e 's/::/:/' -e 's/:$//' -e 's/:/\n/g')
set -- "${p[@]}"
```
Things to note:
1) The naive assignment ```p=($(echo $PATH | sed -e 's/::/:/' -e 's/:$//' -e 's/:/ /g'))``` fails any time an individual element of the PATH has a space in it, usually in Windows. Using the readarray properly keeps such paths as one element.
2) Furthermore, the change of : to \n instead of ' ' is made because readarray is line-based. It becomes more obvious when you lookup up readarray in the bash man page and see that it is just a synonym for 'mapfile'.

#### <img src="https://avatars.githubusercontent.com/u/17122340?v=4" width="50">[sahil-lakhwani](https://github.com/sahil-lakhwani) commented at [2018-07-10T12:49:25Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-403809987):

@matthewpersico Got it. Thanks for the insights.

#### <img src="https://avatars.githubusercontent.com/u/176950?v=4" width="50">[vapier](https://github.com/vapier) commented at [2018-08-28T20:32:21Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-416730436):

should close this bug then ?  sounds like shellcheck is correct and the warning is WAI.

#### <img src="https://avatars.githubusercontent.com/u/10958249?v=4" width="50">[philippgl](https://github.com/philippgl) commented at [2018-09-25T13:23:04Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-424341335):

I think this is a bug / should be fixed.
If you want to set a single variable, you would use `var="$p"` with a nice variable name.

As the `set --` is used for the positional args ($1, $2, ...), I would assume 99% of the times, you want the splitting. I think, this could be a warning in bash or other nice shells with arrays.

I only use this in shells w/o arrays, where this is the only "array" available and I would to place the output of some command into the pos. args like in `set -- $(cmd...)`.


#### <img src="https://avatars.githubusercontent.com/u/176950?v=4" width="50">[vapier](https://github.com/vapier) commented at [2018-09-25T22:10:01Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-424518672):

the linter is correct: the code written `set -- $p` undergoes globbing/splitting and needs quoting.  if you don't want it to warn, disable the warning in your code.

#### <img src="https://avatars.githubusercontent.com/u/10958249?v=4" width="50">[philippgl](https://github.com/philippgl) commented at [2018-09-26T07:55:50Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-424621456):

I know, that `set -- $p` does the splitting and globbing. I think, this is the main reason to do the `set -- $p` in the first place. `set -- $p` is used to assign multiple variables, right?
So for the only (?) legitimate use of `set --`, I always have to disable the warning?
If you do `set --"$p"`, it just assigns the named variable `$p` to `$1`.

#### <img src="https://avatars.githubusercontent.com/u/176950?v=4" width="50">[vapier](https://github.com/vapier) commented at [2018-09-26T19:24:27Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-424839470):

i've seen POSIX code like:

```sh
set -- "${first_arg}"
if <some condition>; then
  set -- "$@" --some-arg
fi
```

the fact that it's difficult to dtrt doesn't mean we should make the linter silent.  `set -- $p` is objectively wrong as the linter is correctly calling out.  you can disable path expansion via `set`, and silence the linting warning for the one line as documentation that you want the word splitting.

#### <img src="https://avatars.githubusercontent.com/u/10958249?v=4" width="50">[philippgl](https://github.com/philippgl) commented at [2018-09-28T10:12:36Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-425390075):

Can I disable this w/o changing the file itself?
I do not want to clutter my source code with linter specific stuff. Probably I also need to do this for each linter?
The only option would be to pass the `--exclude=SC2056`, which would disable it for the whole file, right?

#### <img src="https://avatars.githubusercontent.com/u/176950?v=4" width="50">[vapier](https://github.com/vapier) commented at [2018-09-28T19:35:36Z](https://github.com/koalaman/shellcheck/issues/1284#issuecomment-425543450):

correct, `--exclude` affects the whole file, but does not require modification of it
