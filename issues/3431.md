# [\#3431 Issue](https://github.com/koalaman/shellcheck/issues/3431) `closed`: spurious syntax error for zsh string interpolations

#### <img src="https://avatars.githubusercontent.com/u/5316?u=8763bdb9ba5cbe23084e8249dcbd21bbb23150f2&v=4" width="50">[mcandre](https://github.com/mcandre) opened issue at [2026-03-25 20:40](https://github.com/koalaman/shellcheck/issues/3431):

#### For bugs with existing features

- Rule Id (if any, e.g. SC1000): SC2296
- My shellcheck version (`shellcheck --version` or "online"): 0.11.0
- [x] The rule's wiki page does not already cover this (e.g. https://shellcheck.net/wiki/SC2086)
- [x] I tried on https://www.shellcheck.net/ and verified that this is still a problem on the latest commit

#### Here's a snippet or screenshot that shows the problem:

```zsh
_mage() {
    local tasks=( ${(f)"$(mage -l | awk '{ sub(/\*$/, "", $1); if ($1 != "" && $1 != "Targets:") print $1 }')"} )
    _arguments -C '1:task:('"${tasks[*]}"')'
}
compdef _mage mage
```

#### Here's what shellcheck currently says:

```txt
In src/.zshrc.d/autocompletions.zsh line 36:
        local tasks=( ${(f)"$(mage -l | awk '{ sub(/\*$/, "", $1); if ($1 != "" && $1 != "Targets:") print $1 }')"} )
                        ^-^ SC2296 (error): Parameter expansions can't start with (. Double check syntax.
```

#### Here's what I wanted or expected to see:

Nothing. zsh extends POSIX sh with many features, including `(f)` string interpolation syntax to populate arrays from newline delimited strings.

#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2026-03-26 08:28](https://github.com/koalaman/shellcheck/issues/3431#issuecomment-4132645069):

zsh is not supported (yet?), see #809


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
