# [\#2820 Issue](https://github.com/koalaman/shellcheck/issues/2820) `closed`: localsetting php

#### <img src="https://avatars.githubusercontent.com/u/143692444?v=4" width="50">[faisal599](https://github.com/faisal599) opened issue at [2023-08-31 07:16](https://github.com/koalaman/shellcheck/issues/2820):

#### For bugs
- Rule Id (if any, e.g. SC1000): SC2148, SC2154, SC2283, SC2211, SC2288, SC2154, SC2283, SC2211, SC2288, SC2283, SC2211, SC2288, SC2283, SC2211, SC2288, SC2283, SC2211, SC2288, SC2283, SC2211, SC2288, SC2283, SC2154, SC2283, SC1036, SC1088
- My shellcheck version (`shellcheck --version` or 'online'): online
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit
- [ ] It's not reproducible on shellcheck.net, but I think that's because it's an OS, configuration or encoding issue

#### For new checks and feature suggestions
- [x] shellcheck.net (i.e. the latest commit) currently gives no useful warnings about this
- [ ] I searched through https://github.com/koalaman/shellcheck/issues and didn't find anything related


#### Here's a snippet or screenshot that shows the problem:

```sh
$wgShowExceptionDetails = true;
"$wgDebugLogGroups"['PluggableAuth'] = "/tmp/LDAPPluggable.log";
"$wgDebugLogGroups"['LDAP'] = "/tmp/LDAP.log";
"$wgDebugLogGroups"['MediaWiki\\Extension\\LDAPProvider\\Client'] = '/tmp/LDAPProvider.log';
"$wgDebugLogGroups"['LDAPAuthorization'] = '/tmp/LDAPAuthorisation.log';

"$wgDebugLogGroups"['LDAPUserInfo'] = "/tmp/LDAPUserInfo.log";
"$wgDebugLogGroups"['LDAPGroups'] = "/tmp/LDAPGroups.log";

$safeIPs = array('127.0.0.1','localhost');
$ipsVars = array('HTTP_X_FORWARDED_FOR','HTTP_X_REAL_IP','REMOTE_ADDR');
foreach ($ipsVars as $ipsVar) {
 if (isset($_SERVER[$ipsVar]) && mb_strlen($_SERVER[$ipsVar]) > 3 ) { $wikiRequestIP = $_SERVER[$ipsVar]; break; }
}
$wikiRequestSafe = (isset($wikiRequestIP) && ( in_array($wikiRequestIP,$safeIPs) ));

$wgGroupPermissions['unixmediawiki'] = $wgGroupPermissions['user'];

$wgEmailConfirmToEdit = false;
$wgGroupPermissions['*']['edit'] = false;
$wgGroupPermissions['*']['read'] = false;
$wgGroupPermissions['*']['createaccount'] = false;
$wgGroupPermissions['sysop']['createaccount'] = false;
$wgGroupPermissions['*']['autocreateaccount'] = true;
$wgBlockDisablesLogin = true;


$ldapJsonFile = "$IP/ldap.json";
$ldapConfig = false;
if (is_file($ldapJsonFile) && is_dir("$IP/extensions/LDAPProvider")) {
  $testJson = @json_decode(file_get_contents($ldapJsonFile),true);
  if (is_array($testJson)) {
    $ldapConfig = true;
  } else {
    error_log("Found invalid JSON in file: $IP/ldap.json");
  }
}

if ( $ldapConfig ) {

  wfLoadExtension( 'PluggableAuth' );
  wfLoadExtension( 'LDAPProvider' );
  wfLoadExtension( 'LDAPAuthentication2' );
  wfLoadExtension( 'LDAPAuthorization' );
  wfLoadExtension( 'LDAPUserInfo' );
  wfLoadExtension( 'LDAPGroups' );

  $LDAPProviderDomainConfigs = $ldapJsonFile;
  $wgPluggableAuth_ButtonLabel = "Log In";

  if ($wikiRequestSafe) { $LDAPAuthentication2AllowLocalLogin = true; }
}
```

#### Here's what shellcheck currently says:
```
Line 1	SC2148: Tips depend on target shell and yours is unknown. Add a shebang or a 'shell' directive.
Line 1	SC2154: wgShowExceptionDetails is referenced but not assigned.
Line 1	SC2283: Remove spaces around = to assign (or use [ ] to compare, or quote '=' if literal).
Line 2	SC2211: This is a glob used as a command name. Was it supposed to be in ${..}, array, or is it missing quoting?
Line 2	SC2288: This is interpreted as a command name ending with ']'. Double check syntax.
Line 2	SC2154: wgDebugLogGroups is referenced but not assigned.
Line 2	SC2283: Remove spaces around = to assign (or use [ ] to compare, or quote '=' if literal).
Line 3	SC2211: This is a glob used as a command name. Was it supposed to be in ${..}, array, or is it missing quoting?
Line 3	SC2288: This is interpreted as a command name ending with ']'. Double check syntax.
Line 3	SC2283: Remove spaces around = to assign (or use [ ] to compare, or quote '=' if literal).
Line 4	SC2211: This is a glob used as a command name. Was it supposed to be in ${..}, array, or is it missing quoting?
Line 4	SC2288: This is interpreted as a command name ending with ']'. Double check syntax.
Line 4	SC2283: Remove spaces around = to assign (or use [ ] to compare, or quote '=' if literal).
Line 5	SC2211: This is a glob used as a command name. Was it supposed to be in ${..}, array, or is it missing quoting?
Line 5	SC2288: This is interpreted as a command name ending with ']'. Double check syntax.
Line 5	SC2283: Remove spaces around = to assign (or use [ ] to compare, or quote '=' if literal).
Line 7	SC2211: This is a glob used as a command name. Was it supposed to be in ${..}, array, or is it missing quoting?
Line 7	SC2288: This is interpreted as a command name ending with ']'. Double check syntax.
Line 7	SC2283: Remove spaces around = to assign (or use [ ] to compare, or quote '=' if literal).
Line 8	SC2211: This is a glob used as a command name. Was it supposed to be in ${..}, array, or is it missing quoting?
Line 8	SC2288: This is interpreted as a command name ending with ']'. Double check syntax.
Line 8	SC2283: Remove spaces around = to assign (or use [ ] to compare, or quote '=' if literal).
Line 10	SC2154: safeIPs is referenced but not assigned.
Line 10	SC2283: Remove spaces around = to assign (or use [ ] to compare, or quote '=' if literal).
Line 10	SC1036: '(' is invalid here. Did you forget to escape it?
Line 10	SC1088: Parsing stopped here. Invalid use of parentheses?
```
#### Here's what I wanted or expected to see:

TODO: Describe expected/desired output



#### <img src="https://avatars.githubusercontent.com/u/243351?v=4" width="50">[brother](https://github.com/brother) commented at [2023-08-31 08:52](https://github.com/koalaman/shellcheck/issues/2820#issuecomment-1700630868):

PHP is not supported by shellcheck. Try [pint](https://github.com/laravel/pint), [codesniffer](https://github.com/squizlabs/PHP_CodeSniffer) or some other tool.


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
