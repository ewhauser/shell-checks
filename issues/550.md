# [\#550 Issue](https://github.com/koalaman/shellcheck/issues/550) `closed`: Don't recommend grep -c when using grep -o | wc

#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) opened issue at [2015-11-26T18:01:50Z](https://github.com/koalaman/shellcheck/issues/550):

E.g. `grep -o x <<< xxx | wc -l`

#### <img src="https://avatars.githubusercontent.com/u/21060?v=4" width="50">[taladar](https://github.com/taladar) commented at [2016-03-11T17:19:13Z](https://github.com/koalaman/shellcheck/issues/550#issuecomment-195462898):

It also shouldn't do that when using grep -v


#### <img src="https://avatars.githubusercontent.com/u/644784?v=4" width="50">[hpollinger](https://github.com/hpollinger) commented at [2016-07-13T12:39:16Z](https://github.com/koalaman/shellcheck/issues/550#issuecomment-232342447):

It also shouldn't do that when using any "wc" option except for "-l" on the output of grep, e.g. "wc -c", "wc -w" or "wc -L".


#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2017-01-04T05:28:52Z](https://github.com/koalaman/shellcheck/issues/550#issuecomment-270297558):

Fixed by @Scorpiokat in 85e6c35

#### <img src="https://avatars.githubusercontent.com/u/352559?v=4" width="50">[gwern](https://github.com/gwern) commented at [2019-12-05T00:12:55Z](https://github.com/koalaman/shellcheck/issues/550#issuecomment-561905136):

hpollinger's point doesn't seem to be fixed. I just cabal installed from the Git repo (0.4.6) and checked, and it's still giving incorrect replacements:

```
In /home/gwern/wiki/markdown-lint.sh line 76:
        [ "$(grep -E '^description: ' "$PAGE" | wc --char)" -ge 90 ] || echo 'WARNING: Description metadata too short.'
             ^-- SC2126: Consider using grep -c instead of grep|wc -l.


In /home/gwern/wiki/markdown-lint.sh line 77:
        [ "$(grep -E '^thumbnail: ' "$PAGE" | wc --char)" -ge 90 ] || echo 'WARNING: No thumbnail/illustration defined.'
             ^-- SC2126: Consider using grep -c instead of grep|wc -l.
```

Grep's man page tells me that `grep -c` merely counts lines; I always have exactly one line or less there, and the question is what is the length or number of characters in that line (which is why I use `wc --char` instead of `wc --lines`). I don't see how using `grep -c` could be correct here. So it's still incorrectly recommending alternatives to `wc`.
