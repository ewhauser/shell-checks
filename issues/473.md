# [\#473 Issue](https://github.com/koalaman/shellcheck/issues/473) `open`: SC2094 triggers for foo $(cat bar) > bar

#### <img src="https://avatars.githubusercontent.com/u/1562019?v=4" width="50">[helpermethod](https://github.com/helpermethod) opened issue at [2015-09-24T15:08:51Z](https://github.com/koalaman/shellcheck/issues/473):

Hi,

ShellCheck complains about the implementation of my __inc function:

```
__inc() {
  printf '%d' $(($(< "$1") + 1)) > "$1"
}
```

As far as I understand, there is no race condition here as the read / write don't occur within the same pipe.

I would guess the `$(< "$1")` would first read in the value as a whole, right?

#### <img src="https://avatars.githubusercontent.com/u/197253?v=4" width="50">[kurahaupo](https://github.com/kurahaupo) commented at [2015-09-25T02:21:49Z](https://github.com/koalaman/shellcheck/issues/473#issuecomment-143105727):

I wouldn't guarantee it. Parsing and execution are separate phases, and some expansions are done earlier than others.

And, in the shell grammar, a "pipeline" is **one** or more commands connected by pipes. So yes the read and overwrite redirections _are_ in the same pipeline.


#### <img src="https://avatars.githubusercontent.com/u/302055?v=4" width="50">[deryni](https://github.com/deryni) commented at [2015-09-25T02:26:36Z](https://github.com/koalaman/shellcheck/issues/473#issuecomment-143106258):

Yeah, barring specific spec language indicating that the command substitution _must_ be performed first this seems unnecessarily risky to me. (I took a quick look at the spec but didn't see anything obvious that indicated a required order of operations here.)

It might always work, it might be required to work even I just don't see that anywhere at the moment.


#### <img src="https://avatars.githubusercontent.com/u/1562019?v=4" width="50">[helpermethod](https://github.com/helpermethod) commented at [2015-09-25T07:11:16Z](https://github.com/koalaman/shellcheck/issues/473#issuecomment-143146661):

So I guess it will write the output to a temporary file and then mv it as suggested. Thanks for your answers!


#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2015-09-28T22:01:52Z](https://github.com/koalaman/shellcheck/issues/473#issuecomment-143886464):

You're right, this is a false positive. Here's POSIX:

> .2. The words that are not variable assignments or redirections shall be expanded. If any fields remain following their expansion, the first field shall be considered the command name and remaining fields are the arguments for the command.
> 
> .3. Redirections shall be performed as described in Redirection.

ShellCheck does not account for this, but it should.


#### <img src="https://avatars.githubusercontent.com/u/197253?v=4" width="50">[kurahaupo](https://github.com/kurahaupo) commented at [2022-09-03T05:29:34Z](https://github.com/koalaman/shellcheck/issues/473#issuecomment-1236052782):

In a POSIX-compliant script this is indeed a false positive, so I withdraw my previous "wouldn't guarantee it".

However it's brittle, since a seemingly innocuous change could subvert the necessary preconditions.

For example, even with POSIX, this will break:
```shell
{ printf '%d\n' $(( $(< "$1") + 1 )) ; } >"$1"
```

I would simply read and write as separate statements:
```shell
__inc() {
    local n
    n=$(< "$1") &&
    printf '%d\n' $((n+1)) >"$1"
}
```

Beyond POSIX there are weird combinations where expansions and redirections are necessarily intermingled, but I haven't been able to construct a case where a expansion that's used as input isn't completed before one that's used as an output, unless the ordering is controlled by using a compound command, or the input is process through a pipe such as <(...) or a regular multi-component pipeline.
