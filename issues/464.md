# [\#464 Issue](https://github.com/koalaman/shellcheck/issues/464) `closed`: Shellcheck doesn't recognize [ as a regular command.

#### <img src="https://avatars.githubusercontent.com/u/6459309?v=4" width="50">[Artoria2e5](https://github.com/Artoria2e5) opened issue at [2015-09-19T02:48:13Z](https://github.com/koalaman/shellcheck/issues/464):

Although the misuse of `&&` in `[ expr ]` is common, it's still important to treat `[` as a command, and the check for `&&` / `||` should only happen when the last argument of the last command in the _term_  appears to be `]` (technically both quoted and un-quoted; but people that knows quoting also works since it is only an arg doesn't seem likely to make `&&` mistakes.)

The test content is the same as #463.

``` Bash
# [(){ local IFS=' '; echo -n "$*"; } # shellcheck hangs at this funcdef
# bash: disable the [ builtin
enable -d [; echo -e '#!/bin/bash\necho "$@"'>/tmp/[; chmod +x /tmp/[; PATH=/tmp
[ a && echo b # here we also see shellcheck recgonize && like a syntax keyword; another issue.
```

A quick review on POSIX.1 2013 Volume 3 _Shell & Utilities_ shows that `[` is only a special form of the `test` util, while `[[` and `]]` **may** be keywords.

#### <img src="https://avatars.githubusercontent.com/u/1572859?v=4" width="50">[izabera](https://github.com/izabera) commented at [2015-09-26T01:39:02Z](https://github.com/koalaman/shellcheck/issues/464#issuecomment-143389625):

shellcheck can't determine if your script will produce an error because you _purposely_ wrote something that can only be determined at runtime

how about this?

``` bash
(( RANDOM % 2 )) &&
enable -d [; echo -e '#!/bin/bash\necho "$@"'>/tmp/[; chmod +x /tmp/[; PATH=/tmp
[ a && echo b
```

do you think shellcheck should emit a warning 50% of the times? 


#### <img src="https://avatars.githubusercontent.com/u/6459309?v=4" width="50">[Artoria2e5](https://github.com/Artoria2e5) commented at [2015-09-27T14:39:38Z](https://github.com/koalaman/shellcheck/issues/464#issuecomment-143563362):

@izabera

NO. `[` is never a special syntax structure. It is only a command.


#### <img src="https://avatars.githubusercontent.com/u/2818758?v=4" width="50">[koalaman](https://github.com/koalaman) commented at [2015-09-28T21:51:04Z](https://github.com/koalaman/shellcheck/issues/464#issuecomment-143884110):

Incorrectly treating `[` as a special command is a conscious design decision. 

This gives much better understanding of intent, like `&&` as you mention, and also `||`, `>` for comparisons, `(` for grouping and `(` for regex. 

Unfortunately it also means that it trips up when people do less common things like overriding `[`, or when dynamically generating expressions or using variables as operators.

It's really sad when correctness suffers like this, annoying advanced users in order to better catch clueless newbies' mistakes, but I think ShellCheck is more valuable as a tool for doing it.


#### <img src="https://avatars.githubusercontent.com/u/197253?v=4" width="50">[kurahaupo](https://github.com/kurahaupo) commented at [2015-10-04T22:15:41Z](https://github.com/koalaman/shellcheck/issues/464#issuecomment-145392820):

(update: opened as separate issue #495)
Perhaps this (and similar comments about SC1007) points towards having each warning grouped and graded, so that they can be selected on the command line with keywords rather than numbers.

And being able to put a directive just once in the source to disable particular warnings globally.

Perhaps like these:

```
    # shellcheck: disable special-[ # applies to next line
    # shellcheck: lines 5 disable special-[ # applies to next 5 lines
    # shellcheck: scope disable assignment # applies to end of current compound command
    # shellcheck: global disable arithmetic # applies to rest of file
    # shellcheck: global disable all enable errors enable redirection enable 
    # shellcheck: global quiet
    # shellcheck: global normal
    # shellcheck: global nitpick
    # shellcheck: global lint
```


#### <img src="https://avatars.githubusercontent.com/u/198379?v=4" width="50">[bittorf](https://github.com/bittorf) commented at [2015-10-05T04:35:30Z](https://github.com/koalaman/shellcheck/issues/464#issuecomment-145428197):

@kurahaupo please open another issue for your suggestions
