# [\#2126 Issue](https://github.com/koalaman/shellcheck/issues/2126) `open`: detect unused functions

#### <img src="https://avatars.githubusercontent.com/u/26875430?v=4" width="50">[FinzelStefan](https://github.com/FinzelStefan) opened issue at [2021-01-11 13:52](https://github.com/koalaman/shellcheck/issues/2126):

 For new checks and feature suggestions
- [ x] https://www.shellcheck.net/ (i.e. the latest commit) currently gives no useful warnings about this
- [ x] I searched through https://github.com/koalaman/shellcheck/issues and didn't find anything related

Unused variables are detected by shellcheck but not functions.
bash allows "exporting" a function. 
Using export -f creates an environment variable with the function body. 

Example:
```shell
#!/bin/bash -f
function FUNCTION_UNUSED() {
  VAR_UNUSED=TRUE
}
#eof
```

Here's what I got:
```shell
Line 3:
  VAR_UNUSED=TRUE
  ^-- SC2034: VAR_UNUSED appears unused. Verify use (or export if used externally).
```

Here's what I wanted or expected to see:
```shell
Line 2:
function FUNCTION_UNUSED()
              ^-- SC####: FUNCTION_UNUSED () appears unused. Verify use (or export if used externally).
Line 3:
  VAR_UNUSED=TRUE
  ^-- SC2034: VAR_UNUSED appears unused. Verify use (or export if used externally).
```

#### <img src="https://avatars.githubusercontent.com/u/47339836?v=4" width="50">[walksanatora](https://github.com/walksanatora) commented at [2022-03-20 04:56](https://github.com/koalaman/shellcheck/issues/2126#issuecomment-1073168247):

yeah i was about to suggest this also, good thing i searched to see if someone else suggested it

#### <img src="https://avatars.githubusercontent.com/u/198379?v=4" width="50">[bittorf](https://github.com/bittorf) commented at [2023-04-05 10:06](https://github.com/koalaman/shellcheck/issues/2126#issuecomment-1497237310):

i tried to work around this in a custom check  
using `ctags` from `universal-ctags`, but it is limited:

```
# show all function names:
ctags --sort=no --language-force=sh -x "file.sh"

# iterate over files, search for function names:
grep -q "$name[^(]\|${name}$" "$CODEBASE"
```

But think about this (contructed function names) or  
even function build during execution time using eval:

```
#!/bin/sh

FILE="$1"

function_image_gif()
{
  true
}

MIME="$( file --mime-type --brief "$FILE" )"
MIMEPRE="${MIME%/*}"    # e.g. image
MIMESUB="${MIME#*/}"    # e.g. gif

MYFUNC="function_${MIMEPRE}_${MIMESUB}"
command -v "$MYFUNC" >/dev/null && $MYFUNC
```


-------------------------------------------------------------------------------



[Export of Github issue for [koalaman/shellcheck](https://github.com/koalaman/shellcheck). Generated on 2026.04.01 at 22:59:51.]
