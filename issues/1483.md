# [\#1483 Issue](https://github.com/koalaman/shellcheck/issues/1483) `open`: parser error on valid test

#### <img src="https://avatars.githubusercontent.com/u/1569427?v=4" width="50">[mapreri](https://github.com/mapreri) opened issue at [2019-02-03T13:07:51Z](https://github.com/koalaman/shellcheck/issues/1483):

snippet showing the issue:
```bash
#!/bin/bash

declare -A foo

foo["hello"]=bye

if [ -v foo["hello"] ]; then
    echo "defined! (${foo[hello]})"
else
    echo "not defined.."
fi
```
shellcheck 0.5.0 output:
```
In foo line 7:
if [ -v foo["hello"] ]; then
^-- SC1009: The mentioned syntax error was in this if expression.
   ^-- SC1073: Couldn't parse this test expression. Fix to allow more checks.
        ^-- SC1019: Expected this to be an argument to the unary condition.
                    ^-- SC1020: You need a space before the ].
                    ^-- SC1072: Missing space before ]. Fix any mentioned problems and try again.
```

but that code is just fine for bash (it can't be ported anywhere though).
