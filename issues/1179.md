# [\#1179 Issue](https://github.com/koalaman/shellcheck/issues/1179) `closed`: Inside emacs, SC2148 seems to be overruled 

#### <img src="https://avatars.githubusercontent.com/u/3129206?v=4" width="50">[matthewpersico](https://github.com/matthewpersico) opened issue at [2018-04-12T21:42:50Z](https://github.com/koalaman/shellcheck/issues/1179):

#### For bugs
- Rule Id (if any, e.g. SC1000): SC2148
- My shellcheck version (`shellcheck --version` or 'online'): online
- [x] I tried on shellcheck.net and verified that this is still a problem on the latest commit
- [ ] It's not reproducible on shellcheck.net, but I think that's because it's an OS, configuration or encoding issue

#### For new checks and feature suggestions
- [x] shellcheck.net (i.e. the latest commit) currently gives no useful warnings about this
- [ ] I searched through https://github.com/koalaman/shellcheck/issues and didn't find anything related


#### Here's a snippet or screenshot that shows the problem:

```sh
# -*- sh -*-

```

#### Here's what shellcheck currently says:
```
Line 1	SC2148: Tips depend on target shell and yours is unknown. Add a shebang.
```
#### Here's what I wanted or expected to see:

When inside an editor (to wit, emacs) I do not ever see this error when I do not have a shebang. It guess it is an editor integration thing; is the editor feeding the shell type to shellcheck? I don't think it should.

#### <img src="https://avatars.githubusercontent.com/u/2199511?v=4" width="50">[Nightfirecat](https://github.com/Nightfirecat) commented at [2018-04-12T22:07:43Z](https://github.com/koalaman/shellcheck/issues/1179#issuecomment-380959090):

From [SC2148's wiki page](/koalaman/shellcheck/wiki/SC2148):

> ### Rationale:
> 
> Different shells support different features. To give effective advice, ShellCheck needs to know which shell your script is going to run on. You will get a different numbers of warnings about different things depending on your target shell.
> 
> ShellCheck normally determines your target shell from the shebang (having e.g. `#!/bin/sh` as the first line). The shell can also be specified from the CLI with `-s`, e.g. `shellcheck -s sh file`.
> 
> If you don't specify shebang nor `-s`, ShellCheck gives this message and proceeds with some default (`bash`).
> 
> Note that this error can not be ignored with a [[directive]]. It is not a suggestion to improve your script, but a warning that ShellCheck lacks information it needs to be helpful.

#### <img src="https://avatars.githubusercontent.com/u/3129206?v=4" width="50">[matthewpersico](https://github.com/matthewpersico) commented at [2018-04-12T22:21:33Z](https://github.com/koalaman/shellcheck/issues/1179#issuecomment-380962153):

But that’s my point. I’m in my editor,  shellcheck is being used, there’s no shebang and no shell directive, but shellcheck doesn’t seem to be complaining. If the same file is run through shellcheck on the command line, then I get an SC2148 message.

Update- please ignore my emoji comments. They were fat fingers 

#### <img src="https://avatars.githubusercontent.com/u/3129206?v=4" width="50">[matthewpersico](https://github.com/matthewpersico) commented at [2018-04-12T22:22:51Z](https://github.com/koalaman/shellcheck/issues/1179#issuecomment-380962430):

Ah, but from your comment, it might be that emacs is throwing the -s flag when it invokes shellcheck.  I’ll take a look at the emacs flycheck confit that calls shellcheck.

#### <img src="https://avatars.githubusercontent.com/u/6675894?v=4" width="50">[charlescanato](https://github.com/charlescanato) commented at [2018-04-13T02:44:00Z](https://github.com/koalaman/shellcheck/issues/1179#issuecomment-381004520):

Matthew, this **is** the case. I had my own quota of problems with Flycheck on Emacs, trying to make it use only Shellcheck (it'd use Shellcheck in addition to Bash checking, by default). But Flycheck *always* uses `-s` (even if it's not what you want), so that's why you won't see the warning under it.

#### <img src="https://avatars.githubusercontent.com/u/3129206?v=4" width="50">[matthewpersico](https://github.com/matthewpersico) commented at [2018-04-13T11:15:04Z](https://github.com/koalaman/shellcheck/issues/1179#issuecomment-381104421):

Sounds like it’s time to dig into some lisp code and turn that off. :-). Since this is not a shellcheck issue, I’m going to close it. Thank you all for your help. 

#### <img src="https://avatars.githubusercontent.com/u/3129206?v=4" width="50">[matthewpersico](https://github.com/matthewpersico) commented at [2018-04-18T15:17:05Z](https://github.com/koalaman/shellcheck/issues/1179#issuecomment-382423680):

FYI, as of flycheck-20180326.836/flycheck.el

```lisp
(flycheck-define-checker sh-shellcheck
  "A shell script syntax and style checker using Shellcheck.

See URL `https://github.com/koalaman/shellcheck/'."
  :command ("shellcheck"
            "--format" "checkstyle"
            "--shell" (eval (symbol-name sh-shell))
            (option-flag "--external-sources" flycheck-shellcheck-follow-sources)
            (option "--exclude" flycheck-shellcheck-excluded-warnings list
                    flycheck-option-comma-separated-list)
            "-")
  :standard-input t
  :error-parser flycheck-parse-checkstyle
  :error-filter
  (lambda (errors)
    (flycheck-remove-error-file-names
     "-" (flycheck-dequalify-error-ids errors)))
  :modes sh-mode
  :predicate (lambda () (memq sh-shell flycheck-shellcheck-supported-shells))
  :verify (lambda (_)
            (let ((supports-shell (memq sh-shell
                                        flycheck-shellcheck-supported-shells)))
              (list
               (flycheck-verification-result-new
                :label (format "Shell %s supported" sh-shell)
                :message (if supports-shell "yes" "no")
                :face (if supports-shell 'success '(bold warning)))))))
```

So you would need to remove/override the 

```lisp
"--shell" (eval (symbol-name sh-shell))
```
line to force SC2148 in flycheck mode.


