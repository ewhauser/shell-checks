id: SH-014
name: select-menu-loop
summary: The script uses `select` for a menu-style loop, but that construct is not part of portable `sh`.
shells:
- sh
- dash
example: examples/014.sh
rationale: Build the prompt with `read`, `case`, and a normal loop if the script must run in `sh`.
