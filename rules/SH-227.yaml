id: SH-227
name: su-without-flag
summary: "The `su` command is used without `-l` or `-c`."
shells:
- sh
- bash
- dash
- ksh
example: examples/227.sh
rationale: "Use `su -l user` or `su -c 'command' user` to start a proper login shell."
