id: SH-181
name: function-body-without-braces
summary: A function body is a bare compound command instead of a brace group.
shells:
- sh
- bash
- dash
- ksh
example: examples/181.sh
rationale: "Wrap the function body in `{ ... }` for portability."
