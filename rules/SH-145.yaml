id: SH-145
name: duplicate-redirection
summary: A later redirect for the same stream overrides an earlier one in the same command.
shells:
- sh
- bash
- dash
- ksh
example: examples/145.sh
rationale: Keep only the final redirect, or split the writes into separate commands.
