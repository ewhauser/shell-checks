id: SH-180
name: multiple-loop-variables
summary: A `for` loop tries to assign more than one name from each item in the list.
shells:
- sh
- bash
- dash
- ksh
example: examples/180.sh
rationale: Iterate with one loop variable and split the values inside the loop body instead.
