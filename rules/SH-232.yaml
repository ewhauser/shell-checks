id: SH-232
name: quoted-bash-source
summary: "`$BASH_SOURCE` is quoted without array syntax."
shells:
- sh
- bash
- dash
- ksh
example: examples/232.sh
rationale: "Use `${BASH_SOURCE[0]}` for the current script path."
