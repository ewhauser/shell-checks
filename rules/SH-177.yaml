id: SH-177
name: open-brace-needs-separator
summary: A command starts immediately after `{`, so the shell cannot cleanly recognize the group boundary.
shells:
- sh
- bash
- dash
- ksh
example: examples/177.sh
rationale: Put whitespace, a semicolon, or a newline after `{` before the first command.
