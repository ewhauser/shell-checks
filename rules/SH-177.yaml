id: SH-177
name: missing-semicolon-before-brace
summary: A closing brace follows a command without a semicolon or newline.
shells:
- sh
- bash
- dash
- ksh
example: examples/177.sh
rationale: "Add a semicolon before `}` so the shell sees the end of the group."
