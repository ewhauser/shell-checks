id: SH-053
name: posix-file-slurp-shortcut
summary: The `$(<file)` shortcut depends on Bash-style parsing, so it is not portable in `sh` scripts.
shells:
- sh
- dash
example: examples/053.sh
rationale: Read the file with a POSIX-safe pattern such as `cat` or a `read` loop when portability matters.
