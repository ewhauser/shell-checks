id: SH-178
name: empty-function-body
summary: A function is defined with braces but no commands inside them.
shells:
- sh
- bash
- dash
- ksh
example: examples/178.sh
rationale: Add a placeholder command such as `:` or `true` if the function is meant to do nothing.
