id: SH-179
name: close-brace-needs-separator
summary: A `}` is reached before the preceding command is clearly terminated.
shells:
- sh
- bash
- dash
- ksh
example: examples/179.sh
rationale: End the last command with `;` or a newline before `}`.
