id: SH-009
name: function-keyword-in-sh
summary: The `function` keyword declares functions in a non-POSIX form.
shells:
- sh
- dash
example: examples/009.sh
rationale: Write functions as `name() { ...; }` when the script needs to run under `sh`.
