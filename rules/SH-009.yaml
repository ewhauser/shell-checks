id: SH-009
name: function-keyword
summary: "The script uses the `function` keyword, which is not part of portable `sh` syntax."
shells:
- sh
- dash
example: examples/009.sh
rationale: "Define shell functions with the plain `name() { ...; }` form when portability matters."
