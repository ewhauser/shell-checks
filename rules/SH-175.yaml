id: SH-175
name: if-missing-then
summary: An `if` header is followed by body text without the separator that starts the branch.
shells:
- sh
- bash
- dash
- ksh
example: examples/175.sh
rationale: Insert `then` after the condition before writing the branch body.
