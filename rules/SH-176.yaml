id: SH-176
name: else-missing-then
summary: An `else` branch shows up before the preceding `if` branch has been opened with `then`.
shells:
- sh
- bash
- dash
- ksh
example: examples/176.sh
rationale: Add the missing `then` so the `else` belongs to a complete `if` structure.
