id: SH-247
name: egrep-deprecated
summary: "The `egrep` command is used instead of `grep -E`."
shells:
- sh
- bash
- dash
- ksh
example: examples/247.sh
rationale: "Replace `egrep` with `grep -E` as egrep is deprecated."
