id: SH-049
name: find-output-loop
summary: "Expanding `find` output in a `for` loop loses pathname boundaries and re-splits file names."
shells:
- sh
- bash
- dash
- ksh
example: examples/049.sh
rationale: "Feed `find` results to a consumer that understands line or NUL boundaries instead of shell word splitting."
