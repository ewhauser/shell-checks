id: SH-151
name: eval-on-array
summary: "`eval` is used to execute composed command text."
shells:
- sh
- bash
- dash
- ksh
example: examples/151.sh
rationale: "Avoid `eval`; call the command directly or pass arguments in an array."
