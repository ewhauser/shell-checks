id: SH-013
name: standalone-arithmetic-command
summary: The script invokes arithmetic evaluation as a command, which portable `sh` is not required to support.
shells:
- sh
- dash
example: examples/013.sh
rationale: Move the calculation into a POSIX arithmetic form or use a shell that understands `((...))`.
