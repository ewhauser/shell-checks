id: SH-127
name: array-subscript-condition
summary: Array-like indexing inside `[[ ... ]]` can be parsed as a pattern instead of the value you expected.
shells:
- sh
- bash
- dash
- ksh
example: examples/127.sh
rationale: Use portable parameter handling or switch to a shell with genuine arrays.
