id: SH-168
name: collapsed-echo-spacing
summary: Repeated spacing in an `echo` command is collapsed before printing, so it does not preserve formatting.
shells:
- sh
- bash
- dash
- ksh
example: examples/168.sh
rationale: Quote the intended text or use `printf` when the exact spacing matters.
