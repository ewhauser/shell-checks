id: SH-142
name: case-pattern-variable
summary: A `case` pattern is built from an expansion that can change the match semantics.
shells:
- sh
- bash
- dash
- ksh
example: examples/142.sh
rationale: Use a fixed pattern or quote the value elsewhere if you want a literal comparison.
