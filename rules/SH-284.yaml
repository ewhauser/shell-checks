id: SH-284
name: malformed-arithmetic-in-condition
summary: An arithmetic expansion inside a conditional test is malformed.
shells:
- sh
- bash
- dash
- ksh
example: examples/284.sh
rationale: "Close the `[[ ]]` properly and use `$(( ))` correctly."
