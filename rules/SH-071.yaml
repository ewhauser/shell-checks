id: SH-071
name: grep-output-as-boolean
summary: Using grep's captured output as a condition makes control flow depend on printed text instead of match success.
shells:
- sh
- bash
- dash
- ksh
example: examples/071.sh
rationale: Check grep's exit status directly, or match the text with a simpler, non-capturing test.
