id: SH-128
name: scoped-env-assignment
summary: An environment override hidden inside a substitution can be mistaken for a plain value assignment.
shells:
- sh
- bash
- dash
- ksh
example: examples/128.sh
rationale: Make the temporary environment change explicit so the intent is clear and the output capture stays obvious.
