id: SH-198
name: ls-piped-to-xargs
summary: "The output of `ls` is piped into another command."
shells:
- sh
- bash
- dash
- ksh
example: examples/198.sh
rationale: "Use a glob or `find` instead of parsing `ls` output."
