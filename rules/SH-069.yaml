id: SH-069
name: literal-test-comparison
summary: A test made only of fixed literals cannot react to runtime data and is usually a forgotten variable reference.
shells:
- sh
- bash
- dash
- ksh
example: examples/069.sh
rationale: Compare values that come from inputs or variables so the branch can actually change.
