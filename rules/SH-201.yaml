id: SH-201
name: short-circuit-fallthrough
summary: "A `&&`/`||` chain may not branch as intended."
shells:
- sh
- bash
- dash
- ksh
example: examples/201.sh
rationale: "Use an explicit `if`/`else` to avoid unexpected fallthrough."
