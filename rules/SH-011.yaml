id: SH-011
name: process-substitution-redirection
summary: The script wires command input or output through `<(...)` or `>(...)`, which plain `sh` does not define.
shells:
- sh
- dash
example: examples/011.sh
rationale: Use a pipe, a temporary file, or ordinary redirection to move data between commands portably.
