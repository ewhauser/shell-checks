id: SH-153
name: zsh-only-parameter-flag
summary: Uses a parameter-expansion flag that is specific to zsh.
shells:
- sh
- bash
- dash
- ksh
example: examples/153.sh
rationale: Rewrite the transformation with portable shell logic, or run the script under a shell that supports the syntax.
