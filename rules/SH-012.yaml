id: SH-012
name: ansi-c-string-quoting
summary: The script embeds escape sequences with `$'...'`, which plain `sh` does not standardize.
shells:
- sh
- dash
example: examples/012.sh
rationale: Use regular shell quoting with explicit escaping, or switch to a shell that supports this string form.
