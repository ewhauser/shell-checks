id: SH-147
name: spaced-assignment-operator
summary: Adds whitespace around `=` so the line is parsed as separate words rather than a variable assignment.
shells:
- sh
- bash
- dash
- ksh
example: examples/147.sh
rationale: Write assignments as `name=value`, or quote `=` when you truly mean a literal character.
