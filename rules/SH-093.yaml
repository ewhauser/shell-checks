id: SH-093
name: if-elif-bash-test
summary: "A POSIX sh script uses a bash-style `[[ ... ]]` test in an `elif` clause."
shells:
- sh
- bash
- dash
- ksh
example: examples/093.sh
rationale: "Use a portable `[` test or switch the script to bash."
