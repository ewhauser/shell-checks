id: SH-261
name: extglob-in-sh
summary: "An extended glob pattern `@()` is used in a POSIX shell context."
shells:
- sh
- dash
example: examples/261.sh
rationale: "Use a `case` statement or POSIX glob patterns instead."
