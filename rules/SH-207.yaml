id: SH-207
name: escaped-negation-in-test
summary: "A backslash-escaped `!` is used in a test expression."
shells:
- sh
- bash
- dash
- ksh
example: examples/207.sh
rationale: "Use `!` without the backslash for negation in test."
