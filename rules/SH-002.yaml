id: SH-002
name: read-without-raw-mode
summary: Reading input without raw mode can rewrite backslashes before the text is used later.
shells:
- sh
- bash
- dash
- ksh
example: examples/002.sh
rationale: Use `read -r` when the script needs to preserve the input exactly as typed.
