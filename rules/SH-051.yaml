id: SH-051
name: compound-test-operator
summary: "Using `-a` or `-o` inside a single `[` expression compresses boolean logic into a form that is easy to misread."
shells:
- sh
- bash
- dash
- ksh
example: examples/051.sh
rationale: Split the condition into explicit branches so the intended logic stays obvious.
