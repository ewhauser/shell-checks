id: SH-061
name: echo-backslash-escapes-are-unreliable
summary: Backslash escapes in `echo` vary across shells and flags, so the same command can print different text.
shells:
- sh
- bash
- ksh
example: examples/061.sh
rationale: Use `printf` when you need consistent escape handling or exact output.
