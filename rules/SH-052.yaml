id: SH-052
name: local-outside-function
summary: `local` only has meaning inside a function, so using it elsewhere leaves you with a script-level variable instead of a function-local one.
shells:
- bash
- dash
example: examples/052.sh
rationale: Put the declaration inside the function that owns it, or use a plain assignment when the value should remain global.
