id: SH-019
name: coprocess
summary: The script starts a coprocess, but that concurrency primitive is not part of portable `sh`.
shells:
- sh
- dash
example: examples/019.sh
rationale: Replace it with background jobs, pipes, or a helper process if the script must run in `dash`.
