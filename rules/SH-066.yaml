id: SH-066
name: echo-in-command-substitution
summary: Wrapping `echo` in `$(...)` adds an extra command where a direct emitter or the original command would do.
shells:
- sh
- bash
- dash
- ksh
example: examples/066.sh
rationale: Print directly with `printf` or use the inner command's output without an extra `echo` layer.
