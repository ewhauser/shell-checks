id: SH-018
name: array-assignment
summary: "The script assigns a shell array, which is not part of standard `sh`."
shells:
- sh
- dash
example: examples/018.sh
rationale: Use separate variables or positional parameters instead of array assignment in portable shell.
