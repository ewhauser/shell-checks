id: SH-018
name: array-assignment
summary: The script initializes an array-like value, but POSIX `sh` only provides scalar variables.
shells:
- sh
- dash
example: examples/018.sh
rationale: Use separate variables, positional parameters, or another representation that works in plain `sh`.
