id: SH-073
name: bare-literal-test
summary: A one-word test built from a fixed literal is just a constant truth value.
shells:
- sh
- bash
- dash
- ksh
example: examples/073.sh
rationale: Use a real comparison, or spell the branch directly with `true` or `false` if the outcome is predetermined.
