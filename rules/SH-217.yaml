id: SH-217
name: glob-in-test-comparison
summary: "A glob pattern on the right side of `==` inside `[ ]` is not expanded."
shells:
- sh
- bash
- dash
- ksh
example: examples/217.sh
rationale: "Use `[[ ]]` or `case` for pattern matching."
