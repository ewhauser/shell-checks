id: SH-144
name: stateful-redirect-target
summary: A redirection target performs an expansion that changes shell state while the filename is being resolved.
shells:
- sh
- bash
- ksh
example: examples/144.sh
rationale: Compute the value first, then redirect to the resulting path.
