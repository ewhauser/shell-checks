id: SH-237
name: find-or-without-grouping
summary: "A `find` command uses `-o` without grouping the alternatives."
shells:
- sh
- bash
- dash
- ksh
example: examples/237.sh
rationale: "Add parentheses around the `-o` alternatives in `find`."
