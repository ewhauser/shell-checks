id: SH-311
name: array-to-string-conversion
summary: An array variable is converted to a flat string then used as a string.
shells:
- sh
- bash
- dash
- ksh
example: examples/311.sh
rationale: Keep the array intact or explicitly join it with a delimiter.
