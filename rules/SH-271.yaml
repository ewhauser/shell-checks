id: SH-271
name: plus-equals-in-sh
summary: "The `+=` append operator is used in POSIX sh."
shells:
- sh
- dash
example: examples/271.sh
rationale: "Use `VAR=\"${VAR}value\"` for portable string append."
