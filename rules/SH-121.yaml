id: SH-121
name: c-style-comment-text
summary: C-style comment markers are treated as code, not shell comments.
shells:
- sh
- bash
- dash
- ksh
example: examples/121.sh
rationale: Use `#` comments so the script stays valid in POSIX shells.
