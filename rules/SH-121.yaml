id: SH-121
name: c-style-comment
summary: C-style comment text is not a shell comment.
shells:
- sh
- bash
- dash
- ksh
example: examples/121.sh
rationale: "Use `#` comments in shell scripts instead of `/* ... */`."
