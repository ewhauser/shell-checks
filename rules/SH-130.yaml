id: SH-130
name: unquoted-glob-command
summary: A wildcard path in command position is expanded as a command name candidate instead of a literal path.
shells:
- sh
- bash
- dash
- ksh
example: examples/130.sh
rationale: Quote the pathname or expand the match explicitly before trying to execute it.
