id: SH-163
name: read-without-target-variable
summary: Calls `read` in POSIX sh without naming a destination for the input.
shells:
- sh
- dash
example: examples/163.sh
rationale: Provide a variable name or use a different input-handling pattern, because bare `read` is not defined consistently there.
