id: SH-055
name: expr-arithmetic
summary: Using expr to combine numbers spawns a separate process for work the shell can already do itself.
shells:
- sh
- bash
- dash
- ksh
example: examples/055.sh
rationale: Use shell arithmetic expansion when you only need a numeric result.
