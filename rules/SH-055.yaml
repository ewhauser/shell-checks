id: SH-055
name: expr-for-arithmetic
summary: Using `expr` for arithmetic adds an external command where the shell can already do the math itself.
shells:
- sh
- bash
- dash
- ksh
example: examples/055.sh
rationale: Prefer arithmetic expansion for numeric calculations unless you are keeping legacy compatibility on purpose.
