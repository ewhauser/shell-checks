id: SH-058
name: tr-lowercase-range
summary: A lowercase range in `tr` can expand differently under different locales, so it may miss letters you expected to translate.
shells:
- sh
- bash
- dash
- ksh
example: examples/058.sh
rationale: Use `[:lower:]` or an explicit character list when you need predictable lowercase handling.
