id: SH-006
name: double-bracket-test
summary: The bash-style `[[ ... ]]` test is unavailable in POSIX `sh`.
shells:
- sh
- dash
example: examples/006.sh
rationale: Use `[ ... ]` or `test`, or require a shell that supports `[[ ... ]]`.
