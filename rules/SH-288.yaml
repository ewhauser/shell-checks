id: SH-288
name: string-compared-with-eq
summary: "A string value is compared using the numeric `-eq` operator."
shells:
- sh
- bash
- dash
- ksh
example: examples/288.sh
rationale: "Use `==` or `=` for string comparisons in test expressions."
