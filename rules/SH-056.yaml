id: SH-056
name: ps-grep-pipeline
summary: Searching `ps` output with `grep` matches formatted text rather than the actual process list, so the check is easy to get wrong.
shells:
- sh
- bash
- dash
- ksh
example: examples/056.sh
rationale: Use `pgrep` or another process-aware lookup instead of parsing `ps` output.
