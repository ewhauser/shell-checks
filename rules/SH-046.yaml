id: SH-046
name: line-oriented-input
summary: A loop over command output can turn records into whitespace-delimited words instead of preserving each original line.
shells:
- sh
- bash
- dash
- ksh
example: examples/046.sh
rationale: Read data with a line-aware loop or redirect so the shell does not reshape it into separate words.
