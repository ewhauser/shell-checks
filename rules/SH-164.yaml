id: SH-164
name: test-group-in-subshell
summary: A grouped test runs in a subshell, so any work inside it happens in a separate process.
shells:
- sh
- bash
- dash
- ksh
example: examples/164.sh
rationale: Use a brace group when you only need grouping and want to avoid spawning another shell.
