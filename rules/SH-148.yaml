id: SH-148
name: bare-slash-marker
summary: "A lone `*/` token is not valid shell syntax."
shells:
- sh
- bash
- dash
- ksh
example: examples/148.sh
rationale: "Use `#` for comments and avoid stray comment-like delimiters."
