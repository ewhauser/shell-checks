id: SH-017
name: combined-stream-redirection
summary: The script uses a redirection form that merges stdout and stderr, which portable `sh` does not support.
shells:
- sh
- dash
example: examples/017.sh
rationale: Redirect the two streams separately, or use a portable grouping pattern if you need both outputs captured together.
