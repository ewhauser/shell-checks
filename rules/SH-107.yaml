id: SH-107
name: sh-function-parameters
summary: A POSIX shell function is written with a parameter list instead of the portable form.
shells:
- sh
- bash
- dash
- ksh
example: examples/107.sh
rationale: Define the function with `name() { ...; }` and read arguments from positional parameters.
