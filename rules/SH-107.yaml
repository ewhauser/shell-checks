id: SH-107
name: function-params-in-sh
summary: "A POSIX `sh` function is written with parameter syntax that ShellCheck rejects."
shells:
- sh
- bash
- dash
- ksh
example: examples/107.sh
rationale: "Use the portable `name() { ...; }` form and read arguments through `$1`, `$2`, and so on."
