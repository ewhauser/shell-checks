id: SH-075
name: empty-conditional-expression
summary: An empty conditional has no operand, so it cannot evaluate any real runtime condition.
shells:
- sh
- bash
- dash
- ksh
example: examples/075.sh
rationale: Fill in the expression you intended to check, or use `true` or `false` when you want a fixed result.
