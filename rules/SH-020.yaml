id: SH-020
name: let-command
summary: "The script relies on `let`, which portable `sh` does not define."
shells:
- sh
- dash
example: examples/020.sh
rationale: Prefer arithmetic expansion or another portable arithmetic mechanism instead.
