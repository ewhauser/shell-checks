id: SH-020
name: let-arithmetic-command
summary: The script uses `let` for math, but that builtin is not defined by portable `sh`.
shells:
- sh
- dash
example: examples/020.sh
rationale: Use arithmetic expansion or another POSIX-compatible arithmetic expression instead.
