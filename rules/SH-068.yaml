id: SH-068
name: command-substitution-array-assignment
summary: Assigning command output to an array directly makes the shell re-parse the text as shell syntax.
shells:
- sh
- bash
- dash
- ksh
example: examples/068.sh
rationale: Read the output with a structured reader or split it deliberately instead of feeding it straight into the array.
