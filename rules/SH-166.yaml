id: SH-166
name: non-absolute-shebang-interpreter
summary: The shebang names an interpreter without an absolute path, so the script depends on lookup behavior.
shells:
- sh
- bash
- dash
- ksh
example: examples/166.sh
rationale: Use an absolute interpreter path or `/usr/bin/env` so the kernel can launch the script consistently.
