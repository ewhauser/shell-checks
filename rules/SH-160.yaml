id: SH-160
name: redirected-output-inside-substitution
summary: Places a redirection inside a substitution so the captured text never receives the command's output.
shells:
- sh
- bash
- dash
- ksh
example: examples/160.sh
rationale: Move the redirection outside the substitution, or duplicate the stream with a tool like `tee` if you need both effects.
