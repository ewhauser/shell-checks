id: SH-172
name: backslash-escape-inside-single-quotes
summary: A backslash inside single quotes stays literal, so it does not create an escape sequence.
shells:
- sh
- bash
- dash
- ksh
example: examples/172.sh
rationale: Use double quotes or ANSI-C quoting when you need the backslash to introduce special characters.
