id: SH-007
name: double-equals-in-bracket-test
summary: Using `==` inside `[` is not supported by POSIX `sh`.
shells:
- sh
- dash
example: examples/007.sh
rationale: Use `=` for string comparisons in `sh`, or switch to a shell whose test syntax accepts `==`.
