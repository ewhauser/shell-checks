id: SH-183
name: indirect-array-expansion
summary: An array reference is built from another variable in a way the shell cannot parse directly.
shells:
- sh
- bash
- dash
- ksh
example: examples/183.sh
rationale: Use a supported indirection form or add the braces the shell needs around the array reference.
