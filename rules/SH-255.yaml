id: SH-255
name: xargs-with-inline-replace
summary: "The `-i` flag for `xargs` is deprecated."
shells:
- sh
- bash
- dash
- ksh
example: examples/255.sh
rationale: "Use `-I{}` instead of `-i` with `xargs`."
