id: SH-134
name: pipe-into-kill
summary: `kill` appears at the end of a pipeline even though it does not read piped input.
shells:
- sh
- bash
- dash
- ksh
example: examples/134.sh
rationale: Pass the process IDs as arguments, or use a tool like `xargs` to build that argument list explicitly.
