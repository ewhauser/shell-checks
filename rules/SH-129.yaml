id: SH-129
name: misordered-fd-redirection
summary: A file-descriptor redirection with the symbols in the wrong order turns into a different token sequence.
shells:
- sh
- bash
- dash
- ksh
example: examples/129.sh
rationale: Write the descriptor operator in a conventional form such as `2>&1` so the shell parses it as a redirection.
