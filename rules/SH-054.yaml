id: SH-054
name: echo-option-flags
summary: Passing options to `echo` makes the result depend on the shell's built-in behavior and the arguments being printed.
shells:
- sh
example: examples/054.sh
rationale: Use `printf` for predictable output instead of relying on `echo` switches.
