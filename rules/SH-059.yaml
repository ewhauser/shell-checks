id: SH-059
name: tr-uppercase-range
summary: An uppercase range in `tr` can also vary by locale, which can make character mapping incomplete on some systems.
shells:
- sh
- bash
- dash
- ksh
example: examples/059.sh
rationale: Use `[:upper:]` or an explicit character list when you need predictable uppercase handling.
