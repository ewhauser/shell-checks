id: SH-112
name: nested-else-if
summary: An `else if` sequence starts a fresh conditional instead of using the dedicated branch form.
shells:
- sh
- bash
- dash
- ksh
example: examples/112.sh
rationale: Use `elif` for the next branch, or put the inner `if` on its own line if you truly need nesting.
