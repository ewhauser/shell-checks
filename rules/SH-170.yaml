id: SH-170
name: unnecessary-explicit-status-hand-off
summary: The script immediately feeds `$?` back into `return` or `exit`, which usually adds no new information.
shells:
- sh
- bash
- dash
- ksh
example: examples/170.sh
rationale: Let the surrounding command or function carry the status naturally unless you need to save it first.
