id: SH-239
name: export-with-positional-params
summary: "The `export` command is called with `$@` which may not work as intended."
shells:
- sh
- bash
- dash
- ksh
example: examples/239.sh
rationale: Export specific variable names rather than positional parameters.
