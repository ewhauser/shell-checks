id: SH-113
name: open-double-quote
summary: A double-quoted string is opened inside another quoted string and ShellCheck thinks it was never closed.
shells:
- sh
- bash
- dash
- ksh
example: examples/113.sh
rationale: "Avoid embedding raw shell code inside an `echo` string; use a here-doc or escape the nested quotes explicitly."
