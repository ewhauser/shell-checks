id: SH-039
name: unassigned-variable-reference
summary: The script reads a variable that is never initialized in the file.
shells:
- sh
- bash
- dash
- ksh
example: examples/039.sh
rationale: Initialize the value first or guard the read so typos and missing setup do not pass silently.
