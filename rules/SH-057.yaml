id: SH-057
name: ls-grep-pipeline
summary: Piping ls into grep treats a directory listing like a data format, which is brittle.
shells:
- sh
- bash
- dash
- ksh
example: examples/057.sh
rationale: Use shell patterns or filename-aware tools instead of parsing ls output.
