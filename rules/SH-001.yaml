id: SH-001
name: unquoted-argument-expansion
summary: Unquoted parameter expansion in a normal argument position can split one value into several words or match path patterns unexpectedly.
shells:
- sh
- bash
- dash
- ksh
example: examples/001.sh
rationale: Quote ordinary parameter expansions unless the script deliberately relies on shell field splitting and pathname matching.
