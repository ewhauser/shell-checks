id: SH-185
name: ifs-equals-ambiguity
summary: "Setting IFS to `=` with `IFS==` looks like a comparison."
shells:
- sh
- bash
- dash
- ksh
example: examples/185.sh
rationale: "Quote the delimiter or use `IFS='='` to avoid ambiguity."
