id: SH-008
name: local-in-posix-function
summary: Declaring a variable with `local` depends on shell-specific function scoping.
shells:
- sh
example: examples/008.sh
rationale: Use plain assignments or another scoping strategy in `sh`, or target a shell that implements `local`.
