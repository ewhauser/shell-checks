id: SH-154
name: nested-parameter-expansion
summary: Nests one parameter expansion inside another in a way that depends on zsh-specific substitution behavior.
shells:
- sh
- bash
- dash
- ksh
example: examples/154.sh
rationale: Flatten the logic into ordinary shell variables or branches so the expression remains portable.
