id: SH-044
name: rm-glob-on-variable-path
summary: "Combining a variable directory and a glob inside `rm -rf` makes it easy to delete more than intended."
shells:
- sh
- bash
- dash
- ksh
example: examples/044.sh
rationale: Validate the target path or use a narrower deletion pattern before removing matching files.
