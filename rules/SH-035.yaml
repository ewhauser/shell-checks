id: SH-035
name: legacy-arithmetic-expansion
summary: The script uses the old bracketed arithmetic form instead of the modern arithmetic expansion syntax.
shells:
- sh
- bash
- dash
- ksh
example: examples/035.sh
rationale: "Prefer `$((...))` for arithmetic so the expression reads like arithmetic rather than shell syntax."
